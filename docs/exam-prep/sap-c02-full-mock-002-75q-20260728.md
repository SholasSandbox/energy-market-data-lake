# AWS Certified Solutions Architect - Professional (SAP-C02)

## Full Mock 002 - 75 Questions

Last revised: 2026-07-29

- **Mode:** Fresh closed-book full simulation
- **Time limit:** 180 minutes
- **Question types:** 48 single-response and 27 multiple-response questions
- **Scoring rule:** Multiple-response questions receive credit only when every
  required option, and no extra option, is selected.

This is an original practice examination aligned to the current SAP-C02 domain
weightings. It is not an AWS examination and does not reproduce live exam
questions.

### Attempt rules

1. Keep lessons, reviews, answer keys, documentation, search, and AI assistance
   closed until the submission is frozen.
2. Select one answer unless the question says **Choose TWO** or **Choose
   THREE**.
3. Record start and end times and any uncertain question numbers.
4. Complete the answer template at the end of this document.
5. Change `Submission status` to `FROZEN` before requesting marking. Do not
   revise a frozen response.

---

## Questions

### 1

A company has 80 AWS accounts in AWS Organizations. Employees authenticate
through a corporate identity provider. Platform administrators need to assign
different access levels to groups across selected accounts without creating
IAM users or maintaining a separate federation role manually in every account.

Which solution meets these requirements with the LEAST operational overhead?

A. Create IAM users in the management account and allow them to assume roles in
each member account.

B. Integrate the identity provider with IAM Identity Center, define permission
sets, and create group-to-account assignments.

C. Create Amazon Cognito identity pools for the employees and map each pool to
an IAM role in every account.

D. Store corporate credentials in AWS Secrets Manager and rotate them from a
central security account.

### 2 - Choose THREE

A company is building a ticket-sales API. During popular releases, requests
arrive in unpredictable bursts. A request must be durably accepted within two
seconds, tickets for the same event must be processed in order, and a retried
request must not create a second reservation. The company wants a serverless
design.

Which THREE actions meet these requirements?

A. Place accepted commands on an SQS FIFO queue, using the event ID as the
message group ID.

B. Configure API Gateway to wait synchronously until all reservation processing
is complete.

C. Publish commands only to an SNS standard topic with a direct Lambda
subscription.

D. Use a client-generated reservation ID as the SQS message deduplication ID and
as a business idempotency key.

E. Store every request on an instance store volume before acknowledging it.

F. Process messages with Lambda and use a DynamoDB conditional write before
creating a reservation.

### 3

A global TCP application runs behind Network Load Balancers in two Regions.
Clients retain DNS answers and long-lived connections longer than the company
can control. Regional failover must begin within seconds and must not depend on
clients waiting for DNS cache expiration. Static entry-point IP addresses are
also required.

Which solution is MOST appropriate?

A. Route 53 failover records controlled by Route 53 ARC routing controls

B. Route 53 latency records with a 60-second TTL

C. CloudFront origin failover between the two Network Load Balancers

D. AWS Global Accelerator with both Network Load Balancers as regional
endpoints

### 4

A company must assess 1,200 VMware virtual machines. Security policy prohibits
installing software inside guest operating systems. The migration team needs VM
inventory and utilization metrics and also needs server-to-server IP and port
dependency data for wave planning. The team can provide the required server
credentials and open the required management ports from a collector appliance.

Which discovery approach meets the requirements?

A. Deploy the Application Discovery Service Agentless Collector, configure its
VMware vCenter module, and enable its Network Data Collection module.

B. Install Application Discovery Agent on every guest operating system.

C. Deploy only the legacy Agentless Discovery Connector and infer dependencies
from CPU utilization.

D. Use AWS DataSync task reports to discover application network dependencies.

### 5 - Choose TWO

A research company receives genomic files through an API. Each accepted file
must be scanned and catalogued. Scanning can take 20 minutes, traffic is bursty,
and callers must receive an acknowledgement without waiting for scanning. A
failed scan must not cause the upload metadata to be lost.

Which TWO components should be added to the design?

A. Invoke the scanner synchronously from API Gateway with a 30-minute
integration timeout.

B. Store upload metadata durably and enqueue a work item in Amazon SQS after the
API validates the request.

C. Publish the work item to an SNS topic that has no subscriptions.

D. Use an SQS-triggered compute worker and configure a dead-letter queue for
repeated failures.

E. Store the only copy of the metadata in the worker's local temporary storage.

### 6

A workload OU has an SCP that denies requests when `aws:RequestedRegion` is not
`eu-west-1` or `eu-central-1`. Administrators can no longer manage Route 53
hosted zones. The company wants to preserve the regional restriction without
allowing general workload deployment in additional Regions.

What should a solutions architect do?

A. Add `us-east-1` to the approved Region list for every AWS service.

B. Replace the SCP with permissions boundaries on administrator roles.

C. Exclude the required Route 53 actions in the SCP's `NotAction` list for
global-service endpoint operations.

D. Add an identity policy that explicitly allows `route53:*` to every
administrator role.

### 7

An I/O-intensive database uses an EBS volume. During peak periods, the volume
exhausts burst credits and reaches its throughput ceiling. Storage consumption
is only 30%, but the workload requires sustained 45,000 IOPS, high throughput,
sub-millisecond latency, and the highest EBS durability class.

Which storage change best meets the requirements?

A. Increase the size of the existing gp2 volume until it has enough baseline
IOPS.

B. Move the data to an st1 volume and increase read-ahead.

C. Move the data to an io2 Block Express volume and provision the required IOPS.

D. Use an sc1 volume with RAID 0 striping.

### 8 - Choose TWO

A manufacturer is planning migration waves for a mixed estate of physical Linux
and Windows servers. It needs host-level process information and inbound and
outbound TCP connection evidence. Executives also need one view of discovered
applications and migration status across AWS and partner migration tools.

Which TWO services meet these requirements?

A. Application Discovery Service Agentless Collector VMware vCenter module

B. AWS DataSync

C. Application Discovery Service Agent installed on the target servers

D. AWS Transfer Family

E. AWS Migration Hub

### 9

An application uses a DynamoDB global table configured for multi-Region
eventual consistency (MREC). A client writes an item in Region A and immediately
requests a strongly consistent read of the same key from the local replica in
Region B.

What should the application expect?

A. DynamoDB rejects the request with `ValidationException` because all global
table replica reads must be eventually consistent.

B. The read is strongly consistent with Region B's local replica state but can
return the version that existed before the write in Region A was replicated.

C. The read always returns the Region A write because strong consistency is
global for all global tables.

D. DynamoDB automatically forwards the read to Region A.

### 10

A company has two Direct Connect paths to AWS. A data-centre router learns
`10.40.0.0/16` over the normal path. For a controlled test, traffic to
`10.40.18.0/24` must use the second path, without changing routing for the rest
of the `/16`.

Which action is MOST effective?

A. Advertise `10.40.18.0/24` over the second BGP session while retaining the
`/16` advertisement over the normal path.

B. Prepend the AS path on the normal `/16` advertisement once.

C. Set a lower MED on the second path while advertising only the same `/16` on
both paths.

D. Configure a Route 53 weighted record for `10.40.18.0/24`.

### 11 - Choose TWO

A company serves product pages through CloudFront. The origin receives many
requests that should have been cache hits. Analysis shows that every viewer
request contains a unique analytics cookie and several marketing query strings,
none of which changes the returned product page. The origin needs only a
language header to select content.

Which TWO changes will improve cache efficiency while preserving correct
responses?

A. Create a cache policy that includes only the language header in the cache
key.

B. Include every viewer cookie and query string in the cache key.

C. Set the minimum, default, and maximum TTL values to zero.

D. Use an origin request policy to forward only values that the origin actually
requires.

E. Replace CloudFront with a Network Load Balancer.

### 12

A company must copy 70 TB from an on-premises NFS server to Amazon S3 over a
10-Gbps Direct Connect connection. Files will continue to change for two weeks,
so the company needs repeated incremental transfers, scheduling, encryption in
transit, and end-to-end verification reports.

Which solution meets the requirements with the LEAST development effort?

A. Install the AWS CLI on every application server and schedule recursive
`s3 sync` commands.

B. Create an Amazon Kinesis Data Firehose delivery stream for the NFS files.

C. Use an AWS DMS replication instance with full load and change data capture.

D. Deploy an AWS DataSync agent, define NFS and S3 locations, and run scheduled
DataSync tasks with verification enabled.

### 13

A company is designing disaster recovery for an Aurora PostgreSQL application.
The business requires an RPO below one minute and an RTO below five minutes.
The recovery Region can run a smaller application fleet continuously, and the
database engine must remain PostgreSQL compatible. The company does not require
simultaneous writes in both Regions.

Which architecture best meets the requirements?

A. Nightly Aurora snapshots copied to the recovery Region and a cold
application stack

B. Aurora Global Database with a secondary cluster in the recovery Region and
a warm standby application stack

C. A Multi-AZ Aurora cluster in the primary Region and an empty VPC in the
recovery Region

D. Weekly logical exports to S3 with on-demand restoration

### 14 - Choose TWO

A company uses AWS Organizations. Security administrators must prevent
supported S3 resources in workload accounts from being accessed by principals
outside the organization, even when a workload administrator creates an overly
permissive bucket policy. AWS service integrations that use service principals
must continue to work.

Which TWO actions meet these requirements?

A. Attach an SCP that grants `s3:GetObject` to every identity in the
organization.

B. Attach an RCP to the workload OU that denies access when
`aws:PrincipalOrgID` is not the company's organization ID, with the required
service-principal exceptions.

C. Add a permissions boundary to the S3 bucket.

D. Rely only on Block Public Access, which prevents every form of
cross-organization access.

E. Test the RCP against supported resources and exempt required AWS service
principals or service-to-service request contexts.

### 15

A DynamoDB table has ample write capacity, but writes are throttled during a
new access pattern. CloudWatch indicates `IndexWriteKeyRangeThroughputExceeded`
for a global secondary index (GSI). The base table partition key is well
distributed, but most new items have the same GSI partition-key value.

Which change MOST directly addresses the root cause?

A. Increase only the base table's read capacity.

B. Request strongly consistent reads from the GSI.

C. Redesign the GSI partition key by adding a write-shard attribute and query
the relevant shards when reading.

D. Replace the GSI with an LSI after the table has been created.

### 16

A company must rehost 900 supported Windows and Linux servers from several data
centres. It needs continuous block-level replication, non-disruptive test
launches, automated conversion to EC2, and a short final cutover. The
applications will not be refactored before migration.

Which service should the company use for the server migration?

A. AWS Application Migration Service

B. AWS DataSync

C. AWS Database Migration Service

D. AWS App Runner

### 17 - Choose THREE

A utility receives readings from millions of devices. Records for each device
must remain ordered, two independent applications must process every reading,
and consumers must be able to replay the last 48 hours. Processing latency
should normally be below one second.

Which THREE design choices meet these requirements?

A. Configure a single SQS standard queue that both applications poll.

B. Ingest readings into Kinesis Data Streams.

C. Deliver readings directly to S3 once each day.

D. Use the device ID as the Kinesis partition key.

E. Use EventBridge Scheduler to recreate missed device events.

F. Give each application its own Kinesis consumer and configure an appropriate
stream retention period.

### 18

A company sends an organization CloudTrail trail to an S3 bucket in a dedicated
log archive account. Workload administrators must not be able to alter retained
events, and auditors require write-once retention plus evidence that delivered
log files were not changed.

Which design best meets the requirements?

A. Enable S3 Versioning only and allow workload-account administrators to
delete object versions.

B. Deliver separate trails to buckets owned by each workload account.

C. Copy the logs to instance store volumes in the security account.

D. Enable CloudTrail log file validation and retain the logs in an S3 Object
Lock bucket with an appropriate retention mode and tightly scoped access.

### 19

An ECS service uses rolling deployments behind an Application Load Balancer.
A bad release can pass the initial container health check but increases HTTP
5xx errors under production traffic. The company wants controlled traffic
shifting and automatic rollback based on application metrics.

Which deployment approach is MOST appropriate?

A. Use Systems Manager Patch Manager to patch the running tasks.

B. Increase the ECS task stop timeout and continue replacing every task at
once.

C. Use CodeDeploy blue/green deployments for ECS with a test listener, traffic
shifting, and CloudWatch alarms.

D. Create an AMI and deploy it with EC2 user data.

### 20 - Choose TWO

A company is moving an on-premises PostgreSQL database to Aurora PostgreSQL.
The schema is compatible and no engine conversion is required. The company
needs a full initial load and ongoing replication of changes until a low-
downtime cutover.

Which TWO actions should the company take?

A. Use AWS Schema Conversion Tool to convert PostgreSQL stored procedures to
Oracle PL/SQL.

B. Use AWS DataSync to read the database transaction log.

C. Create an AWS DMS replication configuration or replication instance with
source and target endpoints.

D. Restore an EBS snapshot directly into Aurora.

E. Configure an AWS DMS task for full load and change data capture.

### 21

A fraud-detection system frequently traverses relationships among accounts,
devices, IP addresses, and payment cards. Queries must efficiently follow
several relationship hops, and the company wants a managed database rather than
building graph traversal logic on top of a relational engine.

Which service is the best fit?

A. Amazon Neptune

B. Amazon Timestream

C. Amazon Keyspaces

D. Amazon ElastiCache for Memcached

### 22

A platform team allows developers to create IAM roles in one workload account.
Developers can choose the roles' permissions policies, but every developer-
created role must remain within a maximum set of approved actions. Existing
operations roles in the same account must not be affected.

Which control should the platform team require?

A. An SCP attached to the entire account that grants the approved actions

B. A permissions boundary attached to each developer-created role

C. An RCP attached to the account's OU

D. A resource policy on the IAM console

### 23 - Choose TWO

A company stores petabytes of S3 data owned by many teams. Finance cannot
identify incomplete multipart uploads or buckets with inefficient storage-class
patterns. The company wants organization-wide visibility first, followed by
automated cost controls that do not change application code.

Which TWO actions are appropriate?

A. Use S3 Storage Lens with organization-level dashboards and metrics.

B. Move all objects immediately to S3 Glacier Deep Archive regardless of access
requirements.

C. Create S3 Lifecycle rules for incomplete multipart uploads and validated
storage-class transitions.

D. Replace S3 with EBS st1 volumes.

E. Enable Transfer Acceleration on every bucket.

### 24

An on-premises backup application writes to a virtual tape library by using
standard tape protocols. The company wants to retain the existing backup
software, keep recently written virtual tapes locally available, and archive
ejected tapes durably in AWS.

Which solution should the company deploy?

A. Amazon EFS One Zone

B. Amazon FSx for OpenZFS

C. AWS Transfer Family

D. AWS Storage Gateway Tape Gateway

### 25

A web application needs a distributed cache for simple key-value objects. The
cached data can be rebuilt from the database, the application does not require
persistence, replication, pub/sub, sorted sets, or failover, and it benefits
from multithreaded scale-out across nodes.

Which managed cache is the MOST suitable?

A. DynamoDB Accelerator (DAX)

B. ElastiCache for Valkey with Multi-AZ

C. ElastiCache for Memcached

D. Amazon MemoryDB

### 26 - Choose TWO

An application role in Account A must decrypt objects in an S3 bucket in
Account B by using a customer managed KMS key that is also in Account B. The
application's request is currently denied. The company wants least-privilege
cross-account access.

Which TWO policy changes are required?

A. Add `kms:Decrypt` to an SCP attached to Account A so the SCP grants access.

B. Allow the required S3 and KMS actions in an identity policy attached to the
application role in Account A.

C. Share the KMS key through AWS Resource Access Manager.

D. Permit the application role in both the S3 bucket policy and the KMS key
policy in Account B.

E. Create an IAM user with an access key in Account B and embed the credentials
in the application.

### 27

A company has 60 VPCs that access the same set of third-party services through
interface VPC endpoints. Deploying separate endpoints in every VPC is expensive.
The company already has a transit gateway and a shared-services VPC. Workloads
must continue to resolve the normal service hostnames to the centralized
endpoint addresses.

Which design is MOST appropriate?

A. Create the interface endpoints in the shared-services VPC, disable or
deliberately manage endpoint private DNS, create and share private hosted-zone
records, and route workload traffic through the transit gateway.

B. Replace every interface endpoint with an S3 gateway endpoint.

C. Put one NAT gateway in the shared-services VPC and publish its Elastic IP as
the service's DNS record.

D. Peer every workload VPC directly with every other workload VPC.

### 28

A migration programme uses several AWS and partner tools. The programme office
wants a single view of applications, migration waves, and status. Discovery
data was collected in one AWS Region, but the office is viewing Migration Hub
in a different Region and cannot see it.

What should the team do?

A. Copy the discovery database with AWS DataSync to every Region.

B. View and manage the migration programme in the configured Migration Hub home
Region and connect the migration tools there.

C. Reinstall every discovery agent in the Region that hosts the target VPC.

D. Use AWS Config aggregators instead of Migration Hub.

### 29 - Choose TWO

An inventory application stores one item per product in DynamoDB. Two order
processors can update the same item concurrently. The company must prevent a
stale processor from overwriting a newer quantity, but it wants to avoid
pessimistic locks.

Which TWO actions implement the required control?

A. Store and increment a version attribute on each successful update.

B. Use a Scan operation before every update and assume the result remains
current.

C. Enable DynamoDB Streams, which serializes all writers before they update the
table.

D. Use an eventually consistent read and an unconditional `PutItem` operation.

E. Include a condition expression that checks the version observed by the
writer before applying the update.

### 30

A global enterprise uses consolidated billing for 200 accounts. Finance needs
hourly cost and usage data with resource-level detail where available, and must
allocate charges by application and cost centre in its existing analytics
platform.

Which solution best meets the requirements?

A. Create separate AWS Organizations for each application.

B. Create one AWS Budget for the management account and export screenshots each
month.

C. Use CloudWatch metrics as the authoritative source of line-item billing.

D. Activate the required cost allocation tags and create an AWS Cost and Usage
Report delivered to S3 for downstream analytics.

### 31

An Aurora MySQL application runs periodic analytical queries that consume most
of the writer's CPU and cause transactional latency. The queries can use data
that is several seconds old. The application already distinguishes read-only
operations from writes.

Which improvement has the LOWEST operational overhead?

A. Export a snapshot before every query and restore it to a new cluster.

B. Direct analytical traffic to the Multi-AZ standby through its private IP.

C. Add Aurora Replicas, use the reader endpoint for analytical traffic, and
configure replica Auto Scaling.

D. Increase the writer instance size indefinitely and keep all traffic on it.

### 32 - Choose TWO

A company is evaluating applications for migration. One commodity web
application can move unchanged to EC2 to meet a near-term data-centre exit. A
second application is obsolete, has no legal retention requirement, and its
business owner confirms that no replacement is needed.

Which TWO migration strategies match these decisions?

A. Refactor the commodity web application before any migration.

B. Retain the obsolete application indefinitely on premises.

C. Rehost the commodity web application.

D. Retire the obsolete application.

E. Repurchase both applications as SaaS products.

### 33

A genomics team runs temporary, highly parallel workloads on thousands of EC2
instances. The workloads need a shared POSIX file system with very high
throughput. Input datasets already reside in S3, and results must be exported
back to S3 after each run.

Which storage service is the best fit?

A. Amazon FSx for Lustre linked to the S3 data repository

B. Amazon EFS One Zone with General Purpose performance mode

C. Amazon S3 File Gateway installed on every EC2 instance

D. Amazon RDS for PostgreSQL

### 34

A company uses IAM Identity Center permission sets across 100 accounts. The
security team adds a managed policy to a permission set and changes the session
duration. Some assigned accounts still contain the older generated role
configuration.

What should the security team do?

A. Create IAM users in the affected accounts to bypass provisioning.

B. Reprovision the permission set to the affected accounts and verify the
generated `AWSReservedSSO_...` roles.

C. Edit each generated role manually and stop using permission sets.

D. Attach an SCP that grants the missing permissions.

### 35 - Choose THREE

An SQS-triggered Lambda function processes batches of ten records. A single
malformed record currently causes all ten records to return to the queue.
Processing is idempotent, but repeated good-record processing wastes capacity.
Messages that repeatedly fail must be isolated for investigation.

Which THREE actions improve the design?

A. Set the queue visibility timeout shorter than the function's normal
processing time.

B. Enable partial batch responses and return identifiers only for failed
records.

C. Configure an SQS dead-letter queue with an appropriate maximum receive count.

D. Delete the entire batch before processing begins.

E. Keep the idempotency control because delivery and retries can still produce
duplicates.

F. Disable all retries and discard the first failed message.

### 36

A company is assessing physical servers in several branch data centres. It
requires installed software inventory, running-process data, performance
metrics, and detailed inbound and outbound TCP connection information. The
servers are not managed by VMware vCenter.

Which discovery method provides the required evidence?

A. Use Migration Hub without a discovery source.

B. Deploy only the Agentless Collector VMware vCenter module.

C. Use AWS DataSync Enhanced mode.

D. Install Application Discovery Service Agent on the physical servers.

### 37

A claims workflow can run for several days and pauses for human approval. It
must preserve execution state, expose auditable execution history, and apply
different retry and catch logic to individual steps.

Which orchestration service and workflow type should the company use?

A. EventBridge Scheduler with one recurring schedule per claim

B. Step Functions Express Workflows invoked synchronously for the full period

C. Step Functions Standard Workflows

D. An SQS delay queue with a seven-day visibility timeout

### 38 - Choose TWO

A company centralizes hybrid DNS in a network-services account. Workloads in
many VPCs must resolve `corp.internal` through on-premises DNS servers. The
company wants to avoid deploying outbound Resolver endpoints in every VPC and
wants new VPCs to use the same forwarding rule.

Which TWO actions meet the requirements?

A. Create Route 53 Resolver outbound endpoints and a forwarding rule in the
network-services VPC.

B. Create a public hosted zone named `corp.internal`.

C. Route DNS packets through an internet gateway to the on-premises servers.

D. Share the Resolver rule through AWS Resource Access Manager and associate it
with the workload VPCs.

E. Create one inbound endpoint in each workload VPC for outbound queries.

### 39

A company keeps application logs in CloudWatch Logs indefinitely. Logs are
queried frequently for 30 days, occasionally until day 90, and then need only
low-cost archival retention for seven years.

Which change most directly reduces long-term cost while retaining the data?

A. Set an appropriate CloudWatch Logs retention period and use a supported
export or subscription delivery path to archive the required logs in S3 with
lifecycle transitions.

B. Change every log group to never expire and increase the log stream count.

C. Copy logs to EC2 instance store after 90 days.

D. Disable log ingestion after 30 days.

### 40

A company must modernize a large order-management monolith without a high-risk
big-bang replacement. The team will implement one bounded business capability
as an independently deployed service, route only matching requests to it, and
leave all other requests on the monolith. More capabilities will move over
incrementally.

Which modernization pattern best describes this approach?

A. Rebuild every capability before directing any production traffic to the new
system.

B. Apply the strangler fig pattern and incrementally replace capabilities at a
routing boundary.

C. Retain the monolith permanently without introducing a service boundary.

D. Rehost the same monolith on one larger EC2 instance and call the work
complete.

### 41 - Choose THREE

A global order API runs independently in two Regions. Each Region must accept
traffic during the loss of the other Region. The application is stateless, data
uses a key-value model, and the business requires an RPO of zero for individual
item writes. The application does not use DynamoDB transaction APIs, TTL, or
LSIs. The selected Regions form a supported Region set.

Which THREE design choices meet the requirements?

A. Deploy API Gateway and Lambda in both Regions.

B. Use a single-Region DynamoDB table and restore it from a nightly backup after
failure.

C. Use a DynamoDB global table configured for multi-Region strong consistency
(MRSC).

D. Use DynamoDB transaction APIs for all cross-Region writes.

E. Put all Lambda functions in one Availability Zone.

F. Route clients to healthy regional API endpoints by using an appropriate
global traffic layer.

### 42

A Control Tower environment reports that a governed account's baseline
resources have drifted. The platform team wants to restore the account to the
landing-zone configuration while preserving Control Tower governance.

What should the team do?

A. Edit Control Tower-managed resources ad hoc without repairing the landing
zone.

B. Remove the account from AWS Organizations and recreate it manually.

C. Disable every control so that drift is no longer reported.

D. Use the Control Tower repair or re-register workflow appropriate to the
reported drift and investigate the change source.

### 43

A security team must inspect traffic between VPCs and the internet with AWS
Network Firewall. The firewall is deployed across multiple Availability Zones
behind a transit gateway. Some return traffic bypasses the same stateful
firewall path, causing intermittent connection failures.

Which change is MOST important?

A. Propagate every VPC route into every transit gateway route table.

B. Enable source/destination checks on every firewall endpoint.

C. Enable transit gateway appliance mode on the inspection VPC attachment and
design symmetric forward and return routes.

D. Replace the stateful firewall with a Route 53 Resolver forwarding rule.

### 44 - Choose TWO

A company is configuring DataSync to transfer files from an on-premises SMB
share to Amazon FSx for Windows File Server. It needs scheduled incremental
copies and verification. The on-premises share is not directly accessible from
the DataSync service.

Which TWO components are required?

A. An AWS DMS source endpoint for the SMB server

B. A DataSync agent with network access to the on-premises share

C. A Kinesis shard for each directory

D. A Migration Hub home Region in every target Region

E. Source and destination locations referenced by a DataSync task

### 45

A new SaaS application stores tenant data in Aurora PostgreSQL. Read traffic is
ten times write traffic and varies by tenant. The application can route read-
only queries separately and requires automatic read scaling without promoting
every replica to a separate database.

Which design should the company use?

A. Aurora Replicas behind the cluster reader endpoint with replica Auto Scaling

B. A Multi-AZ standby endpoint for application reads

C. One RDS snapshot per tenant

D. DynamoDB Streams connected to the Aurora writer

### 46

Security analysts need to run SQL-like queries across management and data
events from all accounts in an organization without building a separate Athena
ETL pipeline. A delegated security account should administer the queryable
event store.

Which service is MOST appropriate?

A. VPC Flow Logs delivered only to local log groups

B. AWS CloudTrail Lake with an organization event data store

C. Amazon Inspector delegated administrator

D. AWS Artifact

### 47 - Choose TWO

A gaming application reads player-profile items from DynamoDB millions of times
per second. Most reads are eventually consistent, the same items are requested
repeatedly, and the company needs microsecond read latency with minimal changes
to DynamoDB data access. The cache must remain available after one cache node
fails.

Which TWO actions meet the requirements?

A. Request strongly consistent reads through DAX for every request.

B. Replace the DynamoDB table with ElastiCache for Memcached as the system of
record.

C. Deploy a multi-node, Multi-AZ DAX cluster.

D. Change the application to use the DAX client for supported DynamoDB read
operations.

E. Add a local secondary index with the same sort key.

### 48

A company must migrate an on-premises Oracle database to Amazon RDS for Oracle
with minimal downtime. The database engine and schema will remain unchanged.
The company needs an initial copy followed by continuous change replication
until cutover.

Which solution is MOST appropriate?

A. S3 Cross-Region Replication

B. AWS DataSync with object verification

C. AWS Application Migration Service test launches

D. AWS DMS full load and change data capture

### 49

An HTTP API only validates a small fixed request shape and then starts a Step
Functions Standard Workflow. API Gateway mapping templates can construct the
required `StartExecution` request, and the company wants the fewest runtime
components and least code to operate.

Which integration should the company choose?

A. CloudFront Functions to Step Functions

B. API Gateway to an EC2 proxy that invokes Step Functions

C. API Gateway direct AWS service integration with Step Functions

D. An SQS queue polled by an administrator

### 50 - Choose TWO

A security architect is explaining organization policies to application teams.
Teams need to understand the difference between SCPs and RCPs.

Which TWO statements are correct?

A. SCPs limit the maximum permissions available to identities in affected
member accounts; they do not grant permissions.

B. SCPs are identity policies that must be attached to every IAM user.

C. RCPs are permissions boundaries attached to IAM roles.

D. An explicit Allow in an SCP overrides an explicit Deny in an identity
policy.

E. RCPs limit permissions that can apply to supported resources in affected
member accounts; they do not grant permissions.

### 51

A company runs a stable baseline of EC2, Fargate, and Lambda compute usage but
changes instance families and Regions frequently. It can commit to a consistent
hourly compute spend for three years and wants the discount to apply flexibly
across these compute services.

Which purchasing option is MOST appropriate?

A. Compute Savings Plans

B. Standard Reserved Instances for one fixed EC2 instance family

C. EC2 Spot Instances for the entire baseline

D. S3 Reserved Capacity

### 52

A company is moving a Java web application from self-managed virtual machines
to AWS. The application can be deployed from a standard package, needs a load
balancer and Auto Scaling, and requires access to the underlying EC2 operating
system for a small amount of configuration. The team wants AWS to manage the
platform provisioning while making few code changes.

Which migration target represents the best replatforming choice?

A. Amazon S3 static website hosting

B. AWS Elastic Beanstalk

C. Amazon DynamoDB

D. AWS Snowball Edge

### 53 - Choose TWO

A company serves private static content from an S3 bucket through CloudFront.
Users must not be able to bypass CloudFront and retrieve objects directly from
S3. The company also wants to block common web exploits at the edge.

Which TWO actions meet the requirements?

A. Make the S3 bucket public and depend on an obscure bucket name.

B. Configure CloudFront origin access control and restrict the S3 bucket policy
to the distribution.

C. Attach a security group directly to the S3 bucket.

D. Attach an AWS WAF web ACL to the CloudFront distribution.

E. Put a NAT gateway between CloudFront and S3.

### 54

A company attaches an SCP to the root of its organization and expects it to
restrict administrators who sign in to the Organizations management account.
The administrators can still perform actions that the SCP denies.

What explains this behaviour?

A. SCPs affect only IAM users, not roles.

B. The management account must be invited to its own organization.

C. SCPs apply only after an RCP grants permission.

D. SCPs do not restrict principals in the Organizations management account.

### 55

A Kinesis data stream has 20 shards. Producers are throttled even though total
stream throughput is below aggregate capacity. CloudWatch shows that one shard
receives most records because producers use a low-cardinality `country` value
as the partition key. Ordering is required only for each customer.

Which change MOST directly improves write distribution while preserving the
required ordering?

A. Replace every partition key with one constant value.

B. Increase the retention period to seven days.

C. Use the customer ID as the partition key and reshard if aggregate capacity
also needs to increase.

D. Add more consumers to the hot shard without changing producer keys.

### 56 - Choose THREE

A document platform stores uploads in S3. Four processing teams require durable
independent delivery of upload events. Each team must scale and retry without
affecting the others, and new consumers should be added without changing the
upload application.

Which THREE design choices meet the requirements?

A. Make every team poll the same SQS standard queue.

B. Route S3 object events through EventBridge.

C. Create a separate SQS queue for each processing team.

D. Invoke all processors sequentially inside the upload request.

E. Configure EventBridge rules to send matching events to the teams' queues.

F. Store the event only in a Lambda execution environment.

### 57

A company submits containerized scientific jobs that run for 30 minutes to
three hours. Jobs need queueing, priorities, retries, and dependencies. The team
does not need Kubernetes and does not want to manage EC2 instances.

Which solution has the LOWEST operational overhead?

A. AWS Batch with a Fargate compute environment

B. A fixed EC2 instance that runs cron jobs

C. Amazon EKS with self-managed node groups

D. API Gateway with a three-hour synchronous integration

### 58

A company wants to aggregate security findings from GuardDuty, Inspector, and
other supported services across all organization accounts and Regions. A
dedicated security account should administer the aggregation instead of the
management account.

Which design is MOST appropriate?

A. Create administrator IAM users with identical passwords in every account.

B. Configure AWS Security Hub organization integration, designate the security
account as delegated administrator, and configure the required aggregation.

C. Send only billing events to an SNS topic in the management account.

D. Move all workloads into the security account.

### 59 - Choose TWO

An application uses CloudFront with an Application Load Balancer origin.
Worldwide users report high latency for dynamic requests, and the origin
receives duplicate fetches from many edge locations for the same cacheable
objects. The company wants to keep CloudFront and reduce origin load.

Which TWO actions are appropriate?

A. Enable Origin Shield in an AWS Region appropriate for the origin.

B. Forward all cookies, headers, and query strings by default.

C. Set every object's TTL to zero.

D. Review cache and origin-request policies so only response-varying values
fragment the cache key.

E. Replace the Application Load Balancer with an internet gateway.

### 60

Executives need a data-driven business case for migrating a large on-premises
estate. They want inventory-based cost modelling, right-sized AWS projections,
and comparisons with current infrastructure cost before they approve migration
waves.

Which AWS capability is designed to support this assessment?

A. Amazon Macie

B. AWS WAF

C. Route 53 Resolver DNS Firewall

D. Migration Evaluator

### 61

An energy platform stores years of measurements and runs time-window
aggregations by asset. It also needs a separate, millisecond lookup of each
asset's latest operating state by known asset ID.

Which data design best fits both access patterns?

A. Use an SQS queue as the system of record for both access patterns.

B. Store all history in ElastiCache for Memcached and use S3 Select for latest
state.

C. Store measurement history in Timestream and maintain latest state in
DynamoDB.

D. Use Neptune for measurement history and an EBS snapshot for latest state.

### 62 - Choose TWO

A company uses a transit gateway and a centralized inspection VPC. Production
and development VPCs must not communicate directly. Both may reach shared
services, and all internet-bound traffic must traverse stateful firewalls.

Which TWO design elements are required?

A. Propagate every attachment to every transit gateway route table.

B. Add direct production-to-development routes to each VPC route table.

C. Use separate transit gateway route tables and controlled association and
propagation for the traffic domains.

D. Attach an internet gateway directly to the transit gateway.

E. Enable appliance mode on the inspection attachment and configure symmetric
routes through the firewall endpoints.

### 63

A security engineer wants to reduce an IAM role's permissions based on the
actions it actually used during the last 90 days. The engineer wants AWS to
analyse CloudTrail activity and generate a policy as a starting point for
review.

Which capability should the engineer use?

A. IAM Access Analyzer policy generation

B. AWS Shield Advanced

C. Route 53 traffic flow

D. Amazon Inspector network reachability only

### 64

A company is designing an inventory table that accepts reads and writes in
multiple EU Regions. Every strongly consistent read must reflect the latest
successful write from any participating Region, and the RPO for successful item
writes must be zero. The application does not use transactions, TTL, or LSIs,
and the proposed Regions form a supported set.

Which database configuration meets the requirement?

A. A DynamoDB global table configured for MREC with strongly consistent local
reads

B. A DynamoDB global table configured for MRSC with the required replica or
witness topology

C. A single DynamoDB table with daily on-demand backups

D. A DAX cluster in one Region in front of independent regional tables

### 65 - Choose TWO

A company has a Step Functions workflow that charges a payment and then updates
an order. The payment API can time out after completing the charge, and Step
Functions might retry the task. The company must prevent double charging and
must provide a recovery path if the order update ultimately fails.

Which TWO design choices are most appropriate?

A. Generate a new payment identifier for every retry.

B. Pass a stable business idempotency key to the payment operation and have the
payment service enforce it.

C. Disable workflow history so operators cannot see partial progress.

D. Define a compensating or reconciliation path for the payment when the later
order update cannot complete.

E. Assume a timeout proves that the payment did not occur.

### 66

A company uses a dedicated 10-Gbps Direct Connect connection. Security policy
requires link-layer encryption over the physical Direct Connect circuit, and
the location and devices support the feature. The company wants to avoid
running an IPsec overlay for this requirement.

Which feature should the company configure?

A. VPC peering encryption settings

B. Route 53 DNSSEC signing

C. S3 server-side encryption

D. MAC Security (MACsec) on the Direct Connect connection

### 67

A Lambda function is invoked asynchronously during unpredictable bursts. A
downstream database can safely support only 200 concurrent requests, while
other functions in the account must retain capacity. The team wants to prevent
this function from overwhelming the database.

Which control is MOST direct?

A. Put the function in a public subnet.

B. Increase the account concurrency limit without constraining the function.

C. Configure reserved concurrency of 200 for the function and tune the event
failure destination or dead-letter handling.

D. Set provisioned concurrency to 2,000 and remove all retries.

### 68 - Choose THREE

A migration team must create waves for hundreds of interconnected servers and
then track execution. It needs detailed physical-server dependencies, logical
application groupings, and consolidated status from the tools that perform the
migrations.

Which THREE actions support the requirement?

A. Install Application Discovery Service Agent where host-level process and TCP
evidence is required.

B. Use DataSync as the authoritative source of running-process dependencies.

C. Group discovered servers into applications and waves based on validated
dependency evidence.

D. Assume that one successful ping proves two servers belong in the same wave.

E. Replace discovery with AWS Budgets.

F. Use Migration Hub in its home Region to aggregate application and migration
status.

### 69

A company stores business-critical objects in S3 and replicates them to a
second Region. Compliance requires a predictable replication time for most new
objects and monitoring when that threshold is missed. The target is 15 minutes.

Which feature should the company enable?

A. S3 Replication Time Control with the required replication metrics and
notifications

B. S3 Transfer Acceleration on the source bucket

C. S3 Select on every replicated object

D. An EFS lifecycle policy

### 70 - Choose TWO

A company wants a dedicated security account to administer supported security
services across its organization. The management account should be used only
for organization-level setup, not daily security operations.

Which TWO actions are appropriate?

A. Move the management account into the security OU so SCPs can restrict it.

B. Create shared root credentials for the security team.

C. Enable trusted access between AWS Organizations and each supported service
when required.

D. Assume one delegated-administrator registration automatically applies to
every AWS service.

E. Register the security account as delegated administrator for each supported
service.

### 71

An EFS file system supports thousands of web servers. The workload consists of
small files and latency-sensitive operations. Aggregate throughput is adequate,
but metadata latency increased after the team selected Max I/O performance mode
without a scale requirement that justified it.

Which change should the team evaluate first?

A. Move the files to EBS sc1 and mount one volume from all servers.

B. Create a new EFS file system in General Purpose performance mode, validate
its workload limits, and migrate the data.

C. Enable EFS One Zone solely to change performance mode.

D. Replace NFS with an SQS queue.

### 72 - Choose THREE

A company is designing a serverless order workflow. The workflow must call
inventory and payment services, wait for a callback from a fulfilment partner,
retain an auditable execution history, and recover from partial failures without
creating duplicate payments.

Which THREE design choices meet the requirements?

A. Use a Step Functions Standard Workflow with task-token callback integration
for the partner response.

B. Use a Step Functions Express Workflow that waits several days for the
callback.

C. Generate a new payment key whenever a task is retried.

D. Use explicit Retry and Catch paths that distinguish transient failures from
business rejection.

E. Store the only workflow state in Lambda temporary storage.

F. Propagate and enforce a stable idempotency key at payment and other
side-effect boundaries.

### 73

On-premises applications connected through Direct Connect must access S3 over
private IP addresses. The company cannot route this traffic through a NAT
gateway or the public S3 endpoint. The endpoint must be reachable from the
on-premises network.

Which solution is MOST appropriate?

A. A Route 53 public hosted zone that points to an S3 gateway endpoint

B. An S3 gateway endpoint only, because gateway endpoints are directly
reachable from on-premises networks

C. An internet gateway in the on-premises data centre

D. An S3 interface VPC endpoint reachable through the private network path,
with the required DNS and endpoint policy configuration

### 74 - Choose TWO

A company wants early warning of unexpected spend and detailed investigation of
which services and accounts caused a cost increase. Finance also wants alerts
when forecast spend is likely to exceed a monthly limit.

Which TWO capabilities should the company use?

A. Route 53 health checks

B. AWS Budgets with actual and forecast threshold notifications

C. AWS Cost Explorer for account, service, and usage analysis

D. Security groups with cost tags

E. AWS Shield Standard billing alarms

### 75 - Choose THREE

A legacy EC2 application stores database credentials in a configuration file,
uses manually patched instances, and has no automated response when its
Application Load Balancer target group becomes unhealthy. The company wants to
improve security and operations without a broad application rewrite. A bounded
credential-provider integration change is acceptable.

Which THREE actions are appropriate?

A. Bake permanent access keys into a new AMI.

B. Store credentials in Secrets Manager and configure rotation where the
database and application integration support it.

C. Disable load balancer health checks to prevent replacement.

D. Use Systems Manager Patch Manager with maintenance windows and compliance
reporting for the instances.

E. Create CloudWatch alarms and an Auto Scaling health-replacement or recovery
process for unhealthy capacity.

F. Grant the instance role administrator access so it can repair any resource.

---

## Frozen submission template

```text
Start:12:28
End: 14:47
Uncertain:1,10,16,28,33,44,62,65,66,67

1:C
2:ADF
3:D
4:A
5:BD
6:C
7:C
8:CE
9:B
10:A
11:AD
12:D
13:B
14:BE
15:C
16:A
17:BDF
18:D
19:C
20:CE
21:A
22:B
23:AC
24:D
25:C
26:BD
27:A
28:A
29:AE
30:D
31:C
32:CD
33:A
34:B
35:BCE
36:D
37:C
38:AD
39:A
40:B
41:ACF
42:D
43:C
44:BE
45:A
46:B
47:C
48:D
49:C
50:AE
51:A
52:B
53:BD
54:D
55:C
56:BCE
57:A
58:B
59:AD
60:D
61:C
62:CE
63:A
64:B
65:BD
66:D
67:C
68:ACF
69:A
70:CE
71:B
72:ADF
73:B
74:BC
75:BDE

Submission status: FROZEN
```
