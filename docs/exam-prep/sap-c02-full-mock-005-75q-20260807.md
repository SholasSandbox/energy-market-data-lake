# AWS Certified Solutions Architect - Professional (SAP-C02)

## Full Mock 005 - 75 Questions

**Created:** 2026-08-07
**Mode:** Fresh closed-book full simulation
**Time limit:** 180 minutes
**Question types:** 48 single-response and 27 multiple-response questions
**Submission status:** FROZEN

Multiple-response questions receive credit only when every required option and
no additional option is selected.

This is an original practice examination aligned to the current SAP-C02 domain
weightings. It is not an AWS examination and does not reproduce live exam
questions. The answer key is deliberately excluded from this document.

## Attempt Rules

1. Keep lessons, reviews, answer keys, documentation, search, and AI assistance
   closed until the submission is frozen.
2. Select one answer unless the question says **Choose TWO** or **Choose
   THREE**.
3. Record start and end times and any uncertain question numbers.
4. Complete the answer template at the end of this document.
5. Change `Submission status` to `FROZEN` before requesting marking. Do not
   revise a frozen response.

---

## Questions

### 1

An organization owns sensitive S3 buckets in several member accounts. A bucket
administrator must not be able to grant access to principals outside the
organization, even by changing the bucket policy. The central control must
limit access to supported resources without granting access itself.

Which control best meets the requirement?

A. An IAM permissions boundary attached to the bucket administrator

B. A resource control policy attached to the appropriate organization level

C. An identity-based policy in the management account

D. An S3 lifecycle policy applied to every bucket

### 2 - Choose TWO

A trading platform publishes order-state events. Several independent systems
must receive matching events, and the audit team must be able to replay events
from a retained archive after a consumer defect is fixed.

Which TWO actions best meet these requirements?

A. Put every event into one SQS standard queue and let all consumers poll it.

B. Publish the events to Amazon EventBridge and create rules for the independent
targets.

C. Use an Application Load Balancer to retain the events.

D. Enable an EventBridge archive and replay selected events when required.

E. Use an SNS topic without any durable downstream target or archive.

### 3

A bank wants to modernize a large COBOL mainframe estate. It needs codebase
analysis, generated documentation, business-logic extraction, decomposition
assistance, and human review throughout the transformation.

Which AWS service is designed for this work?

A. AWS Application Migration Service

B. AWS DataSync

C. AWS Transform for mainframe

D. AWS Transfer Family

### 4 - Choose THREE

An enterprise requires centralized evidence of API activity and resource
configuration across all current and future AWS accounts. Security operations
runs from delegated administrator accounts, while immutable logs are delivered
to a protected archive account.

Which THREE actions form the strongest design?

A. Create an organization CloudTrail trail and deliver it to the log archive
account.

B. Depend only on the 90-day Event history available in each account.

C. Enable AWS Config recorders in source accounts and aggregate configuration
and compliance data centrally.

D. Give workload-account administrators permission to delete the organization
trail.

E. Use delegated administration where supported and restrict archive-bucket
and KMS-key access.

F. Replace CloudTrail and AWS Config with VPC Flow Logs.

### 5

A CloudFront distribution uses an origin group for a global API. `GET` requests
fail over correctly, but state-changing `POST` requests require continuity
during a Regional outage. An engineer proposes adding Lambda@Edge so the same
built-in origin failover will apply to every HTTP method.

What should the solutions architect recommend?

A. Accept the proposal because Lambda@Edge expands origin failover to all HTTP
methods.

B. Enable Origin Shield because it adds `POST` failover to origin groups.

C. Keep CloudFront origin failover for eligible read methods and design write
continuity separately with routing, idempotency, retry, and data-consistency
controls.

D. Cache all `POST` responses so CloudFront treats the requests as `GET`
requests.

### 6

A company has a stable EC2 baseline but instances are substantially oversized.
It wants a three-year commitment discount while retaining flexibility across
instance families and Regions.

Which action should be taken first?

A. Buy Standard Reserved Instances for the current oversized fleet.

B. Rightsize using AWS Compute Optimizer, then evaluate a Compute Savings Plan
for the stable baseline.

C. Move every instance to Spot before measuring interruption tolerance.

D. Increase the Auto Scaling minimum capacity to the observed peak.

### 7 - Choose TWO

Applications in a VPC must resolve private names hosted by on-premises DNS.
On-premises clients must also resolve records in Route 53 private hosted zones.
Private network connectivity already exists.

Which TWO components are required?

A. A Route 53 Resolver inbound endpoint for queries originating in the VPC

B. A Route 53 Resolver outbound endpoint with forwarding rules for on-premises
domains

C. A public hosted zone containing copies of all private records

D. A Route 53 Resolver inbound endpoint for DNS queries arriving from
on-premises

E. An internet gateway attached to the DNS VPC

### 8

A company is rehosting 300 servers with AWS Application Migration Service.
Before cutover, application owners must validate launched instances in AWS
while block-level replication from the source servers continues.

What should the migration team do?

A. Finalize cutover and immediately archive every source server.

B. Use AWS DataSync to boot the replicated disks.

C. Stop replication and export each server with VM Import/Export.

D. Launch test instances from the replicated data and complete validation
before scheduling cutover.

### 9 - Choose TWO

An enterprise is replacing workforce IAM users with IAM Identity Center.
Employees need temporary access to several AWS accounts based on group
membership.

Which TWO statements describe the resulting pattern?

A. Permission sets replace IAM policy evaluation inside the target account.

B. A permission set creates a permanent IAM user in each assigned account.

C. An account assignment combines a user or group, an AWS account, and a
permission set.

D. Employees require long-lived access keys for every account assignment.

E. IAM Identity Center provisions and manages an IAM role for the permission
set in each assigned account.

### 10

An API starts a workflow that can run for several hours. A client may retry
after losing the HTTP response, but a repeated request for the same business
operation must not start duplicate processing.

Which design best meets the requirement?

A. Run the workflow synchronously inside one Lambda invocation.

B. Use an Express workflow and treat the TCP connection as the idempotency
record.

C. Generate a random execution name on every retry.

D. Claim a business idempotency key with a conditional DynamoDB write, then
start a Standard Step Functions execution and return an operation reference.

### 11

A DynamoDB base table has unused write capacity, but writes are throttled. A
global secondary index has a low-cardinality partition key and reports write
throttling.

What is the most likely explanation?

A. Strongly consistent GSI reads are consuming the base table's WCUs.

B. The GSI hot key and write throttling are applying backpressure to base-table
writes.

C. DynamoDB Streams consumers synchronously approve every table write.

D. The table has exceeded the LSI 10 GB item-collection limit even though it
has no LSI.

### 12 - Choose THREE

A migration programme includes physical servers and VMware virtual machines.
Physical servers require detailed process and TCP-connection evidence. VMware
inventory must be collected without installing software in guest operating
systems. The programme also needs central application grouping and progress
tracking.

Which THREE components meet these requirements?

A. AWS Application Discovery Agent on the physical servers

B. AWS DataSync agents on every server to collect process dependencies

C. AWS Snowball Edge as the migration metadata database

D. AWS Transfer Family for vCenter discovery

E. AWS Application Discovery Service Agentless Collector in the VMware
environment

F. AWS Migration Hub for grouping and migration tracking

### 13

A role in Account B must read S3 objects in Account A that are encrypted with a
customer-managed KMS key in Account A. The bucket policy already permits the
role to call `s3:GetObject`.

What additional authorization is required?

A. Only an SCP in Account B allowing `kms:Decrypt`

B. Permission in the KMS key policy in Account A and identity permission for
the role in Account B

C. An S3 ACL attached to the KMS key

D. A permissions boundary in Account A granting access to the external role

### 14

An Aurora Global Database is healthy in both Regions. The operations team wants
to move the writer to the secondary Region for a planned exercise with no data
loss.

Which operation should it use?

A. Restore the latest snapshot in the secondary Region.

B. Delete the primary cluster and promote a read replica.

C. Perform a managed Aurora Global Database switchover.

D. Detach the secondary cluster before stopping writes.

### 15 - Choose TWO

A data lake stores objects with unpredictable access. Some objects are read
frequently for several weeks and then might remain untouched for months. The
company wants automatic storage-cost optimization without retrieval fees or
operationally maintained age rules for the unpredictable objects.

Which TWO actions are appropriate?

A. Apply S3 Standard to every object permanently.

B. Use S3 Intelligent-Tiering for the unpredictable-access dataset.

C. Use EBS snapshots as the primary data-lake storage class.

D. Enable the optional Archive Access tiers when their retrieval-time trade-off
is acceptable.

E. Move all new objects directly to S3 Glacier Deep Archive.

### 16

A company must migrate Oracle to Aurora PostgreSQL with minimal downtime.
Tables, views, and stored procedures require conversion before data can be
replicated.

Which tool combination is most appropriate?

A. AWS DataSync for schema conversion and S3 Batch Operations for CDC

B. AWS Schema Conversion Tool or applicable conversion tooling for schema and
code, followed by AWS DMS full load and CDC

C. AWS Application Migration Service for database-engine conversion

D. AWS Transfer Family for transaction-log replication

### 17

An organization uses an SCP to deny API calls outside approved Regions. Route
53 administration unexpectedly fails because the policy evaluates the global
service endpoint's requested Region.

What is the correct remediation?

A. Add the required Route 53 actions to the policy's explicit global-service
exemption while retaining the Region restriction for Regional services.

B. Remove all conditions from the SCP and rely on security groups.

C. Attach a permissions boundary to the Route 53 hosted zone.

D. Move Route 53 resources into the management account, where they become
Regional.

### 18 - Choose TWO

Millions of devices send ordered telemetry. Events for the same device must
remain ordered, and three independent applications must process and replay the
retained stream at different rates.

Which TWO actions best meet the requirement?

A. Use one SQS FIFO message group for every device in the fleet.

B. Put all records in one SQS standard queue.

C. Use Kinesis Data Streams with the device ID as the partition key.

D. Publish to an SNS topic with no durable subscriptions.

E. Configure independent Kinesis consumers, using enhanced fan-out when
dedicated read throughput is required.

### 19

A monitoring account needs centralized visibility into CloudWatch metrics,
logs, and traces from many source accounts without copying all telemetry into
the monitoring account.

Which service provides this cross-account observability linkage?

A. AWS Artifact

B. Amazon Inspector

C. AWS Database Migration Service

D. CloudWatch Observability Access Manager

### 20 - Choose THREE

A company must repeatedly copy changed files from an on-premises NFS server to
Amazon EFS before a short cutover. Transfers must be encrypted and verified.

Which THREE elements belong in the solution?

A. Deploy an AWS DataSync agent with connectivity to the source storage.

B. Configure NFS and EFS locations in a DataSync task.

C. Use VM Import/Export to compare every file.

D. Run the task repeatedly so DataSync transfers changes and performs the
configured verification.

E. Use AWS DMS change data capture against the NFS mount.

F. Require a Snowball device for every incremental run.

### 21

A role has an identity policy allowing `s3:*`. Its permissions boundary permits
only `s3:GetObject` and `s3:ListBucket`. No other policy denies access.

What is the role's effective S3 permission set?

A. Every S3 action because the identity policy grants `s3:*`

B. Every action in the boundary even if the identity policy does not grant it

C. No permissions because a boundary can never contain Allow statements

D. Only the actions allowed by both the identity policy and the boundary

### 22

Application teams operate HTTP services in many VPCs and accounts. They need
service discovery, application-layer routing, monitoring, and IAM-based
authorization without full VPC-to-VPC connectivity.

Which service is the best fit?

A. AWS Transit Gateway

B. Amazon VPC Lattice

C. AWS Direct Connect Gateway

D. Route 53 Resolver DNS Firewall

### 23 - Choose TWO

A Lambda function processes an SQS standard queue in batches. One bad message
causes successful messages to be retried, and permanently failing messages must
eventually be isolated.

Which TWO changes should be made?

A. Return partial batch item failures from the Lambda function.

B. Set the source queue retention period to zero.

C. Disable the event source mapping after the first failure.

D. Configure a dead-letter queue and redrive policy on the source queue.

E. Replace the queue with an unsubscribed SNS topic.

### 24

Migration tools and discovery agents are reporting successfully, but Migration
Hub appears empty when an operator opens the console in a newly selected
Region.

What should the operator check first?

A. Whether S3 Transfer Acceleration is enabled

B. Whether every source server has an Elastic IP address

C. Whether the DataSync task is in the `AVAILABLE` state

D. Whether the console is open in the configured Migration Hub home Region

### 25

An enterprise must inspect north-south and east-west traffic for hundreds of
VPCs. It wants centrally managed stateful firewall rules and scalable routing
through a dedicated inspection VPC.

Which architecture is the best fit?

A. Attach one internet gateway to every workload subnet and use NACLs only.

B. Route traffic through a Transit Gateway and centralized AWS Network Firewall
endpoints in an inspection VPC.

C. Use VPC peering between every workload VPC and inspect traffic with Route 53
health checks.

D. Replace routing with AWS WAF on every private IP address.

### 26 - Choose TWO

A company serves a static website through CloudFront from a private S3 bucket.
Users must not bypass CloudFront, and common web exploits must be filtered at
the edge.

Which TWO configurations meet the requirements?

A. Enable public read access and S3 static website hosting.

B. Attach a security group directly to CloudFront.

C. Configure CloudFront origin access control and a bucket policy scoped to the
intended distribution.

D. Use a gateway endpoint policy as the only internet-viewer control.

E. Associate an AWS WAF web ACL with the CloudFront distribution.

### 27

An I/O-intensive database requires 80,000 provisioned IOPS, high throughput,
sub-millisecond latency, and the highest EBS durability. The workload exceeds
gp3 performance limits.

Which volume type should be used?

A. st1

B. gp2

C. sc1

D. io2 Block Express

### 28

A remote facility must transfer 450 TB of archival data to AWS once. Its
network is too slow and unreliable, but it can receive and return secure
physical appliances.

Which service is most appropriate?

A. AWS Transfer Family

B. S3 Transfer Acceleration over the existing connection

C. AWS Application Migration Service

D. AWS Snowball Edge Storage Optimized

### 29 - Choose TWO

An organization uses AWS Control Tower. Architects need a control that blocks
prohibited API operations and a control that reports resources whose
configuration violates policy.

Which TWO statements are correct?

A. Detective controls block every API call before resource creation.

B. Preventive controls can be implemented using service control policies.

C. A permissions boundary on one role is an organization-wide preventive
control.

D. CloudTrail Event history automatically remediates all violations.

E. Detective controls can evaluate resources through AWS Config rules.

### 30

A settlement table requires strongly consistent reads across Regions and zero
RPO. A separate session table requires DynamoDB TTL and can tolerate eventual
cross-Region consistency.

Which design is correct?

A. Use MRSC for both tables because TTL is supported by every consistency mode.

B. Use MREC for settlement and MRSC for sessions.

C. Use MRSC for settlement and MREC for sessions.

D. Use single-Region tables because global tables cannot serve reads in more
than one Region.

### 31 - Choose TWO

An ECS service uses CodeDeploy blue/green deployment. The team must validate the
replacement task set through test traffic before shifting production, and the
deployment revision must identify the replacement container.

Which TWO configuration elements are required for these specific needs?

A. A test listener on the same load balancer as the production listener

B. One target group that CodeDeploy moves between VPCs

C. An AppSpec file identifying the replacement task definition, container name,
and port

D. Systems Manager Patch Manager as the ECS deployment controller

E. CloudFront origin failover to create the replacement task set

### 32

A company wants to move hundreds of VMware virtual machines to VMware Cloud on
AWS while preserving the VMware operating model and avoiding guest operating
system or application changes.

Which migration strategy best describes this move?

A. Repurchase

B. Relocate

C. Refactor

D. Retire

### 33

An organization wants to restrict what IAM principals in a member account can
do, regardless of broad identity policies. It does not need to restrict
external principals accessing resources owned by that account.

Which organization policy is the correct principal-side guardrail?

A. A resource control policy

B. A permissions boundary attached to the organization root

C. A service control policy

D. An S3 bucket policy

### 34 - Choose TWO

A read-heavy DynamoDB application needs microsecond response times with minimal
query-code change. A DAX cluster has already been deployed, but the application
still sends every request directly to DynamoDB.

Which TWO actions are necessary?

A. Replace every DynamoDB table with Amazon RDS.

B. Configure the application to use a DAX-compatible client and the DAX cluster
endpoint.

C. Enable strongly consistent reads through DAX for every request.

D. Add a global secondary index solely to activate DAX.

E. Ensure the application can reach the DAX cluster and is authorized for the
required DynamoDB and DAX operations.

### 35

Athena queries scan millions of small JSON files and usually filter by trade
date and market. Cost and latency are increasing.

Which change is likely to provide the largest improvement?

A. Increase the Athena query timeout.

B. Enable S3 Transfer Acceleration.

C. Compact the dataset into larger Parquet files and partition it by commonly
filtered columns.

D. Move the files to S3 Glacier Flexible Retrieval before every query.

### 36

Hundreds of physical servers must be grouped into migration waves based on
detailed active inbound and outbound network connections and running process
information.

Which discovery mechanism supplies this OS-level evidence?

A. AWS DataSync task reports

B. Agentless Collector using only VMware vCenter inventory

C. AWS Application Discovery Agent installed on the servers

D. AWS Cost and Usage Reports

### 37 - Choose TWO

An organization wants central resource-compliance visibility and a durable
organization-wide API audit trail.

Which TWO statements are correct?

A. An AWS Config aggregator automatically enables recorders in all source
accounts.

B. An AWS Config aggregator collects configuration and compliance data from
configured source accounts and Regions.

C. CloudTrail replaces AWS Config configuration history.

D. AWS Config records every API request body.

E. An organization CloudTrail trail can deliver member-account management
events to a central S3 destination.

### 38

A service receives volatile TCP and UDP traffic and must preserve client source
IP addresses while handling millions of requests per second with static IP
addresses.

Which load balancer is the best fit?

A. Application Load Balancer

B. Network Load Balancer

C. Gateway Load Balancer

D. Classic Load Balancer used as an API gateway

### 39

Thousands of Lambda executions open database connections to an RDS database,
causing connection exhaustion during bursts. The application needs connection
pooling and faster recovery from database failover.

Which service should be added?

A. DynamoDB Accelerator

B. AWS DataSync

C. Amazon Kinesis Data Firehose

D. RDS Proxy

### 40 - Choose THREE

A company is using AWS Application Migration Service to rehost a group of
servers. It wants a controlled lifecycle from continuous replication through
validation and final production transition.

Which THREE actions belong in the process?

A. Convert every application to containers before MGN can replicate it.

B. Configure replication and launch settings for the source servers.

C. Launch test instances and validate application behaviour before cutover.

D. Use AWS DMS to replicate the operating-system disks.

E. Delete all source servers before the first test launch.

F. Launch cutover instances when the wave is approved, then finalize cutover
after validation.

### 41

A consumer-facing mobile application needs user sign-up, sign-in, social
identity federation, and tokens for application access. It does not need to
assign employees to AWS accounts.

Which service is the best fit?

A. Amazon Cognito user pools

B. IAM Identity Center permission sets

C. AWS Organizations service control policies

D. IAM Access Analyzer

### 42 - Choose TWO

Member-account workloads must publish events to a central EventBridge bus. The
central account will route matching events to several targets.

Which TWO permission configurations are required?

A. A resource policy on the central event bus permitting the intended member
accounts or organization to call `events:PutEvents`

B. VPC peering between every publisher and the central account

C. Long-lived IAM user keys in each member account

D. Sender-role permission to call `events:PutEvents` on the central bus

E. A shared SQS queue configured as the event bus

### 43

A global, non-HTTP application needs two static anycast IP addresses, rapid
endpoint failover, and traffic routing to the optimal healthy Regional
endpoint.

Which service should be used?

A. Amazon CloudFront

B. AWS Global Accelerator

C. Route 53 private hosted zones

D. AWS Direct Connect Gateway

### 44

External business partners must upload and download files through managed SFTP
endpoints backed by Amazon S3. The company does not want to operate transfer
servers.

Which service should it use?

A. AWS DataSync

B. AWS Application Migration Service

C. AWS Transfer Family

D. AWS Database Migration Service

### 45 - Choose TWO

A role in Account B needs to decrypt with a customer-managed KMS key owned by
Account A. No AWS Organizations policy denies the request.

Which TWO permissions are required?

A. An SCP Allow in Account B is sufficient by itself.

B. The key policy in Account A must trust the role or Account B through an
appropriate delegation path.

C. A security group must allow TCP 443 to the key.

D. The role in Account B needs identity permission for the required KMS action.

E. An IAM permissions boundary in Account A directly grants the external role
access.

### 46

An order-processing queue must preserve order per customer and suppress
duplicate message submissions within the supported deduplication model while
allowing different customers to process in parallel.

Which design is best?

A. SQS standard queue with one consumer

B. EventBridge Scheduler with one global schedule

C. SNS topic with no subscriptions

D. SQS FIFO queue using the customer ID as the message group ID and an
appropriate deduplication ID

### 47

A global active-passive application uses Route 53 Application Recovery
Controller routing controls. Operators need a highly available mechanism for
changing whether Route 53 records are considered healthy during a Regional
failover.

How do routing controls provide this capability?

A. They edit every client's DNS cache directly.

B. They use ARC cluster endpoints to change routing-control state, which is
represented through Route 53 health checks used by the records.

C. They replace the Route 53 data plane with an Application Load Balancer.

D. They wait for the primary Region's CloudFormation stack to be deleted.

### 48 - Choose TWO

A SQL Server database is migrating to Amazon RDS for SQL Server with minimal
downtime and no engine change.

Which TWO actions are appropriate?

A. Use AWS Schema Conversion Tool to convert all schema objects to PostgreSQL.

B. Use AWS DMS full load plus change data capture.

C. Copy active database files with DataSync while SQL Server is writing.

D. Use AWS Application Migration Service to transform the database engine.

E. Validate the target and cut over after replication lag meets the approved
threshold.

### 49

An IAM role's permissions policy allows writes to an S3 bucket, but a workload
cannot assume the role.

Which policy should be checked first to determine who may assume the role?

A. The role's trust policy

B. The S3 lifecycle policy

C. The role's permissions boundary only

D. The organization's tag policy

### 50

A company serves objects from S3 buckets in two Regions. It wants one global
endpoint that routes requests to the closest active bucket and can fail over
based on policy, while cross-Region replication maintains object copies.

Which service provides the global S3 access endpoint and routing layer?

A. S3 gateway endpoints

B. Amazon EFS replication

C. S3 Multi-Region Access Points

D. AWS Storage Gateway Volume Gateway

### 51 - Choose TWO

An Auto Scaling group launches large instances whose application installation
takes 20 minutes. Traffic is bursty but predictable, and the company wants
faster scale-out without keeping all instances fully in service.

Which TWO actions can reduce scale-out latency?

A. Configure an Auto Scaling warm pool with instances in an appropriate stopped
or running state.

B. Place installation steps only in a manual operator runbook.

C. Bake stable dependencies into a launch-template AMI.

D. Disable health checks during every scale-out event.

E. Replace Auto Scaling with one permanently oversized instance.

### 52

A large VMware migration needs assisted discovery, application grouping,
AI-generated migration waves, source-network conversion to AWS constructs, and
guided rehosting to Amazon EC2.

Which service most directly provides this integrated migration experience?

A. AWS Transfer Family

B. Amazon AppStream 2.0

C. AWS Backup

D. AWS Transform for migrations

### 53

An SCP attached to the organization root denies an action. An administrator
tries the same action while signed in to the AWS Organizations management
account.

What is the relevant SCP boundary?

A. SCPs restrict identities in the management account exactly like member
accounts.

B. SCPs do not restrict identities in the management account, although other
policies and explicit denies can still apply.

C. SCPs grant the management account every service permission.

D. The SCP becomes a permissions boundary on the root user.

### 54 - Choose TWO

S3 object-created events must trigger three independent processing paths. Each
path needs durable buffering and independent retry behaviour so one slow
consumer does not block the others.

Which TWO components form an appropriate design?

A. Send all work to one SQS queue and have the three processors compete for
each message.

B. Invoke all processors synchronously from the S3 request thread.

C. Route the events through EventBridge or SNS to separate targets.

D. Use one Lambda execution environment as the durable message store.

E. Give each processing path its own SQS queue and dead-letter configuration.

### 55

Two Kinesis consumers are falling behind because they share each shard's
standard read throughput. Both require predictable dedicated throughput and
low-latency delivery.

Which feature should be enabled?

A. Kinesis enhanced fan-out for the consumers

B. SQS long polling on the stream

C. DynamoDB strongly consistent reads

D. S3 Transfer Acceleration

### 56

A company must automate structured business-to-business message exchange with
partners using the AS2 protocol, with messages stored in S3.

Which managed service should it use?

A. AWS Transfer Family AS2

B. AWS DataSync

C. AWS Application Migration Service

D. Amazon MQ for MQTT

### 57 - Choose TWO

An organization is building an SCP that denies use of unapproved Regions while
allowing required global services to operate.

Which TWO design practices are correct?

A. Assume every global service is automatically exempt from all Region
condition logic.

B. Use `aws:RequestedRegion` to scope the Regional deny.

C. Use security-group rules to control IAM and Route 53 API endpoints.

D. Put an Allow statement in a tag policy and treat it as a permission grant.

E. Maintain an explicit exemption such as `NotAction` for required global
service actions, including Route 53 where applicable.

### 58

A multiplayer game uses UDP endpoints in several Regions. It needs static
anycast entry addresses and fast routing away from unhealthy Regional
endpoints.

Which service is the best fit?

A. Amazon CloudFront signed cookies

B. Route 53 Resolver outbound endpoints

C. AWS Global Accelerator

D. AWS Client VPN

### 59

Two clients update the same DynamoDB item. The application must prevent a stale
client from silently overwriting a newer version without using a pessimistic
database lock.

Which mechanism should be used?

A. A conditional update that checks an expected version attribute

B. A table scan before every write

C. A non-idempotent atomic counter as the only version check

D. A GSI with the same partition key

### 60 - Choose THREE

A company is creating a migration business case and wave plan for a mixed
estate. It needs portfolio cost and utilization analysis, detailed server
dependency evidence, and central application grouping and progress tracking.

Which THREE capabilities or services map to these needs?

A. AWS WAF for process-level dependency collection

B. Migration Evaluator for portfolio discovery and business-case analysis

C. S3 Glacier Deep Archive as the migration wave scheduler

D. AWS Application Discovery Service for server inventory and dependency data

E. Route 53 Resolver DNS Firewall as the cost model

F. AWS Migration Hub for application grouping and migration tracking

### 61

Security teams must centrally block DNS queries from VPCs to known malicious
domains while allowing approved internal and external name resolution.

Which service should be used?

A. AWS Network Load Balancer

B. AWS Direct Connect Gateway

C. Amazon Macie

D. Route 53 Resolver DNS Firewall

### 62

An application needs a shared in-memory cache for simple ephemeral key-value
objects. It does not require persistence, replication, pub/sub, sorted sets, or
automatic failover, and horizontal scaling simplicity is the priority.

Which ElastiCache engine best fits?

A. Memcached

B. Valkey or Redis OSS with Multi-AZ as a mandatory requirement

C. DynamoDB Accelerator for a non-DynamoDB data source

D. Amazon Neptune

### 63 - Choose TWO

An API team wants an alarm when application logs contain repeated payment
failures and wants traces that show latency across API Gateway, Lambda, and
downstream calls.

Which TWO capabilities should be used?

A. A CloudWatch Logs metric filter with a CloudWatch alarm

B. S3 Inventory as the real-time failure alarm

C. AWS X-Ray tracing for the request path

D. AWS Artifact reports for live application latency

E. An SCP that counts matching log lines

### 64

An organization wants one security account to aggregate Security Hub findings
and manage organization-wide member enrollment where supported.

Which organizational role should be configured for that security account?

A. Security Hub delegated administrator

B. AWS Backup vault owner

C. Route 53 Resolver rule owner

D. Billing alternate contact

### 65

A multi-Region active-passive application requires an operator-controlled
failover with an RTO under two minutes. The recovery mechanism must use a highly
available control plane designed for changing Route 53 health-check state.

Which capability should be used?

A. Route 53 Application Recovery Controller routing controls

B. A CloudFormation stack deletion in the primary Region

C. DNS TTL changes performed after the outage starts

D. An S3 lifecycle transition

### 66 - Choose THREE

An organization is designing a centralized log archive bucket. CloudTrail must
write encrypted logs, workload administrators must not read or delete them, and
security auditors need controlled read access.

Which THREE controls are appropriate?

A. Public-read ACLs so auditors do not need IAM

B. A bucket policy that permits the CloudTrail delivery service with the
required source conditions

C. Workload-account administrator permissions to delete archive objects

D. A KMS key policy that supports the CloudTrail encryption path and authorized
auditor decryption

E. A lifecycle rule as the only access-control mechanism

F. Least-privilege auditor roles with explicit read and decrypt permissions

### 67

An application restores a large EBS volume from a snapshot before a recovery
exercise. The first reads are too slow because blocks are loaded lazily. The
team needs predictable full-volume performance immediately after creation.

Which feature should it use?

A. EBS Recycle Bin

B. S3 Transfer Acceleration

C. EBS Fast Snapshot Restore

D. EC2 hibernation

### 68

A multi-Region application writes objects to S3. It needs replication to a
second Region and must prevent accidental deletion of current and previous
object versions in the destination by ordinary administrators.

Which design best meets the requirement?

A. Enable S3 Versioning and cross-Region replication, and protect retained
versions with appropriately configured S3 Object Lock where required.

B. Use one unversioned bucket and rely on DNS caching.

C. Copy objects with an EC2 cron job and disable encryption.

D. Store the objects only on instance store volumes.

### 69 - Choose TWO

A producer role in Account A must send messages to an SQS queue in Account B.
No organization guardrail denies the request.

Which TWO policy elements are normally required?

A. A security group attached directly to the SQS queue

B. A permissions boundary in Account B that grants the producer access

C. Identity permission for the producer role to call `sqs:SendMessage` on the
queue ARN

D. An S3 bucket ACL naming the queue

E. A queue resource policy in Account B permitting the producer principal or
an appropriate delegated scope

### 70

A global leaderboard requires sorted sets, atomic score updates, replication,
and automatic failover. The cache is not limited to DynamoDB access.

Which service is the best fit?

A. ElastiCache for Valkey or Redis OSS with an appropriate replication design

B. ElastiCache for Memcached with no replication

C. DynamoDB Accelerator as a general-purpose multi-database cache

D. Amazon S3 Select

### 71

A company wants organization-wide visibility into S3 storage growth, activity,
and cost-efficiency trends, including recommendations and drill-down by account
and bucket.

Which service is designed for this analysis?

A. AWS Application Discovery Service

B. Route 53 Traffic Flow

C. Amazon Inspector

D. S3 Storage Lens

### 72 - Choose TWO

Architects are comparing an SCP with an IAM permissions boundary.

Which TWO statements are correct?

A. An SCP limits the maximum permissions available to identities in affected
member accounts or OUs.

B. A permissions boundary grants every action that it lists.

C. A permissions boundary limits the maximum identity-based permissions of the
IAM principal to which it is attached.

D. An SCP affects resources in the management account in the same way as member
accounts.

E. Either control can replace the role's trust policy.

### 73

A private S3 origin serves content through CloudFront. The company wants signed
origin requests, support for SSE-KMS, and no direct public bucket access.

Which CloudFront feature should be used?

A. S3 static website hosting with a public ACL

B. Route 53 Resolver inbound endpoints

C. Origin access control

D. AWS Global Accelerator client affinity

### 74

An organization wants to deploy and monitor a packaged set of AWS Config rules
and remediation guidance consistently across multiple accounts and Regions.

Which AWS Config capability best matches the requirement?

A. Conformance packs

B. CloudTrail Lake event data stores

C. VPC Reachability Analyzer

D. S3 Multi-Region Access Points

### 75 - Choose THREE

A critical application uses an active-passive multi-Region design. The standby
environment must be continuously ready at reduced capacity, data must replicate
to the secondary Region, and failover must be exercised without relying on an
untested runbook.

Which THREE practices best support the requirement?

A. Maintain a scaled-down but functional warm-standby stack in the secondary
Region.

B. Depend on backups only and create all infrastructure after the incident.

C. Configure appropriate cross-Region data replication and monitor replication
health.

D. Keep the secondary Region disconnected from configuration updates.

E. Automate and regularly test the failover and failback procedure.

F. Treat successful backup jobs as proof that the application meets its RTO.

---

## Frozen Submission Template

```text
Start: 21:28
End: 23:16
Uncertain: 4,7,9,15,19,29,33,34,37,38,47,51,56,63,66,72

1:B
2:BD
3:C
4:ACE
5:C
6:B
7:BD
8:D
9:CE
10:D
11:B
12:AEF
13:B
14:C
15:BD
16:B
17:A
18:CE
19:D
20:ABD
21:D
22:B
23:AD
24:D
25:B
26:CE
27:D
28:D
29:BE
30:C
31:AC
32:B
33:C
34:BE
35:C
36:C
37:BE
38:B
39:D
40:BCF
41:A
42:AD
43:B
44:C
45:BD
46:D
47:B
48:BE
49:A
50:C
51:AC
52:D
53:B
54:CE
55:A
56:A
57:BE
58:C
59:A
60:BDF
61:D
62:A
63:AC
64:A
65:A
66:BDF
67:C
68:A
69:CE
70:A
71:D
72:AC
73:C
74:A
75:ACE

Submission status: FROZEN
```

The frozen submission was independently assessed after completion. Keep this
question-and-submission artifact separate from the
[answer-bearing review](sap-c02-full-mock-005-review-20260807.md).
Shola updated 47 and 56 the answer to reflect the correct answers
