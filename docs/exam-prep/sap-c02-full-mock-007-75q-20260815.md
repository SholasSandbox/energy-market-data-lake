# AWS Certified Solutions Architect - Professional (SAP-C02)

## Full Mock 007 - 75 Questions

<!-- markdownlint-disable MD013 MD060 -->

**Created:** 2026-08-14<br>
**Scheduled attempt:** 2026-08-15<br>
**Mode:** Fresh closed-book, complexity-calibrated full simulation<br>
**Difficulty target:** Equivalent practical reasoning load to the reviewed AWS Skill Builder SAP-C02 assessment<br>
**Time limit:** 180 minutes<br>
**Question types:** 48 single-response and 27 multiple-response questions<br>
**Submission status:** SUBMITTED; frozen for independent marking

Multiple-response questions receive credit only when every required option and
no additional option is selected.

This original practice examination follows the current SAP-C02 domain
weightings and uses multi-constraint scenarios, plausible partial-fit
distractors, balanced answer positions, and exact-match composition traps. Its
difficulty is calibrated to the aggregate characteristics of the locally
reviewed Skill Builder assessment; it cannot be claimed to be psychometrically
identical without AWS item-performance data. It contains no proprietary AWS
assessment wording. It is not an AWS examination and does not reproduce live
exam questions. The answer key, domain labels, rationales, transfer labels, and
scoring hints are deliberately excluded from this document.

## Attempt rules

1. Keep lessons, reviews, answer keys, documentation, search, and AI assistance
   closed until the submission is frozen.
2. Select one answer unless the question says **Choose TWO** or **Choose
   THREE**.
3. Record start and end times, interruptions, and uncertain question numbers.
4. Stop when the 180-minute timer expires and guess rather than leave an item
   blank.
5. Complete the answer template and change `Submission status` to `FROZEN`
   before requesting marking. Do not revise a frozen response.
6. For multiple-response items, perform one final requirement-to-selection
   check before submission if time remains.

---

## Questions

### 1

A company has production VPCs in three AWS Regions. Each Region uses a transit
gateway with separate route tables for production, development, and shared
services. A newly acquired business must privately access shared services in
all Regions. The architecture must remain transitive, preserve segmentation,
and avoid a full mesh of VPC peering connections or third-party appliances.

Which solution best meets these requirements?

A. Peer every acquired VPC with every shared-services VPC and enable route
propagation through the peered VPCs.

B. Connect the VPCs through interface endpoints and use endpoint policies to
route arbitrary private IP traffic between Regions.

C. Create inter-Region transit gateway peering attachments, add the required
static routes, and preserve segmentation with regional transit gateway route
table associations and propagations.

D. Assign public IPv4 addresses to the shared services and restrict access by
source security group.

### 2 - Choose TWO

An organization routes traffic from hundreds of spoke VPCs through a central
inspection VPC. Stateful firewall appliances sit behind a Gateway Load
Balancer. During failover and scaling events, return traffic sometimes reaches
a different appliance and established flows are dropped. The organization
wants to retain transit-gateway-based segmentation.

Which TWO changes address the problem?

A. Enable source/destination checks on every firewall appliance network
interface.

B. Enable appliance mode on the transit gateway attachment for the inspection
VPC.

C. Configure the transit gateway route tables so both directions of inspected
traffic traverse the inspection VPC attachment.

D. Replace the Gateway Load Balancer with a Network Load Balancer because only
Network Load Balancers preserve flow symmetry.

E. Enable appliance mode on every spoke VPC attachment and remove the
inspection VPC route tables.

### 3

A company uses AWS Organizations and AWS Control Tower. Product teams need new
accounts quickly, but account vending must apply approved identity, logging,
networking, and OU placement without distributing root credentials. Teams
already submit requests through an internal service catalogue.

Which solution provides the most appropriate governance model?

A. Create accounts manually from the management account and email root
credentials to each product owner.

B. Use Account Factory for Terraform or Account Factory with the approved
account blueprint and lifecycle automation.

C. Clone an existing member account by copying its CloudFormation stacks and
IAM users.

D. Create all workloads in one account and use tags as the only isolation
boundary.

### 4 - Choose THREE

A regulated enterprise wants centralized evidence across all current and
future organization accounts. Security operations needs API activity retained
in a log archive account, aggregated configuration history, and centrally
managed security findings. Workload administrators must not be able to disable
the organization-wide evidence sources.

Which THREE actions best meet these requirements?

A. Use IAM access keys from the management-account root user for every evidence
collector.

B. Create an organization CloudTrail trail that delivers to an encrypted S3
bucket in the log archive account and protect the destination with an
appropriate bucket policy.

C. Store all application access logs only on the local instance volumes that
produced them.

D. Configure AWS Config organization aggregation using an approved delegated
administrator.

E. Create independent trails in each workload account and grant workload
administrators permission to stop them during deployments.

F. Designate the appropriate delegated administrator and configure central
management for Security Hub across the organization.

### 5

A financial-services company needs private connectivity from two data centres
to multiple VPCs through transit gateways. Its availability target must survive
a device failure and the loss of one Direct Connect location. It also requires
predictable bandwidth and does not want the public internet as its primary
path.

Which design best meets the requirement?

A. Provision one Direct Connect connection and create two private VIFs on that
connection.

B. Create two Site-to-Site VPN tunnels over one customer internet circuit and
disable BGP.

C. Use VPC peering from the data centres and allow AWS to select an available
physical circuit.

D. Provision connections on separate devices in two Direct Connect locations,
use transit VIFs through Direct Connect gateways, and configure dynamic routing
to the regional transit gateways.

### 6 - Choose TWO

Applications in several VPCs must resolve `corp.example` records hosted on
premises. On-premises clients must also resolve private hosted-zone records in
AWS. Connectivity is provided through Direct Connect, and the DNS paths must
not use public resolvers.

Which TWO components are required?

A. Route 53 Resolver outbound endpoints and forwarding rules associated with
the relevant VPCs for `corp.example`.

B. Alias records that point the on-premises domain to a NAT gateway.

C. A public hosted zone containing copies of all private records.

D. Route 53 Resolver inbound endpoints whose IP addresses are queried by the
on-premises DNS resolvers for the AWS private namespaces.

E. An internet gateway in every VPC so Route 53 Resolver can reach the
authoritative on-premises servers.

### 7

An enterprise operates a global network across 20 Regions and several on-premises
sites. It wants a centrally defined network policy, automated segmentation, and
attachment-level route propagation without separately operating and peering a
large number of regional transit gateways.

Which AWS service is the best fit?

A. AWS Cloud WAN with a core network policy

B. Amazon VPC peering

C. AWS PrivateLink endpoint services

D. Amazon Route 53 Resolver DNS Firewall

### 8

An organization uses IAM Identity Center with an external identity provider.
The security team wants day-to-day workforce access administration delegated
outside the Organizations management account while preserving management-
account restrictions.

Which approach is most appropriate?

A. Share the management-account root credentials with the identity team.

B. Move the Organizations management account into the Security OU and attach
SCPs to restrict its root user.

C. Register an eligible member account as the IAM Identity Center delegated
administrator and manage assignments from that account, while retaining the
documented management-account limitations.

D. Create long-lived IAM users in every workload account and synchronize their
passwords from the identity provider.

### 9 - Choose TWO

An S3 replication rule copies objects encrypted with customer managed KMS keys
from a production account to a disaster-recovery account. Replication of
unencrypted objects succeeds, but encrypted objects fail. The destination must
own the replicas and use its own KMS key.

Which TWO changes are required?

A. Enable S3 Transfer Acceleration on both buckets.

B. Replace the destination customer managed key with an AWS managed key because
cross-account replication cannot use customer managed keys.

C. Configure the replication rule to replicate KMS-encrypted objects and name
the destination KMS key.

D. Give the source bucket public read access so the destination account can
copy the ciphertext.

E. Grant the S3 replication role the required decrypt permission on the source
key and encrypt permission on the destination key, with supporting key
policies.

### 10

An organization denies use of unapproved AWS Regions through an SCP. Global
services and approved central security operations must continue to work. A
recent blanket `aws:RequestedRegion` deny blocked required IAM actions.

What is the best correction?

A. Replace the SCP with security groups in the management account.

B. Attach the SCP only to IAM users because SCPs do not affect roles.

C. Remove every regional restriction because SCPs cannot distinguish Regions.

D. Refine the deny with documented exceptions for required global services and
approved principals or operations, then test the policy in a controlled OU
before broader attachment.

### 11

A company exposes an S3-based static application through CloudFront. Users must
not retrieve objects directly from S3, and the bucket must remain private. The
company also needs signed requests from CloudFront to S3 and support for SSE-KMS.

Which design best meets the requirements?

A. Enable S3 static website hosting and allow public read access from all
CloudFront edge IP addresses.

B. Configure CloudFront Origin Access Control and a bucket policy that grants
the distribution access using the distribution ARN condition.

C. Use an Origin Access Identity with the S3 website endpoint and disable block
public access.

D. Place a NAT gateway in front of the S3 bucket.

### 12 - Choose TWO

A media platform has three private S3 origins behind one CloudFront
distribution. The origin for a request depends on a combination of a signed
cookie claim, viewer country, and device class; path patterns alone cannot
express the decision. Direct access to every S3 origin must be blocked.

Which TWO actions satisfy the complete requirement?

A. Use Route 53 weighted records to inspect the signed cookie for every viewer
request.

B. Configure Origin Access Control and restrictive bucket policies for the S3
origins.

C. Create only cache behaviors for `/mobile/*` and `/desktop/*` and ignore the
cookie and country requirements.

D. Use Lambda@Edge on the origin-request event to select the appropriate
origin from the request context.

E. Make each bucket public and rely on unpredictable object names.

### 13

A payment API places accepted requests on an SQS FIFO queue. Mobile clients may
retry the same request, and downstream payment calls are not inherently
idempotent. The system must preserve per-order ordering and prevent duplicate
charges even after a visibility-timeout expiry.

Which design is most reliable?

A. Use a stable deduplication identifier at ingestion and a conditional
idempotency record around the payment side effect in the consumer.

B. Set the queue retention period to zero after the first consumer receives a
message.

C. Use a standard queue and assume each message is delivered exactly once.

D. Reduce the visibility timeout to one second so duplicate consumers finish
more quickly.

### 14 - Choose THREE

A public order API has unpredictable bursts. It must acknowledge an order only
after durable acceptance, process fulfillment asynchronously, prevent repeat
client requests from creating duplicate work, and isolate repeatedly failing
messages. The team wants no continuously running brokers.

Which THREE actions best satisfy these requirements?

A. Integrate API Gateway with SQS using an IAM role and request mapping.

B. Set the SQS visibility timeout to zero so multiple workers race on every
message.

C. Require a stable request idempotency key and enforce it in a durable state
store before applying side effects.

D. Store accepted orders only in API Gateway access logs.

E. Configure a dead-letter queue with an appropriate redrive policy.

F. Process the entire fulfillment workflow synchronously in API Gateway with a
15-minute timeout.

### 15

A globally distributed application requires strongly consistent reads and
writes across its supported Regions. It does not use TTL and can operate only
in the Region combinations supported by DynamoDB multi-Region strong
consistency.

Which DynamoDB design should the architect evaluate?

A. A global table using default multi-Region eventual consistency with
eventually consistent reads

B. A single-Region table fronted by CloudFront

C. Independent tables synchronized daily by AWS DataSync

D. A global table configured for multi-Region strong consistency

### 16 - Choose TWO

A company uses an Aurora global database for a critical application. It needs
a controlled switchover for planned regional maintenance and a documented
unplanned failover procedure. Applications use a global writer endpoint where
supported, and the team wants to minimize data loss.

Which TWO actions are appropriate?

A. Use managed switchover for planned operations when the primary and secondary
clusters are healthy and synchronized.

B. Assume the secondary is synchronously committed and therefore guarantees a
zero RPO during every regional failure.

C. Test the managed failover procedure and reconnect or redirect writers as
required by the selected endpoint and driver behavior.

D. Use S3 Cross-Region Replication as the database transaction log.

E. Promote a read replica by deleting the primary cluster first during every
planned maintenance window.

### 17

Thousands of Lambda invocations open short-lived connections to an Amazon RDS
database. During traffic spikes, database CPU remains moderate but connection
counts and authentication overhead exhaust available resources. The team wants
managed connection pooling and continued IAM database authentication support.

Which solution best meets the requirement?

A. Add a Network Load Balancer in front of the database endpoint.

B. Place Amazon RDS Proxy between the Lambda functions and the database.

C. Cache database passwords in Lambda environment variables and open more
connections.

D. Use CloudFront origin keep-alive settings for the RDS connection.

### 18

A Linux analytics application runs on EC2 instances in three Availability
Zones. All instances must concurrently read and update one shared directory,
observe one coherent POSIX namespace, and preserve file-locking behavior. The
design must not rely on sharing a zonal block volume across Availability Zones.

Which storage solution best meets the requirement?

A. One Multi-Attach EBS volume attached to instances in all three Availability
Zones

B. Instance store on every EC2 instance with S3 replication between hosts

C. One Regional Amazon EFS filesystem with mount targets in the required
Availability Zones

D. One EBS volume in each Availability Zone with hourly DataSync tasks between
the volumes

### 19

A latency-sensitive HPC workload needs a high-throughput parallel filesystem
that integrates with S3 for input and output data. Compute instances access the
filesystem concurrently during each job, and the environment is recreated for
new campaigns.

Which service is the best fit?

A. Amazon FSx for Lustre linked to the S3 data repository

B. Amazon EBS gp3 attached to one instance

C. Amazon S3 Glacier Deep Archive mounted as NFS

D. AWS Storage Gateway Tape Gateway

### 20 - Choose TWO

A company runs queued rendering jobs. Jobs checkpoint progress and can restart
after interruption. The compute fleet needs a proprietary agent baked into a
custom EC2 AMI, and the primary objective is to reduce compute cost while
retaining managed scheduling and scaling.

Which TWO choices meet the requirements?

A. Replace the job queue with an API Gateway REST API and process each render
synchronously.

B. Run every job on On-Demand dedicated hosts because checkpointing prevents
Spot use.

C. Use an AWS Batch Fargate compute environment and specify the custom EC2 host
AMI in the job definition.

D. Create an AWS Batch managed EC2 compute environment that references the
approved custom compute-resource AMI.

E. Use diversified Spot capacity for the interruption-tolerant compute
environment.

### 21

An Auto Scaling application spends most of its time waiting on downstream
services. CPU stays near 25% while request latency and queue depth increase.
Traffic is unpredictable. Testing identifies a stable relationship between
acceptable latency and requests per target.

Which scaling strategy is most appropriate?

A. Scheduled scaling at midnight only

B. Target tracking on an ALB request-count-per-target or validated custom
backlog metric

C. Target tracking on average CPU at 80%

D. Manual scaling after users report latency

### 22

A company wants to migrate x86 EC2 workloads to Graviton instances. Before
changing production, it needs rightsizing recommendations and evidence that
application binaries, agents, and dependencies are compatible with the new
architecture.

Which approach is best?

A. Enable detailed monitoring and assume a lower CPU metric means the migration
is safe.

B. Use AWS Cost Explorer only; cost data proves binary compatibility.

C. Change every instance type directly because all x86 binaries run unchanged
on Arm processors.

D. Use Compute Optimizer recommendations as an input, then test representative
workloads and dependencies on the target architecture before rollout.

### 23

A CloudFront distribution uses an origin group with a primary and secondary
origin. Reads should fail over automatically, but the application also accepts
POST and PUT requests that must remain available during a regional failure.

Which statement is correct?

A. CloudFront origin failover automatically retries every POST and PUT against
the secondary origin.

B. S3 Transfer Acceleration converts write requests into GET requests for
failover.

C. CloudFront origin failover can protect eligible read methods, but write
continuity needs a separate multi-Region application and routing design.

D. Origin Shield makes the primary origin regional-failure proof.

### 24 - Choose TWO

A stateful application uses warm standby in another Region. Database
replication and standby compute are already configured. During exercises, the
database is promoted and capacity scales, but clients continue using the failed
Region. The company requires a repeatable, observable failover with controlled
rollback.

Which TWO additions close the most important gaps?

A. Preconfigure and test health-aware DNS failover or ARC routing controls to
move client traffic.

B. Orchestrate the data, application, capacity, traffic, validation, and
rollback steps through tested runbooks or automation.

C. Increase the primary database instance size without changing the recovery
path.

D. Rely on engineers to edit local client hosts files during an incident.

E. Delete the standby capacity after each exercise to reduce recovery cost.

### 25 - Choose THREE

A company uses Route 53 Application Recovery Controller routing controls for
multi-Region recovery. Operators must be able to change routing-control state
during a severe regional incident even when ordinary service control-plane APIs
are impaired. The company also wants safeguards against unsafe state changes
and DNS-based traffic movement.

Which THREE components are part of the correct design?

A. Use the highly available ARC cluster data-plane endpoints for routing-control
state changes during recovery.

B. Use only the ordinary Route 53 control-plane API because ARC cluster
endpoints cannot change routing-control state.

C. Use routing-control health checks in Route 53 failover records to translate
the control state into DNS routing behavior.

D. Configure ARC safety rules to prevent unsafe routing-control combinations or
require coordinated overrides.

E. Replace Route 53 with an Application Load Balancer spanning multiple AWS
Regions.

F. Store routing-control state in an S3 object that operators edit during an
incident.

### 26

An organization hosts sensitive S3 buckets in member accounts. It already uses
SCPs to limit what identities in the organization may do. Security now wants a
centrally governed control that restricts which principals may access covered
resources, including requests made by identities outside the organization.

Which Organizations policy type should the architect evaluate?

A. Resource control policies

B. Chatbot policies

C. Backup policies

D. Tag policies

### 27 - Choose TWO

A security team wants continuous analysis of unintended external access to S3
buckets, KMS keys, IAM roles, and other supported resources. It also wants to
identify unused IAM permissions and credentials so teams can reduce access.

Which TWO capabilities best meet the requirements?

A. AWS Shield Advanced health-based detection

B. Amazon Inspector network reachability only

C. IAM Access Analyzer external access analyzers

D. IAM Access Analyzer unused access analyzers

E. Route 53 Resolver query logging

### 28

Security operations needs a central view that normalizes findings from services
such as GuardDuty, Inspector, and Macie, evaluates security standards, and
supports cross-account aggregation. It does not need another raw log archive.

Which service is the best fit?

A. AWS Artifact

B. Amazon Managed Service for Prometheus

C. AWS Security Hub

D. AWS CloudTrail Lake only

### 29

A platform team uses AWS Control Tower. It wants to prevent noncompliant
resources before deployment where supported, while retaining detective checks
for resources that already exist.

Which approach is most appropriate?

A. Use detective controls to rewrite every CloudFormation template before it
is submitted.

B. Apply suitable proactive controls for pre-deployment evaluation and
detective controls for continuous resource evaluation.

C. Use only manual console reviews because Control Tower controls cannot
evaluate resources.

D. Replace all controls with cost allocation tags.

### 30

An application in Account A needs to retrieve one secret stored in Account B.
The company requires short-lived role credentials, auditable access, and
encryption with a customer managed KMS key in Account B.

Which solution best meets the requirements?

A. Make the Secrets Manager secret public and restrict it by obscurity.

B. Share an Account B administrator access key with the application.

C. Copy the secret into an unencrypted environment variable in Account A.

D. Allow the application role to assume a tightly scoped role in Account B;
grant that role access to the secret and the required KMS key permissions.

### 31 - Choose TWO

An enterprise wants service-to-service connectivity among applications in many
VPCs and accounts. Teams should discover services by logical name and apply
central authentication and authorization policies without building a full mesh
of load balancers and peering connections. The services use HTTP and gRPC.

Which TWO design elements best fit?

A. Create an internet gateway in every VPC and publish all services publicly.

B. Use Amazon VPC Lattice service networks and associate the required VPCs and
services.

C. Use S3 gateway endpoints to route HTTP traffic between applications.

D. Put every service in one subnet and use network ACLs as the service registry.

E. Apply VPC Lattice auth policies using AWS IAM where identity-aware access is
required.

### 32

A centralized inspection VPC uses AWS Network Firewall. Traffic from spoke VPCs
must be inspected before reaching the internet, and return traffic must traverse
the same firewall path. Which architectural concern is most important?

A. Preserve symmetric routing through the firewall endpoints and transit
gateway route tables, using appliance mode where required.

B. Route return traffic directly from the internet gateway to each spoke while
bypassing inspection.

C. Enable source/destination checks on routing appliances to forward transit
traffic.

D. Configure asymmetric routes so each direction uses a different firewall
endpoint.

### 33

A genomics pipeline submits containerized jobs to AWS Batch. Each compute host
must include a licensed kernel module and monitoring agent installed through an
approved custom EC2 AMI. Jobs checkpoint frequently and tolerate interruption.
The company wants managed capacity and the lowest practical compute cost.

Which solution best meets the requirements?

A. A Fargate Batch compute environment configured with the custom host AMI and
Spot Fleet

B. Lambda functions packaged with the kernel module and configured for six-hour
executions

C. A fixed On-Demand EC2 fleet outside Batch with no job queue

D. A managed EC2 Batch compute environment using the approved custom AMI and a
diversified Spot allocation strategy

### 34

An EKS platform team needs to grant pods access to S3 without placing node-role
credentials in every pod or storing long-lived keys in Kubernetes Secrets.
Permissions must be scoped to the workload identity.

Which solution is most appropriate?

A. Make the S3 bucket public to remove the credential requirement.

B. Grant the node instance profile full S3 access for every cluster workload.

C. Use EKS Pod Identity or IAM roles for service accounts with a dedicated IAM
role and least-privilege policy.

D. Store an administrator access key in a ConfigMap.

### 35 - Choose TWO

An ECS service behind an Application Load Balancer requires blue/green
deployments. The team needs a production listener, a separate test path for the
replacement task set, controlled traffic shifting, and automatic rollback when
deployment alarms fail.

Which TWO configurations are required parts of the design?

A. Use CodeDeploy as the ECS deployment controller with production and test
listeners mapped to the appropriate target groups.

B. Configure Systems Manager Patch Manager as the ECS traffic-shifting engine.

C. Register both task sets permanently in one target group and edit task IP
addresses manually.

D. Configure CloudWatch alarms and rollback behavior in the deployment group.

E. Replace the ALB with an S3 website endpoint.

### 36

A Lambda function consumes SQS batches. One malformed record causes the whole
batch to fail, so successfully processed records are repeatedly delivered. The
application can identify failures per message.

Which change reduces unnecessary retries while preserving failed-message
redelivery?

A. Disable the dead-letter queue and catch all exceptions without reporting
failures.

B. Enable partial batch responses and return identifiers for only the failed
messages.

C. Set the visibility timeout to zero.

D. Delete the SQS queue after every successful invocation.

### 37 - Choose THREE

An enterprise wants cross-account observability without treating CloudTrail as
an application-metrics service. Operations needs shared CloudWatch metrics,
logs, and traces from workload accounts, API audit history in a log archive,
and request correlation through distributed services.

Which THREE capabilities map to these needs?

A. Route 53 Resolver DNS Firewall as the metrics aggregator

B. CloudWatch cross-account observability through Observability Access Manager
links and sinks

C. An organization CloudTrail trail for API audit activity

D. S3 Transfer Acceleration as the trace collector

E. AWS X-Ray tracing or compatible trace instrumentation for request paths

F. AWS Artifact as the live application log stream

### 38

A monitoring platform needs a near-real-time stream of selected CloudWatch
metrics delivered to a third-party HTTP endpoint through a managed delivery
path. It does not want to poll `GetMetricData` across hundreds of accounts.

Which design is most appropriate?

A. CloudWatch metric streams delivered through Amazon Data Firehose

B. VPC Flow Logs delivered to CloudWatch Logs

C. AWS Config snapshots sent once per month

D. An S3 lifecycle transition to Glacier Flexible Retrieval

### 39 - Choose TWO

A company has a stable baseline of EC2, Fargate, and Lambda compute usage, plus
fault-tolerant nightly batch jobs that can be interrupted. It wants to reduce
cost without locking every workload to one instance family or treating the
batch jobs as continuously committed usage.

Which TWO choices are appropriate?

A. Purchase Dedicated Hosts for every Fargate task.

B. Purchase an appropriately sized Compute Savings Plan for the stable baseline.

C. Run the restartable batch capacity on diversified Spot where supported.

D. Cover the maximum observed burst with a three-year EC2 Instance Savings Plan
for one family and Region.

E. Replace the batch queue with provisioned NAT gateways in every Availability
Zone.

### 40

An S3 data lake stores billions of objects. The team wants to discover object
encryption status, replication state, storage class, and other metadata at
scale without issuing a HEAD request for every object.

Which feature is best suited to produce the source inventory?

A. Amazon EBS fast snapshot restore

B. Amazon S3 Inventory

C. AWS Storage Gateway Volume Gateway

D. Amazon S3 Select

### 41

An unpredictable relational workload has long idle periods but requires
Aurora compatibility and rapid scaling without switching database engines.
Connections must survive normal capacity adjustments as far as the engine and
driver support.

Which option should the architect evaluate?

A. DynamoDB provisioned capacity with DAX

B. RDS Custom for Oracle on permanently fixed instances only

C. Aurora Serverless v2 with appropriate minimum and maximum ACUs

D. S3 Glacier Instant Retrieval

### 42

A DynamoDB table receives writes for one very popular tenant. The partition key
is the tenant ID, creating a hot partition. The application can calculate a
small suffix and can query multiple suffixes when retrieving the tenant's data.

Which change best distributes the workload?

A. Add a DAX cluster and leave the write key unchanged because DAX redistributes
writes across base-table partitions.

B. Move the table to one Availability Zone.

C. Increase the item size so each write consumes more capacity.

D. Write-shard the partition key with a calculated or random suffix and fan out
reads across the known suffix set.

### 43 - Choose TWO

A Kinesis Data Streams workload has several independent consumers. One consumer
requires dedicated read throughput and low propagation delay. Incoming volume
also varies substantially, and the team wants to reduce manual shard planning.

Which TWO choices address the requirements?

A. Replace every partition key with one constant value to preserve global
ordering.

B. Configure an SQS visibility timeout for each Kinesis shard.

C. Register the latency-sensitive consumer for enhanced fan-out.

D. Use DynamoDB DAX as the Kinesis consumer registry and throughput allocator.

E. Evaluate on-demand capacity mode for the variable stream workload.

### 44

A company needs a managed Apache Kafka service because existing applications
depend on Kafka APIs, Kafka client libraries, and Kafka ecosystem tooling. It
does not want to rewrite the clients for a different streaming API.

Which service best meets the requirement?

A. Amazon Managed Streaming for Apache Kafka

B. AWS Step Functions Express Workflows

C. Amazon EventBridge Scheduler

D. Amazon SQS Standard

### 45

An OpenSearch production domain must tolerate an Availability Zone failure with
predictable capacity and reduced recovery disruption. The workload is steady,
and the company prefers a managed architecture that reserves standby capacity.

Which option should the architect evaluate?

A. One data node in one Availability Zone with daily snapshots only

B. A CloudFront distribution in front of one OpenSearch node

C. Multi-AZ with Standby using the supported topology and instance counts

D. An S3 gateway endpoint as the OpenSearch cluster manager

### 46

A central platform team must deploy and update approved IAM roles, Config rules,
and logging resources across many organization accounts and OUs. New accounts
should receive the baseline automatically, and operations wants centralized
deployment status.

Which solution is most appropriate?

A. S3 Cross-Region Replication of the templates without executing them

B. One CloudFormation stack in the management account with resources that
magically span all member accounts

C. Manual console deployment in each account

D. Service-managed CloudFormation StackSets integrated with AWS Organizations
and targeted to the required OUs

### 47 - Choose TWO

A ransomware-resilience programme requires protected recovery copies that are
logically isolated from workload accounts and evidence that applications can
actually be restored. Backup success notifications alone are insufficient.

Which TWO actions best meet the requirements?

A. Copy eligible backups into an AWS Backup logically air-gapped vault with the
required governance and access model.

B. Configure scheduled AWS Backup restore testing and validate application-level
recovery outcomes.

C. Store the only backup in the same writable application account and allow
administrators to delete it immediately.

D. Treat a successful snapshot API response as proof of application recovery.

E. Disable backup encryption so incident responders can download recovery data
publicly.

### 48

A business owner defines an RPO of 15 minutes and an RTO of 2 hours for a
regional application. The current design takes nightly snapshots and has no
tested infrastructure deployment in another Region.

What should the architect do first?

A. Buy larger primary instances without designing regional recovery.

B. Identify the gap between the business objectives and the current designed
and tested capability, then select and test a recovery pattern that can meet
both objectives.

C. Rename the nightly snapshot interval as a 15-minute RPO.

D. Declare the objectives met because snapshots exist.

### 49 - Choose THREE

A company is designing a controlled multi-Region recovery process. It wants
operators to avoid split-brain routing, automate the main recovery sequence,
and prove that restored dependencies and user traffic work before declaring
success.

Which THREE practices best support the objective?

A. Define an authorized recovery decision and safety controls that prevent
unsafe simultaneous routing states.

B. Consider DNS changes alone proof that the database and application recovered.

C. Delete all recovery logs after each exercise.

D. Automate or script dependency-aware data promotion, application activation,
capacity scaling, and traffic movement.

E. Depend on one engineer's undocumented console sequence.

F. Validate the recovered service and retain a rollback or failback plan.

### 50

An organization wants centrally defined domain allowlists and blocklists for DNS
queries originating from many VPCs. It also needs query activity available for
investigation.

Which combination is most appropriate?

A. Route 53 Resolver DNS Firewall rule groups plus Resolver query logging

B. Direct Connect MACsec plus EBS snapshot events

C. CloudFront signed URLs plus AWS Budgets

D. Network ACLs plus S3 server access logs

### 51 - Choose TWO

A company will rehost 600 on-premises servers in migration waves. It needs
continuous block-level replication, non-disruptive test launches before each
cutover, and a way to preserve centrally tracked wave status. Application
owners must validate test instances before the final cutover window.

Which TWO actions best meet the requirements?

A. Use AWS DataSync as a block-level boot-volume replication and server launch
service.

B. Export every physical server as an AMI without testing dependencies.

C. Use S3 Cross-Region Replication to create EC2 instances from on-premises
disks.

D. Use AWS Application Migration Service for replication, test launches, and
cutover launches.

E. Group applications into waves and track discovery and migration status
through the selected Migration Hub or orchestration workflow.

### 52

A migration team must discover dependencies for physical Linux servers and
VMware virtual machines. It needs host-level process and network-connection
details for the physical servers. The VMware estate can initially be assessed
without agents, but the team understands that agentless collection has a
different evidence boundary.

Which strategy is most appropriate?

A. Use Route 53 health checks to infer installed processes and server
dependencies.

B. Use Amazon Inspector to create migration dependency maps for all on-premises
hosts.

C. Use the Agentless Collector alone and assume it always produces identical
host-level dependency evidence for every physical and virtual server.

D. Install AWS Application Discovery Agent where host-level process and TCP
dependency evidence is required, and use the Agentless Collector for supported
VMware inventory and assessment use cases.

### 53

A company is moving a 15 TB transactional Oracle database to Amazon Aurora
PostgreSQL. The permitted cutover outage is less than 30 minutes. Stored
procedures and schema objects require conversion, and source changes must be
captured while the target is loaded and validated.

Which approach best meets the requirements?

A. Take one logical export at the start of the project and ignore subsequent
source transactions.

B. Use AWS Schema Conversion Tool or AWS Database Migration Service Schema
Conversion for heterogeneous schema conversion, then AWS DMS full load plus
CDC until cutover.

C. Use EBS snapshot copy because Oracle and Aurora PostgreSQL share a native
storage format.

D. Use S3 Cross-Region Replication to transform stored procedures into
PostgreSQL code.

### 54

A business wants to move a supported commercial database to Amazon RDS while
retaining the same engine and minimizing operational change. Licensing,
extension support, and feature compatibility have been validated. The primary
goal is to transfer database administration responsibility without redesigning
the application.

Which migration strategy best describes the decision?

A. Repurchase an unrelated SaaS product

B. Retire the application

C. Replatform to the managed database service

D. Refactor into event-driven microservices

### 55 - Choose TWO

A research facility must move 900 TB to S3 before a data-centre closure. Its
network cannot transfer the initial dataset in time, but it can support daily
incremental synchronization after the bulk copy. Files continue changing until
cutover, and the final result must be validated.

Which TWO services or actions best meet the requirement?

A. Use an appropriate AWS Snow Family offline transfer for the initial bulk
dataset.

B. Use SQS FIFO to copy file blocks to S3.

C. Use AWS DataSync over the available network for incremental changes and
final verification after the bulk import.

D. Create one Site-to-Site VPN and assume it changes the physical bandwidth of
the existing circuit.

E. Use EBS Multi-Attach to attach the on-premises disks directly to S3.

### 56

A manufacturer exchanges signed and encrypted EDI files with external partners
over AS2. Files must land in S3, partner identities and certificates must be
managed, and message disposition notifications must be available. The company
does not need an ActiveMQ or RabbitMQ broker.

Which service is the best fit?

A. AWS Transfer Family AS2

B. Amazon AppFlow

C. AWS DataSync

D. Amazon MQ

### 57

A company is modernizing a portfolio of C# applications from .NET Framework on
Windows to supported cross-platform .NET versions. It needs automated analysis,
a proposed transformation plan, and assisted code conversion. Private package
dependencies and Windows-specific behavior still require human decisions, and
the company will not treat generated code as production-ready without build,
unit, integration, and behavioral validation.

Which approach best fits?

A. Move the source repository to S3 and declare the application modernized.

B. Use AWS Transform for .NET for assessment and transformation, then review
the transformation report and validate the generated changes, unresolved
dependencies, target runtime, and application behavior.

C. Use AWS Budgets to convert the application into Lambda functions.

D. Replace all integration testing with a Cost Explorer forecast.

### 58

A VMware estate contains tightly coupled applications that cannot all be
replatformed before a fixed facility-exit date. The company wants a staged
strategy: rapidly relocate eligible workloads, preserve network connectivity
and dependency evidence, then modernize selected services after stabilization.

Which plan is most appropriate?

A. Copy virtual-disk files to Glacier Deep Archive and boot them directly from
the archive at cutover.

B. Retire all systems that cannot immediately run as Lambda functions.

C. Refactor every application simultaneously before collecting dependency data.

D. Use discovery and dependency evidence to form waves, rehost eligible systems
with an appropriate migration service, validate each wave, and schedule later
replatforming or refactoring based on business value.

### 59 - Choose TWO

A migration programme has hundreds of applications. Executives need portfolio-
level status, while technical teams need wave-specific tasks and repeatable
runbooks. The programme must not confuse status tracking with the services that
perform server or database replication.

Which TWO actions best support the requirement?

A. Use Migration Hub as the block-level replication engine for every server.

B. Use Migration Hub capabilities to aggregate discovery and migration status
from supported tools.

C. Use AWS Budgets as the database change-data-capture service.

D. Use Migration Hub Orchestrator templates or another governed orchestration
workflow for repeatable wave tasks where appropriate.

E. Use CloudFront invalidations to sequence application cutovers.

### 60

A data warehouse runs complex SQL analytics against petabytes of structured
data. The company wants a managed AWS warehouse, columnar storage, massively
parallel processing, and the ability to query selected S3 data without first
loading all of it.

Which target is the best fit?

A. Amazon SQS

B. Amazon DocumentDB

C. Amazon Redshift with Spectrum where appropriate

D. Amazon ElastiCache for Redis

### 61 - Choose THREE

A monolithic order platform is being decomposed gradually. The new design must
accept bursts without coupling producers to consumers, coordinate a multi-step
business process with explicit retries and compensation, and distribute domain
events to several independently evolving targets. The team wants managed
services and observable failure handling.

Which THREE components best fit the requirements?

A. One synchronous Lambda invocation chain with no durable state

B. Amazon SQS queues for buffering and worker decoupling

C. One shared database table used as an undocumented event bus

D. AWS Step Functions for the stateful business-process orchestration

E. Amazon EventBridge for routing domain events to multiple targets

F. A public S3 bucket polled every second by every consumer

### 62

A finance team wants early notification when AWS spend deviates unexpectedly
from historical patterns, including service-level anomalies that remain below a
monthly budget threshold. Existing budgets must continue to track planned
limits.

Which addition best addresses the anomaly requirement?

A. AWS Cost Anomaly Detection with suitable monitors and subscriptions

B. Route 53 DNSSEC signing

C. S3 Object Lock compliance mode

D. A larger EC2 Auto Scaling group

### 63 - Choose TWO

A storage team must reduce S3 cost across many accounts. It needs organization-
wide trend and activity insights, plus object-level metadata that can drive a
controlled lifecycle or remediation analysis. It must not infer per-object
state from aggregate percentages alone.

Which TWO sources best meet the requirements?

A. EC2 instance system logs

B. Route 53 Resolver query logs

C. S3 Storage Lens with organization-level dashboards and metrics

D. DynamoDB Streams from an unrelated table

E. S3 Inventory reports for object-level metadata

### 64

An EC2 fleet is consistently overprovisioned, but the team does not want to
resize instances solely from average CPU. Memory, network, disk, and application
latency constraints differ by workload. Which process is most appropriate?

A. Disable monitoring to reduce metric charges.

B. Purchase Reserved Instances before determining the target footprint.

C. Use Compute Optimizer and observed workload metrics as inputs, validate
recommendations against application constraints, test changes, and monitor the
result.

D. Resize every instance to the smallest family based only on one hour of CPU
data.

### 65

Private subnets in three Availability Zones send large volumes to S3 and
DynamoDB through one NAT gateway in a fourth path, causing processing charges
and a zonal dependency. Internet-bound traffic is otherwise modest.

Which change provides the largest direct improvement?

A. Route all private-subnet traffic through the single NAT gateway and enable
S3 Transfer Acceleration.

B. Add S3 and DynamoDB gateway endpoints to the relevant route tables, and use
zonal NAT gateways only for traffic that still requires internet egress.

C. Replace the NAT gateway with an internet gateway attached directly to each
private instance.

D. Copy all S3 objects to EBS volumes.

### 66

GuardDuty reports credential use from an unusual location followed by
reconnaissance and access to a sensitive EC2 workload. The incident team must
contain the threat, preserve evidence, and rotate compromised access without
destroying the instance prematurely.

Which incident-response plan is most appropriate?

A. Disable GuardDuty across the organization, remove the finding, and return
the credentials to service without investigating their activity.

B. Leave the workload reachable while publishing the instance-profile
credentials to other teams so they can reproduce the finding.

C. Delete the instance and every log immediately, then rotate credentials after
the evidence-retention period ends.

D. Quarantine the workload while preserving approved investigation access;
capture snapshots, logs, CloudTrail history, and relevant volatile evidence;
then disable or rotate the compromised credentials and investigate their use.

### 67 - Choose TWO

A global application serves cacheable static content over HTTPS and also runs a
latency-sensitive, non-HTTP multiplayer protocol over fixed TCP and UDP ports.
The company needs edge caching for the website and anycast-based acceleration
with static entry IP addresses for the game traffic.

Which TWO services map to the requirements?

A. Amazon CloudFront for the cacheable HTTP content

B. S3 gateway endpoints for public anycast game traffic

C. Route 53 Resolver inbound endpoints for content caching

D. AWS Global Accelerator for the TCP and UDP application traffic

E. AWS DataSync for persistent client connections

### 68

An operations team finds that administrators make emergency console changes to
CloudFormation-managed resources. The team wants to detect divergence and then
restore approved configuration through a controlled process rather than blindly
overwriting every change.

Which solution is best?

A. Use CloudFormation drift detection and AWS Config evidence to identify
differences, review the incident change, and update or redeploy the approved
source-controlled configuration.

B. Run an unrestricted Lambda function every minute that replaces resources
without checking impact.

C. Treat the live console state as the only source of truth and delete the
templates.

D. Disable CloudTrail so emergency changes are not recorded.

### 69

A critical application needs cross-Region recovery with an RPO measured in
seconds and an RTO under 15 minutes. Cost analysis permits scaled-down but
running application capacity in the recovery Region. The team can continuously
replicate data and automate traffic movement and scale-up.

Which disaster-recovery pattern best fits?

A. Backup and restore only

B. Warm standby

C. No recovery environment until after the incident

D. Monthly offline exports

### 70

A global TCP application runs behind Network Load Balancers in two Regions.
Clients require static anycast IP addresses, rapid endpoint health-based
traffic shifts, and improved routing over the AWS global network. Content is
not cacheable HTTP.

Which service should front the regional endpoints?

A. AWS Storage Gateway

B. Route 53 Resolver DNS Firewall

C. Amazon CloudFront with an S3 origin

D. AWS Global Accelerator

### 71 - Choose TWO

An organization requires centrally governed backups for member accounts. Copies
must be placed in a separate backup account, policies should target resources
using organization-aware selection, and encryption permissions must support the
cross-account copy path.

Which TWO actions are appropriate?

A. Store every backup only in the source account and grant all workload
administrators delete permission.

B. Use AWS Backup policies or centrally managed backup plans with the required
cross-account destination vault and Organizations configuration.

C. Configure destination-vault access, service roles, and KMS key policies to
permit the authorized copy while denying unintended access.

D. Use AMI launch permissions as the organization backup policy.

E. Disable encryption because encrypted recovery points cannot be copied
between accounts.

### 72

An EC2 application behind an Application Load Balancer requires immutable
deployments, weighted traffic shifting, validation hooks, and automatic rollback
based on alarms. The team does not use ECS.

Which deployment approach best fits?

A. S3 lifecycle transitions between blue and green storage classes

B. In-place SSH updates to every production instance with no rollback group

C. AWS CodeDeploy blue/green deployment for EC2/On-Premises with appropriate
target groups, hooks, traffic configuration, and alarms

D. Systems Manager Patch Manager as the weighted load-balancer traffic router

### 73

A read-heavy DynamoDB application repeatedly retrieves the same items and needs
microsecond response times. Developers can update the application to use a
DynamoDB-compatible cache client. The write partition-key design is already
healthy.

Which solution is most appropriate?

A. Amazon DynamoDB Accelerator with the application using the DAX client

B. A DAX cluster without changing the application client path

C. S3 Transfer Acceleration

D. An RDS Proxy in front of DynamoDB

### 74 - Choose TWO

A team wants to reduce Lambda cost and latency. Functions are memory-bound in
some paths and CPU-bound in others, while downstream throttling can make higher
concurrency harmful. The team wants evidence-driven tuning rather than simply
choosing the smallest memory size.

Which TWO actions are appropriate?

A. Use an EC2 Reserved Instance recommendation as the only Lambda sizing input.

B. Remove timeouts and concurrency limits so retries can grow without bound.

C. Set every function to the minimum memory because Lambda CPU allocation is
independent of memory.

D. Test representative invocations across memory settings and compare duration,
cost, and application outcomes using a tool or controlled benchmark.

E. Tune reserved or provisioned concurrency only after considering downstream
capacity, startup behavior, and the workload's latency requirement.

### 75 - Choose THREE

An enterprise is establishing a new production OU. It requires preventative
guardrails against leaving the organization and unauthorized Region use,
central detection of configuration drift, and a safe rollout that will not
accidentally block incident-response roles or required global services.

Which THREE actions best meet the requirements?

A. Attach an untested blanket deny to the organization root and remove all
break-glass paths.

B. Use tag policies as the permission boundary that prevents accounts from
leaving the organization.

C. Develop and test narrowly scoped SCPs in a controlled OU, including required
exceptions and rollback procedures, before production attachment.

D. Give every workload administrator access to disable the organization trail
and configuration recorder.

E. Enable organization-wide AWS Config recording and aggregation with the
required delegated-administrator design.

F. Validate policies with supported policy-analysis tools and run explicit
allowed and denied test cases using representative roles.

---

## Submission template

Complete this section only during the attempt. Freeze it before requesting
marking.

```text
Attempt date: Aug 15, 2026
Attempt number: 1
Start: 14:58
End: 17:20
Elapsed: 142 minutes
Interruption note:
Uncertain:5,14,16,25,27,40,51

1:C
2:BC
3:B
4:BDF
5:D
6:AD
7:A
8:C
9:CE
10:D
11:B
12:BD
13:A
14:ACE
15:D
16:AC
17:B
18:C
19:A
20:DE
21:B
22:D
23:C
24:AB
25:ACD
26:A
27:CD
28:C
29:B
30:D
31:BE
32:A
33:D
34:C
35:AD
36:B
37:BCE
38:A
39:BC
40:B
41:C
42:D
43:CE
44:A
45:C
46:D
47:AB
48:B
49:ADF
50:A
51:DE
52:D
53:B
54:C
55:AC
56:A
57:B
58:D
59:BD
60:C
61:BDE
62:A
63:CE
64:C
65:B
66:D
67:AD
68:A
69:B
70:D
71:BC
72:C
73:A
74:DE
75:CEF

Submission status: FROZEN
Submission date: 2026-08-15
```
