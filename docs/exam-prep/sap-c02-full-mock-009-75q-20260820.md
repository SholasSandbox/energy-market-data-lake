# AWS Certified Solutions Architect - Professional (SAP-C02)

## Full Mock 009 - 75 Questions

<!-- markdownlint-disable MD013 MD060 -->

**Created:** 2026-08-20<br>
**Last revised:** 2026-08-22<br>
**Mode:** Fresh closed-book, complexity-calibrated full simulation<br>
**Difficulty target:** SAP-C02 professional-level multi-constraint reasoning<br>
**Time limit:** 180 minutes<br>
**Question types:** 48 single-response and 27 multiple-response questions<br>
**Submission status:** FROZEN; independently marked

Multiple-response questions receive credit only when every required option and
no additional option is selected.

This original practice examination follows the current SAP-C02 domain
weightings and uses multi-constraint scenarios, plausible partial-fit
distractors, balanced answer positions, and exact-match composition traps. It
contains no proprietary AWS assessment wording. It is not an AWS examination
and does not reproduce live exam questions. The answer key, domain labels,
rationales, transfer labels, and scoring hints are deliberately excluded from
this document.

## Attempt rules

1. Keep lessons, reviews, answer keys, documentation, search, and AI assistance
   closed until the submission is frozen.
2. Select one answer unless the question says **Choose TWO** or **Choose
   THREE**.
3. Record start and end times, interruptions, and uncertain question numbers.
4. Stop when the 180-minute timer expires and guess rather than leave an item
   blank.
5. Complete the answer template and change `Submission status` to `FROZEN`
   before requesting marking. Do not revise a frozen response.
6. For multiple-response items, perform one final requirement-to-selection
   check before submission if time remains.

---

## Questions

### 1

A company has an AWS Organizations hierarchy with a production OU and a
sandbox OU. A role in a production member account has an identity policy that
allows `s3:PutBucketPolicy`. An SCP attached to the production OU denies that
action unless the request is made by a named break-glass role. The role's
permissions boundary allows all Amazon S3 actions.

What happens when the role tries to change a bucket policy?

A. The request succeeds because the permissions boundary allows the action.

B. The request is denied because the SCP limits the maximum permissions
available to the role.

C. The request succeeds because resource policies take precedence over SCPs.

D. The request is denied only if the bucket contains objects encrypted with an
AWS KMS key.

### 2 - Choose TWO

A security team must centrally retain API activity and query configuration
state for every current and future account in an organization. Evidence must
be delivered to a dedicated log archive account, and the security account must
perform organization-wide configuration queries without assuming a role in
each member account.

Which TWO actions satisfy these requirements?

A. Create an organization trail that delivers logs to a protected Amazon S3
bucket in the log archive account.

B. Create an individual CloudTrail trail in each current account and rely on
new account owners to repeat the configuration.

C. Designate an AWS Config delegated administrator and create an organization
aggregator in the security account.

D. Use Amazon EventBridge cross-account event buses as the authoritative store
for historical configuration state.

E. Enable VPC Flow Logs in the management account and query them with Amazon
Athena.

### 3

A payment service runs in two Availability Zones behind an Application Load
Balancer. Its relational database must survive an Availability Zone failure
without manual promotion, preserve transactional consistency, and remain
compatible with PostgreSQL drivers. The company does not require cross-Region
writes.

Which solution has the LOWEST operational overhead?

A. Run PostgreSQL on two EC2 instances and replicate by using custom scripts.

B. Use an Amazon RDS for PostgreSQL read replica in a second Availability Zone
and update DNS manually after failure.

C. Store database files on Amazon EFS and mount them from PostgreSQL instances
in both Availability Zones.

D. Use an Amazon Aurora PostgreSQL-Compatible cluster with replicas in
multiple Availability Zones.

### 4

A public API accepts image-processing jobs. Clients require an immediate,
durable acceptance response even when processors are unavailable. Jobs may be
processed asynchronously, and sudden traffic bursts must not overload the
processing fleet.

Which architecture BEST meets these requirements?

A. Configure API Gateway to invoke a synchronous Lambda function that waits
for the image processing to finish.

B. Place jobs on an Amazon SQS queue from the API tier and process them with an
auto-scaling consumer fleet.

C. Send jobs to an Amazon SNS topic and depend on email subscriptions for
durability.

D. Write jobs to instance store on the API servers and use Auto Scaling to add
processors.

### 5 - Choose THREE

A research organization runs tightly coupled MPI simulations. The workload
requires low-latency, high-throughput node-to-node communication, a shared
POSIX file system for very large datasets, and maximum network performance
within one Availability Zone.

Which THREE components should a solutions architect select?

A. Cluster placement group.

B. Spread placement group.

C. NAT gateways in every subnet.

D. Elastic Fabric Adapter on supported EC2 instances.

E. General purpose Amazon EFS with bursting throughput as the mandatory
scratch layer.

F. Amazon FSx for Lustre.

### 6 - Choose TWO

An online game stores player events in an Amazon DynamoDB table. A small set of
tournament IDs receives extremely high write volume, creating hot partitions.
The application must retain the tournament-based access pattern and return all
events for a tournament.

Which TWO changes address the problem?

A. Replace the partition key with the event timestamp only.

B. Add a DynamoDB Accelerator cluster without changing the write pattern.

C. Append a calculated shard suffix to the tournament partition key for
writes.

D. Use strongly consistent reads to distribute write traffic.

E. Query all shard suffixes for a tournament and merge the results in the
application.

### 7

A company uses IAM Identity Center with permission sets provisioned to 80 AWS
accounts. Security requires a maximum one-hour session for a privileged
permission set. Administrators want the change applied consistently without
editing roles in every account.

What should they do?

A. Edit the generated IAM role directly in every member account.

B. Attach an SCP that denies all requests more than one hour after sign-in.

C. Change the session duration on the permission set and reprovision it to the
assigned accounts.

D. Configure a one-hour maximum session on the IAM user in the management
account.

### 8

An Auto Scaling group serves traffic through an Application Load Balancer.
Requests have similar processing cost, and the company wants capacity to track
load per healthy target without maintaining a custom metric.

Which target-tracking metric is MOST appropriate?

A. `ALBRequestCountPerTarget`.

B. Total `RequestCount` for the load balancer.

C. `NetworkIn` summed across the Auto Scaling group.

D. Number of healthy hosts as a step-scaling threshold.

### 9 - Choose TWO

A platform team provisions governed landing-zone resources through AWS Control
Tower. It wants to reject noncompliant CloudFormation resources before they are
created and prevent member accounts from disabling mandatory logging controls.

Which TWO control types should the team use?

A. Detective controls only for both requirements.

B. Proactive controls for the CloudFormation resource checks.

C. AWS Trusted Advisor checks to block account API calls.

D. Preventive controls for actions that must be disallowed in member accounts.

E. Amazon Inspector suppression rules for the CloudFormation checks.

### 10

A Linux application runs on EC2 instances across three Availability Zones and
requires a shared, concurrently mounted file system. Files must remain
available if an instance or an Availability Zone fails, and the team wants a
managed service with minimal administration.

Which storage service BEST meets the requirements?

A. EC2 instance store replicated by application scripts.

B. A single EBS volume attached to every instance across Availability Zones.

C. Amazon S3 mounted as a block device by using custom kernel drivers.

D. Amazon EFS with mount targets in the required Availability Zones.

### 11

A company must expose a private TCP service from a provider VPC to hundreds of
consumer VPCs with overlapping CIDR ranges. Consumers must connect through
private IP addresses and must not receive general network access to the
provider VPC.

Which front-end component is required for the conventional endpoint-service
pattern?

A. Application Load Balancer.

B. Network Load Balancer.

C. Gateway Load Balancer endpoint.

D. Transit Gateway peering attachment.

### 12

A company has enabled AWS Compute Optimizer across its organization. The
operations team wants to reduce EC2 cost without accepting recommendations
that might degrade performance.

What should the team do FIRST?

A. Automatically apply every downsizing recommendation to production.

B. Terminate every instance marked over-provisioned and replace it with
serverless compute.

C. Review utilization findings, performance risk, and recommendation options,
then test selected changes against workload requirements.

D. Treat Compute Optimizer recommendations as a contractual performance SLA.

### 13 - Choose TWO

A media company distributes paid objects from a private S3 bucket through
CloudFront. Users must not bypass CloudFront, and each customer should receive
time-limited access to authorized objects.

Which TWO controls should be used?

A. Configure CloudFront origin access control and restrict the bucket policy to
the distribution.

B. Make the S3 bucket public and use an S3 website endpoint as the origin.

C. Add an Internet Gateway to the S3 bucket's VPC.

D. Use security groups on the S3 bucket to allow CloudFront IP addresses.

E. Issue CloudFront signed URLs or signed cookies after application
authorization.

### 14

A networking account owns a Transit Gateway that must be used by VPCs in 60
member accounts in the same organization. The networking team wants member
accounts to create attachments without transferring ownership of the Transit
Gateway.

Which service should the team use?

A. AWS Resource Access Manager.

B. VPC peering with the management account.

C. AWS PrivateLink resource endpoints.

D. An organization-wide Internet Gateway.

### 15 - Choose THREE

A company stores analytics data in Amazon S3. Object sizes and access patterns
are unpredictable, some datasets become cold over time, and finance needs
organization-wide visibility into storage usage and optimization
opportunities. The company wants managed controls with minimal manual object
classification.

Which THREE actions are appropriate?

A. Move every object directly to S3 Glacier Deep Archive on creation.

B. Use S3 Intelligent-Tiering for datasets with unknown or changing access
patterns.

C. Configure lifecycle transitions for datasets with known age-based access
patterns.

D. Store a second full copy of every dataset on EBS for cost comparison.

E. Use S3 Storage Lens to analyze usage and activity across accounts.

F. Use CloudFront cache statistics as the authoritative inventory of all S3
objects.

### 16

A global application uses active-passive regional stacks. Operators require a
highly available control plane that can prevent traffic from entering an
unhealthy Region during a recovery event, including when the affected Region's
service APIs are impaired.

Which service is designed for this requirement?

A. Route 53 geolocation routing alone.

B. Elastic Load Balancing cross-zone load balancing.

C. AWS Global Accelerator endpoint weights only.

D. Amazon Route 53 Application Recovery Controller routing controls.

### 17 - Choose TWO

A company deploys an Amazon ECS service behind an Application Load Balancer.
It wants CodeDeploy blue/green deployments with test traffic sent to the new
task set before production traffic shifts.

Which TWO resources are required for this traffic-routing design?

A. Two target groups, one for the original task set and one for the replacement
task set.

B. A Gateway Load Balancer with appliance mode enabled.

C. A production listener and, when test traffic is required, a test listener
whose forwarding is managed by CodeDeploy.

D. One target group shared permanently by both task sets with no listener
changes.

E. An S3 Multi-Region Access Point in front of the service.

### 18

A security team wants to manage Amazon GuardDuty for all organization accounts
from a dedicated security account and automatically include new accounts.

Which design has the LOWEST operational overhead?

A. Invite every account into a separate GuardDuty administrator relationship
outside AWS Organizations.

B. Designate the security account as the GuardDuty delegated administrator and
configure organization auto-enable settings.

C. Run GuardDuty only in the management account and aggregate VPC Flow Logs
manually.

D. Grant the security account `AdministratorAccess` in every member account.

### 19

An operations team receives hundreds of alarms during a downstream service
failure. It wants to notify responders only when a customer-facing symptom and
its supporting infrastructure condition occur together, while retaining the
underlying alarms for diagnosis.

Which CloudWatch feature should the team use?

A. Metric Streams.

B. Contributor Insights.

C. Anomaly detection bands only.

D. Composite alarms.

### 20

A company is moving an existing Apache Kafka event platform to AWS. Producers
and consumers already use Kafka APIs, the team requires broker-level
configuration control and topic semantics, and application rewrites must be
minimized. The company wants AWS to manage broker provisioning and replacement.

Which service is the BEST fit?

A. Amazon Managed Streaming for Apache Kafka.

B. Amazon Kinesis Data Streams with every application rewritten for Kinesis
APIs.

C. One Amazon SQS FIFO queue shared by all consumer groups.

D. Amazon MQ for RabbitMQ with the Kafka protocol enabled.

### 21 - Choose TWO

A company wants a central observability account to view metrics, logs, and
traces from workload accounts without copying every metric into a new custom
namespace. The design must use CloudWatch cross-account observability.

Which TWO configurations are required?

A. Create one CloudTrail trail for every metric namespace.

B. Peer every workload VPC with the observability account.

C. Create an observability access manager sink in the monitoring account.

D. Share the monitoring account's root credentials with workload teams.

E. Create links from source accounts to the sink for the required telemetry
types.

### 22

A central platform team must deploy a baseline IAM role to every current and
future organization account and Region selected by policy. Deployments should
update automatically when accounts enter the target OU.

Which solution BEST meets the requirement?

A. Use self-managed StackSets and manually maintain account lists.

B. Use CloudFormation StackSets with service-managed permissions and automatic
deployment to the target OU.

C. Store the role template in S3 and ask account owners to deploy it.

D. Use an AMI shared from the management account.

### 23

A serverless order workflow spans API Gateway, Lambda, and downstream AWS
services. Operators need to trace an individual request across service
boundaries and identify latency contributed by each segment.

Which service should they use?

A. AWS X-Ray distributed tracing.

B. AWS Artifact.

C. AWS Cost Explorer resource-level granularity.

D. Amazon Inspector network reachability findings.

### 24 - Choose TWO

A global shopping-cart service must accept writes in two AWS Regions, keep the
same logical table synchronized, and route users to healthy regional API
endpoints. The application can tolerate the supported consistency model and
conflict behavior of the selected database configuration.

Which TWO components form the most appropriate managed design?

A. One regional DynamoDB table restored nightly into the second Region.

B. A DynamoDB global table spanning the two Regions.

C. An EBS Multi-Attach volume shared between Regions.

D. Route 53 or Global Accelerator health-aware routing to the regional API
stacks.

E. One NAT gateway advertised to both Regions through VPC peering.

### 25

A company wants to enforce centrally managed AWS WAF rules across Application
Load Balancers in organization accounts and automatically apply the policy to
new in-scope resources.

Which service should it use?

A. Amazon Detective.

B. IAM Access Analyzer.

C. AWS Firewall Manager.

D. AWS Network Manager.

### 26 - Choose THREE

A regulated organization is designing an immutable, centralized audit trail
for all organization accounts. It must detect alteration of delivered log
files, prevent workload administrators from deleting retained evidence, and
automatically include new accounts.

Which THREE controls should be included?

A. Create an organization CloudTrail trail that covers all Regions.

B. Store the only copy of the logs in CloudWatch Logs in each workload account.

C. Grant every member-account administrator permission to delete objects from
the archive bucket.

D. Disable S3 Versioning so an object has only one version.

E. Enable CloudTrail log file integrity validation.

F. Deliver the trail to a tightly controlled S3 bucket in a separate log
archive account with retention protections appropriate to the requirement.

### 27

A SaaS application has unpredictable database demand. It uses PostgreSQL,
requires connection compatibility with existing drivers, and must scale
database capacity rapidly while retaining a highly available managed
relational architecture. Demand does not fall to zero for long periods.

Which option BEST meets these requirements?

A. DynamoDB on-demand capacity with no application changes.

B. PostgreSQL on one large EC2 instance with scheduled resizing.

C. Amazon Redshift Serverless for transactional requests.

D. Aurora PostgreSQL-Compatible with Aurora Serverless v2 capacity scaling.

### 28 - Choose TWO

A security team must apply the same AWS WAF policy to internet-facing
Application Load Balancers across multiple organization accounts. It wants
central policy administration and automatic inclusion of newly created
matching resources.

Which TWO steps are required?

A. Deploy a separate manually maintained web ACL from every application
pipeline.

B. Configure AWS Config in the required accounts and Regions as required by
Firewall Manager.

C. Create an AWS Firewall Manager policy from the organization's delegated
administrator account and scope it to the intended resources.

D. Replace all Application Load Balancers with Network Load Balancers.

E. Share one security group from the management account through AWS RAM.

### 29

A company operates Security Hub in many Regions and accounts. The security
operations team wants findings from linked Regions available through one home
Region while retaining regional collection.

Which feature should the team configure?

A. Security Hub cross-Region aggregation with a designated aggregation Region.

B. An S3 Multi-Region Access Point for Security Hub findings.

C. Route 53 latency routing between Security Hub endpoints.

D. One CloudWatch Logs subscription filter in the management account.

### 30

A data platform needs multiple independent consumers to process the same event
history at different rates. Consumers may replay retained records, and ordering
is required for events with the same customer ID.

Which service should the platform use?

A. An Amazon SQS FIFO queue with all consumers competing for messages.

B. Amazon SNS with email subscriptions.

C. Amazon Kinesis Data Streams with the customer ID as the partition key.

D. An EventBridge schedule that invokes consumers once per minute.

### 31

A compliance team must be alerted when deletion is scheduled for a customer
managed KMS key. The response should be event driven and should not poll the
KMS API.

Which solution is MOST appropriate?

A. Use an S3 lifecycle notification for the key metadata.

B. Use VPC Flow Logs to identify the KMS endpoint request.

C. Schedule a daily Trusted Advisor report.

D. Match the relevant CloudTrail management event with Amazon EventBridge and
invoke a notification or remediation target.

### 32 - Choose TWO

A software vendor must expose a private service to customer VPCs in many
accounts. Customer CIDR ranges overlap, customers must not obtain transitive
access to the provider VPC, and connections must use private IP addresses.

Which TWO components should the provider use for the conventional
service-exposure design?

A. Create a VPC endpoint service backed by a Network Load Balancer.

B. Attach every customer VPC to one shared Transit Gateway route table.

C. Establish full-mesh VPC peering with every customer.

D. Publish the service through an internet-facing Application Load Balancer.

E. Have customers create interface VPC endpoints for the endpoint service.

### 33

A security architect wants to identify resource policies that grant access to
principals outside the company's organization. The trust boundary should be
the organization, and findings should update as policies change.

Which service meets the requirement?

A. Amazon Macie sensitive-data discovery.

B. IAM Access Analyzer external access analysis with an organization trust
zone.

C. AWS Shield Advanced.

D. Amazon Inspector EC2 network reachability.

### 34

A company is beginning a migration of several hundred on-premises servers. It
needs utilization data, dependency discovery, and a business case before wave
planning. No server migration should begin yet.

Which service combination is MOST appropriate for this discovery phase?

A. AWS Application Migration Service and Route 53 ARC.

B. AWS DataSync and Amazon S3 Storage Lens.

C. AWS Application Discovery Service and Migration Evaluator.

D. AWS DMS and AWS Schema Conversion Tool.

### 35

An Amazon RDS database has intermittent query latency. The team needs to
identify database load by waits, SQL statements, hosts, and users before
changing instance size.

Which capability should the team use?

A. Amazon RDS database performance monitoring capabilities that expose DB load
and wait dimensions.

B. AWS CloudTrail data events for every database row read.

C. VPC Reachability Analyzer between the application and each table.

D. AWS Cost and Usage Report grouped by SQL statement.

### 36 - Choose TWO

A company has one Direct Connect connection. On-premises systems must reach
private VPC addresses through a virtual private gateway and must also reach
AWS public service endpoints over the dedicated connection. The company does
not need general internet transit from AWS.

Which TWO virtual-interface mappings are correct?

A. A transit VIF attaches directly to a virtual private gateway for one VPC.

B. A private VIF provides the path to private VPC addresses through a virtual
private gateway, directly or through an appropriate Direct Connect gateway
association.

C. A public VIF provides transitive private routing between arbitrary VPCs.

D. A private VIF is required for all AWS public service endpoints.

E. A public VIF acts as a general-purpose internet service for non-AWS
destinations.

F. A public VIF provides access to AWS public service endpoints using public IP
addressing without becoming general internet transit.

### 37 - Choose THREE

A company has 70 VPCs across several accounts and two Regions. On-premises
systems require private connectivity to the VPCs and bidirectional resolution
between on-premises domains and Route 53 private hosted zones. The design must
avoid a full mesh and must not add public internet access.

Which THREE components form the appropriate architecture?

A. Transit Gateways shared with spoke accounts and configured with the required
route domains.

B. An Internet Gateway in every spoke VPC to support DNS forwarding.

C. A Direct Connect gateway with transit VIF connectivity to the Transit
Gateways, or an appropriately designed VPN path.

D. Route 53 Resolver inbound and outbound endpoints with forwarding rules.

E. Full-mesh VPC peering between all VPCs.

F. Public hosted zones for all internal names.

### 38

A company must migrate an on-premises Oracle database to Amazon RDS for Oracle
with minimal downtime. The schema remains compatible, and changes must be
replicated until cutover.

Which migration approach BEST meets the requirements?

A. Use AWS Schema Conversion Tool to convert Oracle objects to DynamoDB and
perform a one-time export.

B. Copy the database files to EFS and mount them from RDS.

C. Use AWS Application Migration Service to replicate the database server into
an RDS instance.

D. Use AWS DMS for full load plus ongoing change data capture to RDS for
Oracle.

### 39

A global application stores objects in S3 buckets in several Regions. Clients
should use one global endpoint that routes requests to the closest active
bucket while the company applies centralized traffic controls and failover
settings.

Which feature should be used?

A. S3 Transfer Acceleration on one bucket only.

B. Amazon S3 Multi-Region Access Points.

C. One gateway endpoint shared between Regions.

D. Amazon EFS replication.

### 40 - Choose TWO

A company has billions of existing S3 objects encrypted with an older key. It
must apply a new encryption configuration at scale and needs an authoritative
list of objects for the operation without writing a custom crawler.

Which TWO services or features should it use?

A. CloudFront invalidations to rewrite every object.

B. EBS snapshots as the object manifest.

C. S3 Inventory to produce the object list.

D. S3 Select to update encryption metadata in place.

E. S3 Batch Operations to perform a copy operation that applies the required
encryption.

### 41

A company applies an Organizations tag policy that standardizes the spelling
and allowed values of a `CostCenter` tag. An engineer assumes the tag policy
will deny every API request that creates an untagged resource.

Which statement is correct?

A. Tag policies standardize and report tag compliance for supported resources;
use an appropriate enforcement control when API-level denial is required.

B. Tag policies grant IAM permissions to create only tagged resources.

C. Tag policies replace all SCPs and permissions boundaries.

D. Tag policies automatically stop every running resource with a missing tag.

### 42

A company wants to rehost hundreds of supported VMware virtual machines on EC2
with continuous block-level replication and short cutover windows. It does not
want to redesign the applications during this wave.

Which service should it use?

A. AWS DMS.

B. AWS DataSync.

C. AWS Application Migration Service.

D. AWS App Runner.

### 43

A multiplayer game uses regional TCP endpoints. Players need static anycast IP
addresses, rapid health-based regional failover, and improved routing over the
AWS global network. Caching is not required.

Which service BEST meets the requirement?

A. Amazon CloudFront with an S3 origin.

B. Route 53 private hosted zones.

C. AWS Direct Connect SiteLink.

D. AWS Global Accelerator.

### 44 - Choose TWO

A company deploys an ECS rolling update and wants deployments to fail quickly
and return to the last working task set when replacement tasks cannot become
healthy. It also wants application alarms to participate in rollback.

Which TWO configurations should it use?

A. Enable the ECS deployment circuit breaker with rollback.

B. Configure deployment alarms and rollback behavior for the service.

C. Disable all load balancer health checks during deployment.

D. Set minimum healthy percent to zero and ignore failed tasks.

E. Replace the service with an S3 static website.

### 45

An application role in Account A must decrypt data with a customer managed KMS
key in Account B. The role's identity policy allows `kms:Decrypt` on the key,
but requests are denied.

What additional authorization is required?

A. An SCP in Account A that grants `kms:Decrypt`.

B. A permissions boundary that contains no KMS actions.

C. The KMS key policy in Account B must allow the external account or role to
use the key, in addition to the caller-side permission.

D. A security group rule on the KMS key.

### 46

A company is migrating a Microsoft SQL Server application to Amazon Aurora
PostgreSQL-Compatible Edition. The schema and stored procedures require
conversion, and production changes must continue replicating until cutover.

Which approach is MOST appropriate?

A. Use AWS Application Migration Service for both schema conversion and
row-level change replication.

B. Use AWS Schema Conversion Tool for schema conversion and AWS DMS for full
load plus change data capture.

C. Export the SQL Server disks to an AMI and attach them to Aurora.

D. Use Amazon Data Firehose to translate stored procedures.

### 47

A manufacturer sends telemetry from millions of intermittently connected
devices. The service must authenticate devices individually, ingest MQTT
messages at scale, and route selected messages to AWS services by using managed
rules.

Which service is the BEST fit?

A. AWS IoT Core.

B. Amazon MQ for ActiveMQ with one queue per device.

C. AWS Transfer Family.

D. Amazon AppFlow.

### 48 - Choose TWO

A company is using Amazon VPC Lattice for service-to-service communication
across VPCs and accounts. It wants application-level authorization and central
visibility into requests without exposing broad network routes.

Which TWO capabilities should it use?

A. Transit Gateway route-table propagation as the only authorization control.

B. Internet Gateways on every participating VPC.

C. S3 bucket ACLs attached to each service.

D. VPC Lattice auth policies for request authorization.

E. VPC Lattice access logs and service-network monitoring for visibility.

### 49 - Choose THREE

A regulated organization wants centrally governed backups across member
accounts. Backup policies should apply by OU, backup administration should be
delegated to a dedicated account, and critical recovery points must be
protected from deletion even if a workload account is compromised.

Which THREE actions should be included?

A. Store every backup only in the source account's default vault.

B. Use AWS Organizations backup policies to apply plans to target accounts and
OUs.

C. Grant workload administrators permission to shorten vault retention at any
time.

D. Register a delegated administrator for AWS Backup.

E. Replace backups with CloudWatch Logs exports.

F. Copy protected recovery points to an appropriately locked or isolated vault
under separate administrative control.

### 50

A logistics company exchanges signed business documents with partners by using
the AS2 protocol. It wants a managed AWS service that integrates partner
profiles, certificates, agreements, and Amazon S3 storage.

Which service should it use?

A. Amazon MQ.

B. Amazon AppFlow.

C. AWS Transfer Family AS2.

D. AWS DataSync.

### 51

A genomics company runs containerized batch jobs that require a licensed
kernel driver installed in a custom Amazon Machine Image. Jobs are fault
tolerant, and the company wants to reduce compute cost by using diversified
Spot capacity where possible.

Which compute environment should the company use?

A. An AWS Batch managed EC2 compute environment configured with an approved
launch template or custom AMI and suitable Spot allocation settings.

B. An AWS Batch Fargate-only compute environment that loads a custom kernel at
runtime.

C. Lambda functions with 24-hour execution timeouts.

D. An EKS Fargate profile with privileged host kernel modules.

### 52 - Choose TWO

An event-processing team wants to retain events that arrive on an EventBridge
event bus and replay selected historical events after deploying a corrected
consumer. It must also capture events that cannot be delivered to a target
after retries are exhausted.

Which TWO configurations meet the requirements?

A. Enable S3 Versioning on the event bus.

B. Create an EventBridge archive with an appropriate event pattern and
retention period.

C. Use an Auto Scaling lifecycle hook as the replay mechanism.

D. Configure an Amazon SQS dead-letter queue for failed target deliveries.

E. Use Route 53 query logging for undelivered events.

### 53

A company has an organization trail whose logs are stored in a separate log
archive account. Workload administrators must be unable to stop or delete the
organization trail, while an audited central break-glass process retains the
ability to manage it.

Which control MOST directly enforces this requirement in workload accounts?

A. A security group that blocks the CloudTrail public endpoint.

B. A tag policy that marks the trail as mandatory.

C. An IAM policy attached only to newly created users.

D. An SCP that denies the relevant CloudTrail modification actions except for
the approved central principal or process.

### 54

A company must move an initial 120 TB file archive from an on-premises site to
Amazon S3. The available 80 Mbps internet link cannot complete the transfer in
the required window, and the archive is static during export.

Which service is the MOST cost-effective fit for the initial bulk transfer?

A. Amazon Kinesis Data Streams.

B. An AWS Snowball Edge device.

C. AWS Storage Gateway volume snapshots sent continuously over the existing
link.

D. An Amazon SQS FIFO queue.

### 55

A Lambda-based API experiences database connection storms when concurrency
rises. It connects to Amazon RDS for MySQL, and the company wants managed
connection pooling and improved resilience to brief database failovers.

Which service should be introduced?

A. DynamoDB Accelerator.

B. AWS Global Accelerator.

C. Amazon RDS Proxy.

D. Amazon ElastiCache for Memcached as a SQL protocol endpoint.

### 56 - Choose TWO

A company wants to assess whether a multi-Region application is likely to meet
its resilience objectives and then safely validate selected failure
assumptions in a controlled experiment. The tools must complement, not replace,
documented recovery procedures and restore tests.

Which TWO services should the company use?

A. AWS Resilience Hub to assess the application against defined resilience
objectives and surface recommendations.

B. AWS Artifact to inject faults into production resources.

C. Amazon Inspector to perform database point-in-time restores.

D. AWS Cost Explorer to simulate Availability Zone failures.

E. AWS Fault Injection Service to run controlled fault experiments with safety
controls.

### 57

A central cloud team maintains approved products for networking and security
baselines. Business-unit accounts should launch those products while the
central team retains product governance and version control.

Which solution BEST meets the requirement?

A. Share AMI passwords through Secrets Manager.

B. Create AWS Service Catalog portfolios and share them with the required
organization accounts or OUs.

C. Give every business unit administrator access to edit the central
CloudFormation templates directly.

D. Publish the products through a public S3 website.

### 58

A company wants to accelerate assessment and transformation of a large VMware
estate by using an AWS managed, AI-assisted modernization capability. It needs
portfolio analysis and migration-wave recommendations before selecting the
final target patterns.

Which service is designed for this use case?

A. AWS AppConfig.

B. AWS Control Tower.

C. Amazon DevOps Guru.

D. AWS Transform.

### 59

A ransomware-resilience design requires backup copies in a vault that is
logically isolated, has separate access controls, and can support controlled
recovery workflows when the production environment is compromised.

Which AWS Backup capability is the BEST fit?

A. A logically air-gapped vault.

B. The default backup vault in the workload account with broad administrator
access.

C. An EBS fast snapshot restore setting without a backup plan.

D. A CloudWatch Logs archive.

### 60 - Choose TWO

An organization wants evidence that backups are recoverable and that backup
configurations comply with defined controls. It wants managed, recurring
restore validation and auditable compliance reporting.

Which TWO capabilities should it use?

A. Route 53 health checks against backup vault names.

B. S3 Transfer Acceleration for all recovery points.

C. AWS Backup restore testing plans.

D. EC2 Auto Scaling scheduled actions as proof of database recovery.

E. CloudFront origin failover for backup compliance.

F. AWS Backup Audit Manager frameworks and reports.

### 61

A central operations account uses CloudWatch cross-account observability. A
workload account already has an observability link to the monitoring account's
sink. The workload team adds a new metric namespace within the telemetry types
allowed by the link.

What is the intended operational model?

A. The monitoring account must assume the workload account root user for every
query.

B. The metric must be copied manually into a duplicate custom namespace in the
monitoring account.

C. Authorized users in the monitoring account can observe linked telemetry
through the cross-account observability relationship, subject to its scope.

D. The VPCs must be fully peered before CloudWatch can display the metric.

### 62 - Choose THREE

A company has completed discovery for 400 servers and is preparing migration
waves. It wants centralized progress tracking, automated rehosting for suitable
servers, and explicit dependency-aware wave grouping. Database conversion will
be handled separately.

Which THREE actions are appropriate?

A. Group applications and servers into migration waves based on dependency and
business information from discovery.

B. Convert every application to Lambda before any migration begins.

C. Track application and wave status centrally with AWS Migration Hub.

D. Use AWS DMS to replicate operating-system boot volumes.

E. Use AWS Application Migration Service for servers selected for rehosting to
EC2.

F. Replace dependency data with random wave assignment to maximize parallelism.

### 63

A company has a containerized web application with standard HTTP traffic. It
wants a managed deployment target that automatically builds and deploys from a
source or container image, scales the web service, and avoids managing
clusters. The application does not require Kubernetes APIs.

Which service is the BEST fit?

A. Amazon EKS with self-managed nodes.

B. AWS App Runner.

C. AWS Outposts racks.

D. Amazon EMR on EC2.

### 64 - Choose TWO

A company runs two Lambda functions in the same account. A bursty reporting
function must not consume all regional concurrency and starve a critical API
function. The critical function also needs predictable low start latency for a
known baseline load.

Which TWO configurations address these requirements?

A. Configure reserved concurrency for the functions to protect the required
concurrency allocation.

B. Increase the reporting function timeout to 15 minutes.

C. Put both functions in the same security group.

D. Configure provisioned concurrency for the critical function's expected
baseline.

E. Use an S3 lifecycle rule to pre-warm execution environments.

### 65

A security analyst is investigating a suspected compromise. The company wants
a managed service that analyzes and visualizes relationships among findings,
users, roles, IP addresses, and affected resources to support investigation.

Which service should the analyst use?

A. AWS Service Catalog.

B. Amazon Forecast.

C. AWS Application Discovery Service.

D. Amazon Detective.

### 66

A company is migrating an on-premises SMB file server to Amazon FSx for Windows
File Server. It must preserve Windows file metadata and ACL information while
transferring data over an available private network path.

Which service should it use?

A. AWS DataSync with SMB and FSx for Windows File Server locations.

B. AWS Database Migration Service.

C. Amazon Kinesis Data Firehose.

D. AWS Application Migration Service.

### 67

A company operates a commercial application that uses standard JMS with
ActiveMQ. It wants to move the brokers to AWS with minimal application change
and managed high availability.

Which service is the MOST appropriate target?

A. Amazon SQS standard queues.

B. Amazon Kinesis Data Streams.

C. Amazon MQ for ActiveMQ.

D. Amazon EventBridge Pipes.

### 68 - Choose TWO

A finance team wants early warning of unusual AWS spending and an automated
action when forecasted monthly spend exceeds an approved threshold. It must
retain human notification and controlled automation rather than terminate
arbitrary resources immediately.

Which TWO capabilities should it use?

A. AWS Artifact reports to forecast daily spend.

B. AWS Cost Anomaly Detection monitors and subscriptions for unusual-spend
alerts.

C. VPC Flow Logs as the billing forecast source.

D. S3 Storage Lens free metrics as the organization-wide cost authority.

E. AWS Budgets with forecasted thresholds, notifications, and an appropriately
governed Budgets action.

### 69

A compliance account must query AWS Config resource state from organization
accounts and Regions without building custom cross-account collectors. New
accounts should be included through organization integration.

Which design is MOST appropriate?

A. Use an AWS Config organization aggregator in a delegated administrator
account.

B. Copy CloudTrail event history manually from every Region once per month.

C. Create one public S3 bucket for member accounts to upload screenshots.

D. Use a Transit Gateway route table as the configuration database.

### 70

A company must move a large VMware estate rapidly while retaining the current
VMware operational model, tools, and network constructs during a data-center
exit. Refactoring is explicitly deferred.

Which target is the BEST strategic fit?

A. Rewrite every workload for Lambda and DynamoDB.

B. VMware Cloud on AWS.

C. Amazon Redshift Serverless.

D. AWS Transfer Family.

### 71 - Choose TWO

A company must migrate a 90 TB on-premises PostgreSQL analytical store to
Amazon Redshift within 21 days. The available 40 Mbps link cannot carry the
initial dataset in time, but daily source changes must continue until cutover.
The company wants to avoid introducing an unrelated database engine.

Which TWO actions form the most appropriate migration approach?

A. Provision an Oracle database on EC2 and replicate PostgreSQL into Oracle
before migrating to Redshift.

B. Wait for a new Direct Connect circuit and transfer the entire dataset only
after it is provisioned, regardless of the deadline.

C. Use an offline AWS data-transfer device for the bulk export, ingest the data
into Amazon S3, and load the historical dataset into Redshift.

D. Use AWS DMS change data capture over the available connection for ongoing
changes until cutover.

E. Use Amazon MQ to convert relational rows into Redshift tables.

### 72

A portfolio assessment identifies an unsupported application that delivers no
business value, has no downstream consumers, and is expensive to operate. The
business owner approves permanent shutdown instead of migration.

Which migration strategy applies?

A. Rehost.

B. Relocate.

C. Retain.

D. Retire.

### 73

A finance team needs detailed AWS cost and usage data delivered to a central S3
bucket for SQL analysis and allocation by activated cost-allocation tags. The
dataset should provide line-item detail beyond the standard dashboard.

Which service or feature should the team configure?

A. Amazon Inspector findings export.

B. VPC Flow Logs with Athena.

C. AWS Cost and Usage Reports delivered to Amazon S3.

D. AWS Artifact agreements.

### 74 - Choose TWO

A company is migrating a 35 TB on-premises SQL Server data warehouse to Amazon
Redshift. The schema requires conversion, the network can support ongoing
changes but not a timely full transfer, and the cutover window permits only a
short write pause.

Which TWO actions BEST satisfy the requirements without adding an unrelated
intermediate database engine?

A. Use AWS Schema Conversion Tool to assess and convert the warehouse schema
for Amazon Redshift.

B. Deploy an Oracle RAC cluster on EC2 and synchronize SQL Server into Oracle
before starting the Redshift migration.

C. Use AWS Application Migration Service to convert SQL Server tables into
Redshift distribution styles.

D. Perform the entire full load over the constrained connection and ignore the
cutover deadline.

E. Seed the bulk data through an appropriate offline-to-S3 transfer path, then
use AWS DMS for ongoing changes and cutover coordination where supported.

### 75 - Choose THREE

A company requires a warm-standby disaster-recovery design for a stateful
application. The secondary Region must run a scaled-down but functional stack,
receive continuous state replication, and support a tested, controlled traffic
transition and later failback.

Which THREE elements are required for a complete design?

A. Keep only infrastructure templates in the secondary Region and create all
runtime resources after the disaster.

B. Operate a scaled-down application and data stack in the secondary Region.

C. Depend on CloudFront origin failover alone for every read and write method.

D. Continuously replicate the required application state and data to the
secondary Region.

E. Delete the primary Region immediately after the first successful health
check failure.

F. Define and test health evaluation, traffic cutover, scale-up, recovery, and
failback procedures.

---

## Submission template

Complete this section only during the attempt. Freeze it before requesting
marking.

```text
Attempt date: Aug 22, 2026
Attempt number: 1
Start: 10:43
End: 12:29
Elapsed: 106 minutes wall clock; 101 active minutes after 5-minute pause
Interruption note: Stopped at 11:29, Resumed at: 11:34
Uncertain: 9,11,16,19,20,24,25,26,28,32,37,41,47,57,60,63

1:B
2:AC
3:D
4:B
5:ADF
6:CE
7:C
8:A
9:BD
10:D
11:C
12:C
13:AE
14:A
15:BCE
16:D
17:AC
18:B
19:D
20:A
21:CE
22:B
23:A
24:BD
25:C
26:AEF
27:D
28:BC
29:A
30:C
31:D
32:AE
33:B
34:C
35:A
36:BF
37:ACD
38:D
39:B
40:CD
41:A
42:C
43:D
44:AB
45:C
46:B
47:A
48:DE
49:BDF
50:C
51:A
52:BD
53:D
54:B
55:C
56:AE
57:B
58:D
59:A
60:CF
61:C
62:ACE
63:B
64:AD
65:D
66:A
67:C
68:BE
69:A
70:B
71:CD
72:D
73:C
74:AE
75:BDF

Submission status: FROZEN
Submission date: Aug 22, 2026
```
