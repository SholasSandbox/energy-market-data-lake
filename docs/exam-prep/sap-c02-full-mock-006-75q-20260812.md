# AWS Certified Solutions Architect - Professional (SAP-C02)

## Full Mock 006 - 75 Questions

<!-- markdownlint-disable MD013 MD060 -->

**Created:** 2026-08-12
**Mode:** Fresh closed-book, complexity-calibrated full simulation
**Time limit:** 180 minutes
**Question types:** 48 single-response and 27 multiple-response questions
**Submission status:** FROZEN

Multiple-response questions receive credit only when every required option and
no additional option is selected.

This is an original practice examination aligned to the current SAP-C02 domain
weightings and response types. It also includes limited, unlabeled coverage of
current emerging-topic cues. Before the attempt began, the question structure
was recalibrated against aggregate characteristics of the locally held AWS
Skill Builder assessment: scenarios now combine multiple requirements, and
distractors are designed as plausible partial fits rather than short service-
recognition cues. No proprietary question wording was copied. It is not an AWS
examination and does not reproduce live exam questions. The answer key, domain
labels, rationale, and scoring hints are deliberately excluded from this
document.

## Attempt Rules

1. Keep lessons, reviews, answer keys, documentation, search, and AI assistance
   closed until the submission is frozen.
2. Select one answer unless the question says **Choose TWO** or **Choose
   THREE**.
3. Record start and end times and any uncertain question numbers.
4. Complete the answer template at the end of this document.
5. Change `Submission status` to `FROZEN` before requesting marking. Do not
   revise a frozen response.
6. Stop when the 180-minute timer expires and guess rather than leave a
   question blank.

---

## Questions

### 1

An enterprise has 60 application VPCs split across three AWS Regions. Each
Region already uses a Transit Gateway with separate production, development,
and shared-services route tables. A newly acquired business must communicate
privately with shared services in every Region. The network team requires
transitive connectivity, centralized route control, and no public-internet
path. It will not operate third-party routing appliances, and it wants to avoid
the connection count and routing administration of a VPC-peering mesh.

Which solution best meets these requirements?

A. Create inter-Region Transit Gateway peering attachments, then add the
required static peering routes and preserve segmentation through the regional
Transit Gateway route-table associations and propagations.

B. Attach an internet gateway to every workload VPC, advertise public prefixes
between Regions, and use security groups to preserve the existing segmentation.

C. Create S3 gateway endpoints in every VPC, associate them with the Transit
Gateway route tables, and use the endpoint policies to route inter-Region
application traffic.

D. Build an inter-Region VPC-peering mesh for every VPC pair and rely on route
propagation through the peered VPCs to provide transitive shared-service access.

### 2 - Choose TWO

A public order API receives unpredictable bursts of requests. The API must
respond within two seconds after an order has been durably accepted, but
payment and fulfillment processing can take several minutes. Mobile clients
retry when they do not receive a response, and a repeated request must not
create another charge. Operations wants independent worker scaling, failed-
message isolation, and no continuously running broker or web fleet. Existing
consumers can process messages from an AWS queue.

Which TWO actions best meet these requirements?

A. Invoke one Lambda function synchronously from API Gateway, increase its
timeout to 15 minutes, and return success before the payment provider confirms
whether the operation was accepted.

B. Integrate API Gateway with SQS using an IAM role and request mapping, and
return an accepted response only after SQS confirms that it durably stored the
message.

C. Write the request body only to API Gateway access logs, subscribe a Lambda
function to the logs, and treat successful log delivery as the order system of
record.

D. Require a stable client idempotency key and use a conditional write in the
worker's durable state store before applying the external payment side effect.

E. Set the SQS visibility timeout to zero and let multiple workers race to
process every order so that at least one invocation completes quickly.

### 3

An Application Load Balancer distributes requests to an EC2 Auto Scaling group.
The application spends most of its time waiting on downstream services, so CPU
utilization remains near 30% even when response latency rises sharply. Load can
increase at any time and has no reliable daily schedule. Testing shows that a
target stays within its latency objective while it receives no more than a
known number of requests per minute. The team wants a managed dynamic policy
that maintains that relationship as targets enter and leave service.

Which scaling approach most directly tracks the load?

A. Configure target tracking using `ALBRequestCountPerTarget`, selecting a
target value derived from the tested per-target request rate that maintains the
latency objective.

B. Configure scheduled scaling for the historical peak hour and keep CPU target
tracking as the only policy outside that period.

C. Publish average disk queue length from one instance and use it as a step-
scaling metric for the entire Auto Scaling group.

D. Alarm on the load balancer's response time and require operators to adjust
desired capacity manually after each unpredictable traffic increase begins.

### 4 - Choose THREE

An AWS organization contains 140 workload accounts in six Regions. Security
operations works from a dedicated account and must centrally administer threat
detection, aggregate security-posture findings, and query resource-
configuration compliance. New accounts must be enrolled with minimal manual
work. The management account must not be used for daily security operations,
and a single-Region findings view must not hide relevant findings from the
other enabled Regions.

Which THREE actions best meet these requirements?

A. Register the security operations account as the delegated administrator for
each supported security service through AWS Organizations, rather than running
daily administration from the management account.

B. Create root-user credentials for security engineers in every member account
and use a shared password vault to switch accounts during investigations.

C. Configure organization-aware GuardDuty and Security Hub enrollment, and
configure the required home, linked, aggregation, or central-configuration
Regions for the organization's enabled-Region model.

D. Create an IAM group in the management account and use its membership list as
the authoritative inventory of resource configurations in every member
account.

E. Create an organization AWS Config aggregator in the security or audit
account, with the required authorization and recording coverage in source
accounts and Regions.

F. Export screenshots from the GuardDuty, Security Hub, and AWS Config consoles
in every account once per month and store them in the security account.

### 5

A new PostgreSQL-compatible order platform serves users from two continents.
Both Regions require low-latency local reads, but normal writes can use one
primary Region. After a complete regional failure, the business requires a
recovery point measured in seconds and managed promotion of a database in the
other Region. The database team wants storage-level cross-Region replication
and does not want to build logical replication, conflict handling, or a custom
database-promotion system.

Which database architecture is the best fit?

A. Run one RDS PostgreSQL Multi-AZ deployment in the primary Region, copy hourly
snapshots cross-Region, and restore the latest snapshot after declaring a
regional disaster.

B. Use Aurora PostgreSQL Global Database with a secondary cluster in the
recovery Region, local reader endpoints, and tested managed switchover or
failover procedures.

C. Add an RDS PostgreSQL read replica in another Availability Zone of the
primary Region and use Route 53 latency routing to present it as a cross-Region
database.

D. Deploy a DAX cluster in each Region in front of the PostgreSQL database and
use DAX replication to create the recovery-region database copy.

### 6

An application tier runs in private subnets in three Availability Zones. Every
private subnet uses one NAT gateway in the first zone for software-repository
and third-party API traffic. Cost reports show significant cross-AZ data
processing, and a recent zonal impairment removed outbound access from all
three zones even though application instances in two zones remained healthy.
The architecture team wants zonal fault isolation and lower cross-AZ cost while
retaining private subnets and managed network address translation.

Which improvement best addresses both problems?

A. Attach a second internet gateway to the VPC in another Availability Zone and
route the two remote private subnets directly to that gateway.

B. Replace the NAT gateway with a cross-zone Network Load Balancer whose targets
are the private application instances.

C. Keep the single NAT gateway and add more-specific routes from all three
private subnet route tables to that same zonal endpoint.

D. Deploy one NAT gateway in each active Availability Zone and update each
private subnet route table to use the NAT gateway in the same zone.

### 7 - Choose TWO

A company is separating a tightly coupled Windows application and IBM Db2
database during a data-centre exit. The application vendor supports only the
current Windows operating system and binaries, so the application server must
be rehosted with block-level replication and a controlled cutover. The database
must instead move to an AWS managed PostgreSQL-compatible engine, requiring
schema conversion and continuous replication until the final outage window.
The business will not accept rehosting the Db2 virtual machine as the final
database state.

Which TWO services should the company use?

A. Use AWS Application Migration Service to continuously replicate the Windows
server, perform test launches, and complete a cutover launch with minimal guest-
operating-system change.

B. Use AWS DataSync to copy the Windows filesystem into S3 and automatically
convert that object data into a bootable EC2 AMI.

C. Use Amazon MQ for ActiveMQ to translate Db2 tables, stored procedures, and
data types into PostgreSQL-compatible definitions.

D. Use Volume Gateway snapshots as a heterogeneous database change-data-capture
mechanism and restore them directly into the managed PostgreSQL engine.

E. Use AWS SCT or AWS DMS Schema Conversion for the heterogeneous schema, and
AWS DMS full-load plus change-data-capture tasks to keep the target current
until cutover.

### 8

All accounts in a Human Resources OU inherit three common SCPs. Recruiting
accounts need those same controls plus a restriction on additional services.
The accounts already contain production workloads, consolidated billing and
organization integrations that must not be interrupted. The governance team
wants inheritance to express the relationship and wants future Recruiting
accounts to receive both control sets automatically, without duplicating every
policy on every account.

Which design is most appropriate?

A. Remove Recruiting accounts from the organization, create a separate
organization with the additional SCP, and later reinvite the accounts to the
original organization for billing.

B. Leave Recruiting accounts directly in the Human Resources OU and attach
copies of all common and additional SCPs to every existing and future account.

C. Create a Recruiting child OU under Human Resources, attach only the
additional restriction to the child OU, and move the Recruiting accounts into
that OU.

D. Replace the Human Resources SCPs with permissions boundaries on roles in the
management account and rely on those boundaries to govern member-account root
users.

### 9 - Choose TWO

A media company serves private objects from S3 through CloudFront. Direct S3
requests must be denied even if someone discovers an object URL. For each
viewer request, the origin must be selected from a combination of country,
device category, and a signed exception cookie; path alone is insufficient.
Selection occurs after the cache behavior is chosen but before CloudFront sends
the request to an origin. The company wants to preserve CloudFront caching and
avoid publishing the bucket.

Which TWO actions best meet these requirements?

A. Enable S3 static website hosting, make the bucket public, and rely on object
prefixes and unpredictable file names to discourage direct requests.

B. Configure CloudFront Origin Access Control and an S3 bucket policy that
authorizes the intended distribution while retaining S3 Block Public Access.

C. Create one cache behavior per URL path and assume that path behaviors can
evaluate country, device, and cookie combinations after behavior selection.

D. Use a Lambda@Edge origin-request function to evaluate the required request
attributes and dynamically set the eligible origin before the origin fetch.

E. Attach an internet gateway to the S3 bucket, remove CloudFront, and implement
country and device routing in VPC route tables.

### 10

A product catalogue uses a DynamoDB table with predictable writes and a very
small hot set that receives millions of repeated, eventually consistent reads.
Provisioned read capacity and read latency are both higher than desired. The
application team can adopt a DynamoDB-compatible caching client and can tolerate
cache invalidation behavior for supported operations. The solution must remain
managed, provide microsecond response times for cache hits, and avoid an
application-managed Redis caching layer.

Which solution is most appropriate?

A. Create a global secondary index using the same partition and sort keys as the
base table and direct every hot-item read to that index.

B. Deploy a multi-node DAX cluster across Availability Zones and update the
application to use the DAX client for supported DynamoDB operations.

C. Deploy DAX but require strongly consistent reads for every request so that
all hot-item requests are served from the DAX item cache.

D. Export the DynamoDB table to S3 before each reporting interval and use S3
Select as the low-latency cache for individual hot items.

### 11

A manufacturer is replacing an on-premises B2B gateway used for EDI document
exchange. Trading partners require AS2 over HTTPS, signed and optionally
encrypted payloads, partner-specific certificates and agreements, and
synchronous or asynchronous Message Disposition Notifications. Received files
must land in S3 and operational events must be visible in CloudWatch. The
company does not want to manage an ActiveMQ or RabbitMQ broker or build the AS2
protocol implementation itself.

Which AWS service is designed for this requirement?

A. Use AWS DataSync tasks with TLS to copy partner directories into S3 and treat
task-completion reports as AS2 Message Disposition Notifications.

B. Use Amazon MQ for RabbitMQ, expose AMQP endpoints to each partner, and store
broker messages in S3 after consumers acknowledge them.

C. Use an Amazon SNS HTTPS topic for partner uploads and model AS2 certificates
and agreements as message attributes.

D. Use AWS Transfer Family AS2 with partner and local profiles, certificates,
agreements or connectors, S3-backed storage, MDNs, and CloudWatch logging.

### 12 - Choose THREE

An enterprise uses Transit Gateway to connect hundreds of workload VPCs to
central networking VPCs and two data centres. DNS resolution must be
bidirectional: on-premises clients need names from Route 53 private hosted
zones, and workloads in associated VPCs need private data-centre domains. The
company wants conditional forwarding, not zone transfers or public DNS. The
design must account for the VPCs that originate queries and for network ACL,
security-group, routing, and port 53 reachability.

Which THREE components are required?

A. Deploy highly available Route 53 Resolver inbound endpoints in the central
DNS VPCs and make their IP addresses reachable from the on-premises resolvers.

B. Publish the private data-centre namespaces in Route 53 public hosted zones so
AWS workloads can resolve them without forwarding rules.

C. Deploy highly available Route 53 Resolver outbound endpoints to send matched
AWS-originated queries toward the on-premises DNS resolver addresses.

D. Attach an internet gateway to every VPC associated with a private hosted zone
and use it to carry recursive DNS queries.

E. Create conditional Resolver forwarding rules, share or associate them with
the query-originating VPCs, and permit the required UDP and TCP DNS paths.

F. Create S3 interface endpoints in the DNS VPC and configure their private IP
addresses as recursive DNS forwarders.

### 13

A company is designing an event-ingestion platform for customer account
changes. Events for one customer must be processed in order, while different
customers should scale across independent processing units. Fraud, analytics,
and billing consumers must read the same event independently and maintain their
own checkpoints. If a consumer deploys defective code, it must be able to
replay seven days of retained events without asking producers to resend them.
Typical throughput is high and email delivery is not involved.

Which service is the best fit?

A. Place all events on one SQS Standard queue and allow the three independent
systems to compete for and delete messages from that queue.

B. Publish events to an SNS topic with email subscriptions and use email inbox
retention as the seven-day replay store.

C. Create an EventBridge Scheduler schedule for every event and use schedule
names to preserve ordering and represent each consumer's checkpoint.

D. Use Kinesis Data Streams with customer ID as the partition key, configure
seven-day retention, and give each consumer its own application and checkpoint.

### 14

An application stores authoritative user-session and workflow state in a
self-managed Redis deployment. Data loss is not acceptable merely because a
cache node fails. The replacement must be Redis-compatible, keep a durable
multi-AZ transaction log, support fast failover, and remove database patching
and node-recovery work from the application team. The requirement is for a
primary database, not for an accelerator in front of another database and not
for an ephemeral Memcached tier.

Which service is the best fit?

A. Migrate the authoritative state to Amazon MemoryDB, using its Redis-compatible
API, durable multi-AZ transaction log, replicas, and managed failover.

B. Store the state in ElastiCache for Memcached and rely on consistent hashing
to reconstruct all lost session records after node replacement.

C. Deploy DynamoDB Accelerator without a DynamoDB table and use the DAX item
cache as the durable system of record.

D. Model every Redis key as a Neptune graph edge and use read replicas as a
drop-in Redis protocol endpoint.

### 15 - Choose TWO

A global migration programme has thousands of servers across several data
centres. Architects need inventory, utilization, and network-dependency data to
group systems into applications and migration waves. Programme managers then
need one AWS view of migration status across discovery, rehost, database, and
partner tools. The company is not asking for a vulnerability scanner or for a
tool that performs every migration itself; it needs discovery evidence and
portfolio tracking with minimal custom integration.

Which TWO services best support these needs?

A. Deploy CloudFormation stack sets into the data centres and infer process-
level application dependencies from CloudFormation drift reports.

B. Use AWS Application Discovery Service collectors or agents as appropriate to
gather server inventory, performance, process, and dependency information.

C. Use Amazon Inspector findings as the authoritative application-dependency
graph and automatically convert vulnerability groups into migration waves.

D. Use AWS Migration Hub in the selected home Region to group applications and
track migration progress across supported tools and Regions.

E. Deploy AWS WAF in front of the data-centre servers and derive CPU, memory,
and east-west process dependencies from web ACL logs.

### 16

An organization wants every supported new resource to include `CostCenter` and
`Application` tags, with values that follow centrally defined capitalization
and allowed-value rules. A create request that omits a required tag must be
rejected where the target API and condition keys support that control. The
governance team also needs an organization-wide report of existing resources
whose tags do not follow the standard. It understands that one policy type does
not both normalize values and universally block untagged creation.

Which solution best meets all requirements?

A. Define the tag standard in an Organizations tag policy, use Resource Groups
tag-policy compliance reporting for existing resources, and apply tested SCP
denies to supported create APIs that omit the required request tags.

B. Attach only a tag policy at the organization root and assume that tag-policy
compliance automatically denies every AWS resource-creation API when a tag is
missing.

C. Deploy an AWS Config rule after resources are created and treat its detective
evaluation as a synchronous preventive authorization check on every create
request.

D. Activate cost-allocation tags in Cost Explorer and rely on Cost Explorer to
rewrite invalid resource tags and reject untagged resources before creation.

### 17

A commercial application runs on EC2 instances in two Availability Zones. Both
instances concurrently open, lock, read, and update files through NFS and must
see one coherent POSIX namespace. If one Availability Zone fails, the surviving
instance must continue accessing the same filesystem without waiting for a
scheduled synchronization job. The vendor will not rewrite the application for
object storage, and the solution must not depend on attaching one zonal block
volume across zones.

Which storage service is most appropriate?

A. Attach one EBS volume per instance and schedule bidirectional DataSync tasks
every hour to approximate a shared writable filesystem.

B. Store active files in S3 Glacier Flexible Retrieval and mount the archive
tier directly as a low-latency NFS filesystem on both instances.

C. Create one EBS Multi-Attach volume in a third Availability Zone and mount it
simultaneously from EC2 instances in both application zones.

D. Create a Regional Amazon EFS filesystem with mount targets in the required
Availability Zones and mount it from both application instances.

### 18 - Choose TWO

Security teams repeatedly find workload security groups allowing SSH or RDP
from `0.0.0.0/0`. They need continuous evaluation, evidence of noncompliance,
and automatic removal of the offending rule through an approved runbook. The
central security account also needs a current organization-wide configuration
view without signing into each source account. The solution is detective and
remediating; it is not intended to become the inline packet-processing path for
all application traffic.

Which TWO actions best meet these requirements?

A. Parse VPC Flow Logs for successful administrative connections and delete the
corresponding security-group rule only after an external connection has already
occurred.

B. Deploy an AWS Config managed or custom rule and associate an approved Systems
Manager Automation remediation runbook with appropriate permissions and
controls.

C. Configure CloudTrail Insights as an inline firewall that rejects every
`AuthorizeSecurityGroupIngress` request containing an administrative port.

D. Capture monthly security-group screenshots in each account and ask the
security team to compare them manually with the preceding month.

E. Configure an organization AWS Config aggregator in the security account and
ensure the required source-account and Region recording coverage exists.

### 19

A data centre must move 20 TB of active NFS data to S3 within a two-week
transition. A dedicated 200-Mbps connection is available, the estimated
transfer fits the window, and users will continue changing files until the
final cutover. The team needs repeated incremental copies, encryption in
transit, integrity verification, scheduling, filtering, and task-level
reporting. It prefers an online managed transfer and does not want to ship an
appliance when the network can meet the deadline.

Which service is the best fit?

A. Order one Snowcone, disconnect the source filesystem during the entire
two-week copy, and use the device as the only mechanism for detecting changed
files.

B. Deploy an AWS DataSync agent, configure source and S3 locations, and run
initial plus incremental tasks over the dedicated connection with verification
and reporting enabled.

C. Transition empty S3 prefixes to Glacier Deep Archive and use archive-
retrieval jobs to pull the active on-premises NFS files into those prefixes.

D. Configure Amazon AppFlow with the NFS export as a native SaaS connector and
use flow schedules to preserve POSIX file metadata in S3.

### 20 - Choose THREE

An organization with regulated workloads must centralize management-event audit
logs from all existing and newly created member accounts. Logs must be
delivered to a separately administered Log Archive account. Workload
administrators, including member-account administrators, must not be able to
delete or shorten required retention on delivered objects. Auditors also need
cryptographic evidence if a delivered log file is modified or deleted. The
solution must use native organization and storage controls rather than a
monthly manual export.

Which THREE actions best meet these requirements?

A. Create independent account trails that deliver only to buckets controlled by
each workload-account administrator, and copy screenshots to the Log Archive
account monthly.

B. Create an organization trail that automatically includes member accounts and
delivers to a tightly controlled bucket in the separately administered Log
Archive account.

C. Grant workload-account administrators permission to suspend or shorten
retention whenever they declare an operational incident.

D. Protect the destination with restrictive bucket and key policies and use S3
Object Lock compliance-mode retention where the stated retention must be WORM.

E. Disable CloudTrail log-file validation and rely on S3 object ETags as the
complete cryptographic chain for all log files.

F. Enable CloudTrail log-file integrity validation, retain the digest files,
and monitor trail delivery and validation failures.

### 21

A multiplayer application exposes custom TCP and UDP protocols from Network
Load Balancers in two Regions. Console clients and firewall allowlists require
the same small set of static anycast IP addresses worldwide. The service must
use the AWS global network to improve path performance and move new connections
away from an unhealthy regional endpoint quickly, without relying on DNS TTLs
or requiring the client to resolve a new address. Content caching is not part
of the requirement.

Which service best meets the requirement?

A. Put the TCP and UDP listeners behind CloudFront, assign fixed edge IP
addresses to the distribution, and use cache invalidations for endpoint
failover.

B. Publish the public endpoints only in a Route 53 private hosted zone and use
private alias records as globally reachable static anycast addresses.

C. Create an AWS Global Accelerator with TCP and UDP listeners, regional
endpoint groups, health checks, and the accelerator's static anycast IP
addresses.

D. Create an edge-optimized API Gateway REST API and tunnel both arbitrary TCP
and UDP sessions through HTTP request bodies.

### 22

An Auto Scaling group is designed to add hundreds of compute-optimized
instances during quarter-end processing. At the previous event, scaling
policies increased desired capacity correctly, but instance launches failed
after the account exhausted the applicable regional On-Demand vCPU quota.
Demand forecasts show further growth. The reliability team wants a proactive,
repeatable control that distinguishes a quota ceiling from an Auto Scaling
policy defect and provides sufficient lead time before the next event.

Which long-term improvement is most appropriate?

A. Monitor the relevant Service Quotas utilization and usage metrics, alarm
before the remaining headroom is exhausted, and request justified quota
increases ahead of forecast growth.

B. Configure the Auto Scaling group to retry every failed instance launch
indefinitely and suppress quota alarms so retries do not page operators.

C. Add several additional target-tracking and step-scaling policies, each of
which raises desired capacity beyond the same unchanged regional quota.

D. Disable the current scaling metrics and use S3 server-access-log volume as
the account's authoritative On-Demand vCPU quota measurement.

### 23 - Choose TWO

A migration programme contains two distinct workloads. A large COBOL mainframe
estate needs automated code analysis, documentation generation, business-logic
extraction, and transformation assistance with human review at decision
points. A separate group of Linux application servers has supported operating
systems and must move to EC2 with minimal application change through continuous
block-level replication, test launches, and a scheduled cutover. The company
does not want to force both workloads through one migration mechanism.

Which TWO services best match the workloads?

A. Use AWS DataSync metadata filters to decompose COBOL programs and generate
business-logic documentation from changed-file reports.

B. Use AWS Transfer Family SFTP connectors as continuous block-level replication
agents for the Linux operating-system disks.

C. Use AWS Transform for mainframe to analyze the estate, generate artifacts,
assist decomposition and transformation, and retain human review.

D. Publish COBOL source files to Amazon MQ and use broker acknowledgements as
generated mainframe documentation and transformation plans.

E. Use AWS Application Migration Service for continuous replication, test
launches, and cutover of the minimally changed Linux servers to EC2.

### 24

A deployment role in Account A must assume `ReleaseRole` in Account B. The
target role's trust policy already names the exact Account A role and includes
the required external ID. CloudTrail shows that the caller reaches STS but is
denied before receiving temporary credentials. The caller's current policy
allows CloudFormation operations in Account A but contains no STS permission.
No SCP, permissions boundary, or session policy supplies an explicit denial.

Which change most directly completes the two-sided authorization model?

A. Add the Account A role principal to an S3 bucket policy in Account B and
assume that resource access implicitly authorizes STS role assumption.

B. Add `sts:AssumeRole` for the exact `ReleaseRole` ARN to the caller's identity-
based policy while retaining the target role's trust and external-ID condition.

C. Add an inbound security-group rule for TCP 443 to the target account because
STS evaluates VPC security groups before IAM policies.

D. Replace both roles with matching IAM users and long-term access keys so that
the password identity is identical in both accounts.

### 25

A new financial ledger has a key-value access pattern and must accept local
writes in two application Regions. Every acknowledged write must be readable
strongly from either Region, and the business requires an RPO of zero after a
regional failure. The team can deploy the topology only in supported Regions
and can include a third Region as a full replica or witness. It wants a managed
multi-active database and will not build consensus or conflict-resolution code
in the application.

Which database design best meets the requirement?

A. Use a DynamoDB global table in multi-Region eventual-consistency mode and
assume asynchronous last-writer-wins replication provides strong reads and a
guaranteed zero RPO.

B. Use Aurora Global Database with one writer Region and asynchronous cross-
Region storage replication while allowing both application Regions to write
independently.

C. Create independent DynamoDB tables in the two Regions and reconcile them
with daily S3 exports after a regional failure.

D. Create a DynamoDB global table in multi-Region strong-consistency mode, using
the two application replicas plus the required third supported replica or
witness topology.

### 26 - Choose TWO

A customer request crosses API Gateway, Lambda, ECS, and database calls in
eight AWS accounts. Teams have host metrics but cannot identify the downstream
call responsible for intermittent latency, and operators must switch accounts
to correlate logs and metrics. The company wants standards-based distributed
instrumentation, trace context propagated across service boundaries, and one
monitoring account that can observe linked source accounts without deploying a
self-managed telemetry platform in every account.

Which TWO improvements best address these problems?

A. Instrument supported services with AWS Distro for OpenTelemetry, propagate
trace context, and send distributed traces to AWS X-Ray.

B. Query CloudTrail Lake management events and infer every internal application
method call and trace-parent relationship from AWS API audit activity.

C. Deploy and operate an unrelated self-managed tracing cluster in every
workload account, with no common trace identifiers or central view.

D. Use source and destination IP addresses from VPC Flow Logs as the sole trace
identifier for every logical microservice request.

E. Configure CloudWatch cross-account observability links from source accounts
to a central monitoring account for the required metrics, logs, and traces.

### 27

A company has several VMware vCenter environments containing thousands of
virtual machines. Before selecting migration waves, architects need VM
inventory, resource-utilization history, and network-connection data. Security
will not approve installing an agent in every guest operating system during the
discovery phase, but it allows a supported collector appliance to read the
virtualization-management environment. The output must feed AWS migration
planning rather than vulnerability remediation.

Which discovery approach is most appropriate?

A. Install one CloudWatch agent on each vCenter server and infer guest-level
process dependencies from the host's standard CPU metric.

B. Use Amazon Inspector agentless scanning as the portfolio's migration-
dependency and utilization database.

C. Deploy and register the AWS Application Discovery Service Agentless
Collector for the VMware environments and export the collected discovery data
to the migration-planning workflow.

D. Place AWS WAF in front of vCenter and use web ACL logs to reconstruct all
east-west connections between guest virtual machines.

### 28

An enterprise has two resilient Direct Connect locations and central Transit
Gateways that connect hundreds of VPCs across several Regions. It wants the
on-premises routers to exchange routes with the Transit Gateway-based network
through a scalable gateway association, not through a separate private virtual
interface and virtual private gateway for every VPC. Public AWS service access
is handled separately and must not be confused with private VPC routing.

Which design is most appropriate?

A. Create a public virtual interface for every workload VPC and attach each
public virtual interface directly to that VPC's route tables.

B. Associate an internet gateway with each Direct Connect connection and use it
as the BGP termination point for private Transit Gateway prefixes.

C. Terminate a private virtual interface on an S3 gateway endpoint and
propagate all Transit Gateway routes through the endpoint policy.

D. Create transit virtual interfaces to a Direct Connect gateway and associate
that gateway with the required regional Transit Gateways using allowed-prefix
and routing controls.

### 29 - Choose TWO

A studio submits thousands of independent rendering jobs during one week each
month. Each container job requires proprietary drivers baked into an approved
custom EC2 AMI. Jobs are queued, checkpoint every few minutes, and can restart
after interruption. The studio wants AWS to schedule jobs, scale compute to the
queue, and diversify instance selection. It wants the lowest practical compute
cost and does not want to keep a full fleet running between render periods.

Which TWO choices best meet the requirements?

A. Use an AWS Batch Fargate compute environment and configure the custom EC2 AMI
as the Fargate host image for each job.

B. Use an AWS Batch managed EC2 compute environment with a launch template that
selects the approved custom AMI and suitable instance families.

C. Keep a fixed On-Demand fleet sized for the monthly peak, poll the queue with
a custom scheduler, and leave the fleet running between render periods.

D. Package the rendering engine in Lambda, configure a 24-hour function timeout,
and store the proprietary GPU drivers in environment variables.

E. Configure diversified Spot allocation across compatible instance types for
the interruption-tolerant workload, with retry and checkpoint-aware job
behavior.

### 30

An application uses an ElastiCache for Memcached cluster and connects by using
a hardcoded list of node endpoints. Operations adds nodes during a traffic
increase, but the old client keeps its original server list, so the added nodes
receive no keys and the original nodes remain overloaded. The company wants the
client to discover membership changes and distribute keys across the current
cluster without placing Memcached behind a layer-7 load balancer or changing to
a database that solves a different requirement.

Which improvement most directly addresses the problem?

A. Enable Redis Multi-AZ automatic failover on the existing Memcached cluster
and expect it to update the application's Memcached server list.

B. Add a global secondary index to the Memcached cluster and use index
propagation to redistribute existing cache keys.

C. Use a compatible Memcached client with ElastiCache Auto Discovery, or update
the client configuration from the cluster's current node membership.

D. Register individual Memcached nodes behind an Application Load Balancer and
route the binary Memcached protocol by HTTP path.

### 31 - Choose TWO

A migration programme begins with hundreds of self-managed database servers of
several engines and versions. Architects need automated inventory, utilization,
and target recommendations before selecting candidates. Selected Oracle
databases will then move to Aurora PostgreSQL, requiring heterogeneous schema
and code conversion plus an initial data load and ongoing changes until a
short cutover window. The solution must separate estate assessment from schema
conversion and data replication.

Which TWO services or capabilities best meet these requirements?

A. Use AWS DMS Fleet Advisor collectors and inventory analysis to assess the
database estate and generate candidate target-engine recommendations.

B. Use AWS Application Migration Service launch settings to translate Oracle
stored procedures and data types into Aurora PostgreSQL syntax.

C. Use AWS DataSync enhanced mode to interpret Oracle redo logs and apply
transaction-level changes directly to Aurora PostgreSQL.

D. Use AWS DMS Schema Conversion or AWS SCT for heterogeneous conversion, then
AWS DMS full-load and change-data-capture tasks until cutover.

E. Use Amazon Inspector findings to rewrite unsupported Oracle schema objects
and to replicate production rows into Aurora.

### 32

An application uses Route 53 failover records whose health checks are linked to
Amazon Application Recovery Controller routing controls. During a regional
incident, engineers must switch a routing-control state that was configured and
tested in advance. The ordinary Route 53 API—the Route 53 control plane—is
impaired. The Route 53 DNS data plane remains healthy, and the runbook has
cached all five ARC cluster endpoint addresses and valid IAM credentials.

Which action uses the intended highly available mechanism?

A. Call `ChangeResourceRecordSets` through the ordinary Route 53 API and wait
for the impaired Route 53 control plane to edit the failover records.

B. Call the ARC routing-control data-plane API against an available regional
cluster endpoint and let the routing-control health check influence the Route
53 failover records.

C. Modify the failed Region's Application Load Balancer listener and assume the
listener change updates the ARC cluster's routing-control consensus state.

D. Delete the routing-control health check through the Route 53 control plane
and rely on DNS cache expiration as the ARC data-plane operation.

### 33

A regulated company is building a generative-AI support assistant with Amazon
Bedrock foundation models. The same policy must evaluate user prompts and model
responses, block configured harmful-content categories at defined strengths,
and prevent discussion of company-defined prohibited topics. The team wants a
managed policy layer that can be associated with model inference and does not
require training, deploying, and monitoring a custom text classifier. This is a
content-safety requirement, not network-rate limiting or S3 discovery.

Which service capability most directly meets the requirement?

A. Use GuardDuty Malware Protection to classify every prompt and model response
and define the prohibited conversational topics as malware signatures.

B. Put a WAF rate-based rule in front of the application and use request counts
as the harmful-content and denied-topic classifier.

C. Configure Amazon Bedrock Guardrails with the required content filters and
denied-topic policies, and apply the guardrail to inference requests and
responses.

D. Store prompts in S3, wait for Macie classification jobs, and use bucket
findings as synchronous generation controls.

### 34 - Choose TWO

An ECS service already uses CodeDeploy blue/green deployments with separate
target groups. Container health checks cannot detect a known business-logic
failure. Release engineers need to route synthetic validation requests to the
replacement task set before any production shift, then use canary production
traffic. If either infrastructure metrics or a custom order-success KPI
breaches its threshold during deployment, CodeDeploy must stop and restore the
original task set without waiting for manual intervention.

Which TWO additions complete the design?

A. Terminate the original task set immediately after container health checks
pass, before synthetic or canary validation begins.

B. Replace CodeDeploy traffic shifting with Systems Manager Patch Manager and
use operating-system patch compliance as the business KPI.

C. Configure the load balancer test listener and CodeDeploy deployment group so
synthetic validation traffic reaches the replacement target group before the
production shift.

D. Run an independent all-at-once ECS rolling update and use its deployment
timeout as the custom order-success metric.

E. Associate infrastructure and custom business-metric CloudWatch alarms with
the deployment and enable CodeDeploy automatic rollback.

### 35

A media archive must move 1 PB of static, encrypted-at-rest data from a closing
facility to S3 within two weeks. The only network path is a shared 100-Mbps
internet connection, and bandwidth calculations show that an online copy
cannot meet the deadline even if the link is saturated. The source dataset will
not change after export begins. The company can use chain-of-custody procedures
and wants AWS-managed shipping, device tracking, and ingestion rather than
building a temporary high-speed circuit.

Which migration method is most appropriate?

A. Order enough appropriately sized AWS Snow Family devices, copy and validate
the static dataset, return the devices through the managed shipping workflow,
and import into the target S3 bucket.

B. Upload the full petabyte through the existing 100-Mbps link using one AWS CLI
process and assume multipart upload removes the physical bandwidth limit.

C. Encode each archive file as an SQS message body and use queue retention as
the bulk-data transport into S3.

D. Send the binary archive through CloudWatch Logs subscriptions and reconstruct
the files from log events after two weeks.

### 36

Executives want one prioritized view of findings from AWS security services and
supported partner products across an organization. The security operations
account must administer member relationships through Organizations and reduce
manual account enrollment. The company also needs multi-Region visibility
through a defined home or aggregation model. An architect proposed Audit
Manager, but that service's evidence-assessment purpose does not satisfy the
threat and posture findings requirement.

Which service should replace Audit Manager for this requirement?

A. Use AWS Security Hub with Organizations integration, a delegated
administrator, and the required central configuration or cross-Region
aggregation model.

B. Use AWS Artifact to ingest live GuardDuty and Inspector findings and assign
remediation owners across member accounts.

C. Use AWS Budgets anomaly alerts as the normalized findings store for security
services and partner products.

D. Use AWS Service Catalog portfolios to aggregate security findings and treat
product versions as finding severity.

### 37 - Choose TWO

A company selected pilot light for a new application because its RTO permits
application compute to remain mostly off in the recovery Region, but its RPO
requires current replicated data. During a regional disaster, operators must
activate networking and application capacity from tested definitions rather
than build servers manually. The company wants the minimum always-on footprint
consistent with pilot light, not the continuously serving scaled-down
application tier of warm standby.

Which TWO components are essential to this strategy?

A. Run a full-size active application fleet in both Regions and distribute all
production traffic evenly, regardless of the selected pilot-light strategy.

B. Maintain cross-Region replication or a continuously updated data replica in
the recovery Region, with lag monitoring aligned to the RPO.

C. Store manual server build instructions only in a wiki and begin package
installation after the primary Region has failed.

D. Retain monthly backups only in the primary Region and assume they remain
accessible after a complete primary-Region outage.

E. Keep tested infrastructure-as-code and automated bootstrap, deployment, and
scale-up actions ready to activate the recovery application tier.

### 38

A document platform stores 500 TB in S3. Access cannot be classified reliably
by prefix or age: some objects become cold after one day, others remain active
for years, and individual objects can become hot again. The business wants
millisecond access for the active tiers and automatic per-object movement based
on observed access without writing lifecycle rules that transition an entire
prefix on a fixed date. Retrieval fees and operational analysis should be
minimized for the frequently accessed portion.

Which storage class is the best fit?

A. Place all objects in S3 One Zone-IA and accept loss of a whole Availability
Zone as the automatic access-pattern optimization mechanism.

B. Transition every object immediately to S3 Glacier Deep Archive and use bulk
restores for all interactive document requests.

C. Keep every object permanently in S3 Standard and conduct a manual annual
review to identify cold objects.

D. Use S3 Intelligent-Tiering so eligible objects move automatically among
access tiers according to their individual observed access patterns.

### 39

A factory application portfolio is moving to AWS, but one calibrated appliance
uses proprietary PCI hardware and a vendor license tied to its current physical
host. The vendor will not support virtualization or cloud deployment for two
more years. The appliance still provides business value and must communicate
with migrated services over private hybrid networking until a funded
replacement project completes. The company is neither shutting it down nor
changing its architecture during this migration wave.

Which migration strategy describes the appliance?

A. Classify the appliance as rehost and replicate its unsupported physical
hardware directly into a general-purpose EC2 instance without vendor approval.

B. Classify it as refactor and rewrite the appliance firmware during the current
data-centre-exit wave despite the licensing and hardware constraints.

C. Classify it as retain, document the constraint and replacement trigger, and
provide controlled hybrid connectivity to the migrated systems.

D. Classify it as retire and power it off even though dependent production
processes still require its functionality.

### 40 - Choose THREE

Security requires organization-wide packet enforcement for internet-bound
traffic from many VPCs, regardless of inconsistent workload security groups.
The network team has chosen centralized inspection VPCs with AWS Network
Firewall and Transit Gateway. Workload administrators must not bypass the
inspection path. Return traffic must traverse the same stateful inspection
path, and stateless trusted-pass rules must be evaluated before the default
drop. The organization wants centralized policy deployment rather than one
manually managed firewall per account.

Which THREE actions best meet these requirements?

A. Use AWS Firewall Manager with Organizations to deploy and manage AWS Network
Firewall policies across the intended accounts and organizational units.

B. Route application packets through Security Hub findings and use finding
severity as the stateless firewall action.

C. Configure workload, Transit Gateway, inspection-subnet, and egress routes so
forward and return traffic traverse the intended firewall endpoints
symmetrically.

D. Assign the default drop stateless rule a lower numeric priority than trusted
pass rules so the drop is evaluated before every exception.

E. Replace inline enforcement with a monthly AWS Config compliance report and
allow all packets until an operator reviews the report.

F. Assign trusted stateless pass rules lower numeric priorities than the default
drop path so approved traffic is evaluated first.

### 41

A consumer mobile application needs managed sign-up, sign-in, account recovery,
and federation with social identity providers. Authenticated users must receive
temporary AWS credentials scoped by application roles, and unauthenticated
guests may receive a more restricted temporary role for a limited feature. The
company will not embed long-term IAM keys in the application and does not want
to operate a public Active Directory domain or custom token-to-STS broker.

Which design best meets the requirements?

A. Create one IAM user per customer, embed each long-term access key in the
mobile application, and rotate the keys through application-store releases.

B. Expose Simple AD directly to the internet and use LDAP passwords as permanent
AWS access keys for authenticated and guest users.

C. Use a Cognito user pool for user authentication and federation, and a
Cognito identity pool to exchange identities for scoped temporary AWS
credentials, including an optional unauthenticated role.

D. Deploy an EC2 bastion that stores every user's social-provider password and
returns long-term IAM access keys after each login.

### 42 - Choose TWO

An Aurora PostgreSQL application scales from 30 to 700 containers during
reporting periods. Each container opens multiple short-lived connections,
causing writer connection exhaustion and a reconnection storm during failover.
At the same time, long-running read-only reports consume writer resources. The
application can change connection endpoints and separate read-only operations,
but the company wants to retain Aurora and avoid a major data-access rewrite.

Which TWO improvements most directly address the two bottlenecks?

A. Route every SQL statement, including transactions that modify data, to one
Aurora replica and rely on replica promotion for normal write processing.

B. Place RDS Proxy between the container fleet and Aurora to pool connections,
control connection churn, and improve application behavior during failover.

C. Remove connection limits and retry every failed connection immediately from
all 700 containers without jitter or backoff.

D. Add appropriately sized Aurora replicas and route eligible reporting queries
through the reader endpoint while writes remain on the writer path.

E. Replace the Aurora endpoint in the application with an S3 website endpoint
and submit SQL statements as object keys.

### 43

An engineering office is moving an on-premises NFS share to S3 over several
months. Legacy applications must continue using their existing NFS mount and
need low-latency access to recently used files through an on-premises cache.
The authoritative cloud copy must be stored as native S3 objects so other AWS
services can access it directly. The company does not want a block-volume
gateway whose data is stored as opaque EBS snapshots.

Which service is the best fit?

A. Deploy Volume Gateway in stored-volume mode and assume its block snapshots
appear as native S3 objects with the original filenames.

B. Attach one EBS volume simultaneously to the on-premises NFS server and EC2
instances across the WAN.

C. Publish each file as an Amazon MQ message and mount the broker queue as an
NFS filesystem.

D. Deploy Amazon S3 File Gateway on premises, expose an NFS share, size the
local cache, and map the file data to native objects in the S3 bucket.

### 44

An organization stores regulated datasets in S3 buckets owned by many member
accounts. Workload administrators can manage bucket policies, but a central
guardrail must ensure that supported resources cannot be made accessible to
principals outside the organization—even if a local administrator adds an
allow. The control must apply to the resource side of authorization, including
requests made by external principals, and must define maximum permissions
without granting any access on its own.

Which control best meets the requirement?

A. Enable and test an Organizations resource control policy at the appropriate
root, OU, or account scope that denies unsupported external access to the S3
resources in member accounts.

B. Attach a VPC security group to every S3 bucket and use ingress rules to
override bucket-policy grants made to external AWS principals.

C. Store one IAM access key in the management account and require bucket
administrators to include that key ID in every resource policy.

D. Create an S3 lifecycle rule that transitions externally shared objects to
Glacier and assume the storage-class change revokes the external principal.

### 45 - Choose TWO

A customer platform implements warm standby in two Regions. The recovery Region
already runs a scaled-down but functional application stack and receives
continuous data replication. The business requires health-informed traffic
movement and an RTO that cannot tolerate operators manually promoting data,
editing DNS, and scaling each tier from console instructions. The team must
prove the whole sequence through recurring controlled exercises while keeping
the secondary below full production scale during normal operation.

Which TWO components best complete the design?

A. Remove the running recovery stack and keep only undeployed templates so the
design becomes a backup-and-restore strategy while retaining the warm-standby
label.

B. Use Route 53 latency routing without health evaluation or routing controls
and assume it always detects application and data-tier failure.

C. Configure health-based failover routing or preconfigured routing controls
for the regional endpoints, with mechanisms suited to the required failover
authority.

D. Stop cross-Region replication and keep the only current database copy in the
primary Region until after an outage is declared.

E. Automate and regularly exercise data failover, application deployment or
activation, capacity scale-up, validation, and rollback in the recovery Region.

### 46

A regulated company reports 100% successful AWS Backup jobs but has never
restored a representative workload. Its recovery points must remain immutable
for the required retention period even if a privileged identity in the
workload account is compromised. Auditors also require recurring, recorded
evidence that selected protected resource types can be restored into an
isolated environment. The company wants managed controls rather than treating
backup-job completion as proof of application recoverability.

Which solution best meets both requirements?

A. Use the successful backup-job count as proof of RTO and RPO compliance and
perform the first restore only after a production incident occurs.

B. Configure AWS Backup Vault Lock with the required governance or compliance
mode and retention settings, and create restore testing plans for supported
representative resources.

C. Grant every workload administrator `backup:DeleteRecoveryPoint` so they can
remove immutable evidence quickly during an incident.

D. Keep the only recovery point in the workload account's default, unlocked
vault and rely on an IAM allow policy as ransomware isolation.

### 47

A commercial application running on physical servers must move to AWS without
an operating-system or application rewrite. The vendor licenses by physical
socket and requires the customer to control host placement, track socket and
core consumption, and maintain affinity to specific physical hosts. The
migration team wants continuous block replication, non-disruptive test launches,
and a controlled cutover, followed by an EC2 tenancy model compatible with the
licensing constraint.

Which approach is most appropriate?

A. Rewrite the application for Lambda during the data-centre exit and assume
the vendor's physical-socket license applies unchanged to serverless
invocations.

B. Use AWS Application Migration Service for replication and test or cutover
launches, then place the EC2 instances on suitably configured Dedicated Hosts
with host allocation and license tracking.

C. Store the executable binaries in S3 Glacier Deep Archive and execute them
directly in the archive tier while preserving socket affinity.

D. Represent each physical CPU socket as an item in a DynamoDB global table and
run the unmodified operating system inside those items.

### 48 - Choose TWO

On-premises analytics applications need low-volume access to one 800-TB S3
dataset in a different AWS Region. Replicating the full dataset would add
unacceptable storage and replication cost. Traffic must use private IP
addresses and remain on the AWS network after entering through the company's
existing Direct Connect or Site-to-Site VPN path. The architects know that an
S3 gateway endpoint is not reachable from on premises or from a different
Region through Transit Gateway.

Which TWO components best meet the requirement?

A. Create an S3 interface endpoint in a reachable VPC in the bucket's Region,
with endpoint policy, security groups, and DNS behavior appropriate for the
on-premises clients.

B. Create an S3 gateway endpoint inside the on-premises router and advertise its
route-table prefix over Direct Connect.

C. Replicate all 800 TB into every Region that might consume one object, despite
the explicit storage-cost and duplication constraint.

D. Provide a supported routed private path from on premises to the endpoint
network interfaces through Direct Connect or VPN and the VPC connectivity
architecture.

E. Enable S3 static website hosting and access the public website endpoint over
the internet while describing the traffic as private.

### 49

A computational-fluid-dynamics workload runs tightly coupled MPI jobs on EC2.
Profiling shows that node-to-node latency and collective communication—not CPU
capacity or object-storage throughput—dominate elapsed time. The software
supports Elastic Fabric Adapter and can run within one Availability Zone. The
team wants placement and network capabilities that provide low-latency,
high-throughput communication and OS-bypass support, rather than adding ordinary
instances spread across fault domains.

Which architecture best improves performance?

A. Spread burstable instances across several Regions and use internet paths to
increase the geographic diversity of MPI collectives.

B. Register every MPI rank behind an Application Load Balancer and use HTTP
request balancing for all collective communication.

C. Serialize every MPI message to S3 Standard-IA and poll for object creation
to replace east-west network communication.

D. Select EFA-capable instances, configure the workload for EFA, and launch the
nodes in a cluster placement group within one supported Availability Zone.

### 50

Finance must allocate AWS cost monthly by linked account, service, usage type,
purchase option, and application tag. Summary dashboards do not retain enough
line-item detail for chargeback disputes, and analysts need SQL access to
historical usage and pricing fields. The solution should use a supported
billing export, preserve files in S3, and allow a serverless query engine to
join cost data with internal allocation tables. Tags must be activated before
they can appear as cost-allocation dimensions.

Which solution best meets the requirement?

A. Use only the current month's AWS Budgets forecast and infer every historical
resource-level usage line from the forecasted account total.

B. Activate the required cost-allocation tags, configure a Cost and Usage Report
or supported detailed billing export to S3, and query the data with Athena.

C. Store CloudTrail management events in DynamoDB and calculate exact amortized
cost from API names without using billing line items.

D. Export Trusted Advisor cost recommendations once per year and treat the
recommendation text as the complete historical chargeback ledger.

### 51 - Choose TWO

A company has selected AWS Transfer Family for inbound AS2 document exchange.
Each partner has distinct identifiers, signing and encryption certificates,
agreement rules, and Message Disposition Notification expectations. Payloads
must land in an S3-backed directory after Transfer Family validates the partner
and agreement. The team must configure the AS2 protocol objects rather than
substitute an AMQP broker or expose the S3 bucket publicly. It also wants the
configuration to support later partner-specific auditing.

Which TWO configuration groups are required?

A. Create an Amazon MQ broker and MQTT topic, then treat each broker virtual
host as an AS2 partner certificate and agreement.

B. Configure local and partner AS2 profiles with the required identifiers and
certificates for signing, verification, encryption, and decryption.

C. Create a Kinesis shard for each partner and use the shard iterator as the
partner's AS2 identity and MDN configuration.

D. Make the destination S3 bucket a public website and use an email subscription
as proof that Transfer Family authenticated the AS2 partner.

E. Create an AS2 agreement associated with the server, local and partner
profiles, S3-backed access, and the required message and MDN behavior.

### 52

A company is deploying Amazon WorkSpaces in a VPC connected to its data centre
through resilient Direct Connect links. Users already exist in on-premises
Microsoft Active Directory, and the company does not want a directory replica
or managed domain storing directory data in AWS. WorkSpaces must send
authentication requests privately to the existing domain controllers, with the
required DNS resolution, routes, security rules, and highly available connector
placement in the WorkSpaces VPC.

Which design is most appropriate?

A. Install AD Connector on the on-premises domain controllers and expose a
public LDAP endpoint for WorkSpaces to use over the internet.

B. Create matching IAM users for every directory identity and authenticate the
Windows desktops with IAM access-key pairs.

C. Publish all Active Directory records in a public Route 53 hosted zone and
use public DNS as the authentication protocol.

D. Deploy AD Connector across subnets in the WorkSpaces VPC and configure
private routing, DNS, and security connectivity to the on-premises domain
controllers.

### 53

A company is building an agentic operations assistant that can propose
production changes. The model may collect diagnostic evidence automatically,
but any mutating action must pause, send an approval request to an authorized
human, persist the workflow state for hours if necessary, record the approval
or rejection, and resume deterministically from that decision. The company
wants an auditable managed orchestration service rather than keeping an API
request or Lambda invocation open while waiting.

Which service is the best fit for orchestrating this control?

A. Use a Step Functions Standard Workflow with a callback-token or approval
pattern, durable state, explicit approved and rejected branches, and execution
history.

B. Use an S3 lifecycle transition to pause the agent's model output in Glacier
until an approver restores the object.

C. Encode approval state in a Route 53 latency record and consider the action
approved when DNS returns the production Region.

D. Put the agent and production tools in a cluster placement group and rely on
network proximity as evidence of human authorization.

### 54 - Choose TWO

An application team claims that its multi-AZ service tolerates instance,
network, and dependency failures, but no controlled test supports the claim.
It wants reusable, reviewed production experiments with explicitly scoped
targets and IAM permissions. Every experiment must stop automatically if the
customer-error rate crosses a CloudWatch threshold, and the team must retain
experiment execution evidence. It does not want an unbounded script that keeps
injecting faults after the safety condition is breached.

Which TWO actions best meet these requirements?

A. Disable metrics and alarms during experiments so customer errors cannot
trigger an automatic stop before all faults have been injected.

B. Define approved AWS Fault Injection Service experiment templates with scoped
targets, actions, duration, IAM role, and logging or report settings.

C. Use AWS Config evaluation rules as a random instance-termination engine and
omit an experiment template or target scope.

D. Treat the next uncontrolled production outage as the experiment and create
the safety threshold after the outage has ended.

E. Configure the customer-error CloudWatch alarm as an AWS Fault Injection
Service stop condition and verify alarm health before starting.

### 55

A multinational migration programme uses Application Migration Service, AWS
DMS, discovery tooling, and partner products in several AWS Regions. Programme
managers want one portfolio view of application groupings and migration status.
Before importing and aggregating discovery information, the team must select
the AWS Region in which Migration Hub stores and processes the programme's
discovery and tracking metadata. Architects must distinguish this setting from
traffic-routing home Regions and from S3 multi-Region access constructs.

Which service concept is relevant?

A. Configure a CloudFront origin group and treat the primary origin Region as
the storage location for Migration Hub discovery metadata.

B. Select the Region containing the Route 53 private hosted zone because DNS
association automatically determines migration metadata residency.

C. Select and consistently use the AWS Migration Hub home Region for the
programme's discovery, application grouping, and migration-tracking metadata.

D. Create an S3 Multi-Region Access Point and use its global alias as the
Migration Hub home Region identifier.

### 56

A regulated signing platform requires dedicated, single-tenant hardware
security modules in the customer's VPC, direct use of standard cryptographic
interfaces, control over users and partitions, and ownership of HSM cluster
backup and recovery procedures. The workload cannot use a multi-tenant managed
key service even if that service meets the same validation level. The company
accepts the additional operational responsibility and wants a managed AWS
service for provisioning the dedicated HSM appliances.

Which service best meets the requirement?

A. Deploy a multi-AZ AWS CloudHSM cluster, initialize and manage its crypto
users, and integrate the application through supported HSM interfaces.

B. Use an AWS managed KMS key whose policy and key material are controlled only
by the AWS service and shared managed infrastructure.

C. Store private signing keys as plaintext Secrets Manager values without a KMS
key and treat Secrets Manager as a dedicated HSM partition.

D. Upload signing keys to S3 with SSE-S3 and invoke S3 object reads as direct
PKCS #11 cryptographic operations.

### 57 - Choose TWO

Dozens of VPCs owned by several accounts host HTTP and gRPC microservices.
Consumers need private service-name-based connectivity and application-layer
routing without public IP addresses. The platform team wants to associate VPCs
and services with a common service network, then use IAM-based service
authorization. It wants to avoid a full VPC-peering mesh and a dedicated load
balancer or custom service-discovery stack for every producer-consumer pair.

Which TWO actions best meet the requirement?

A. Create an Amazon VPC Lattice service network, associate the participating
VPCs, and register the producer services and target groups with the service
network.

B. Give every service a public IP address and public Route 53 record, then rely
on unguessable hostnames for private communication.

C. Create VPC peering for every producer-consumer pair and require one internet-
facing load balancer for every internal service.

D. Configure VPC Lattice service-network or service auth policies and the
required caller IAM permissions to enforce intended service access.

E. Create S3 gateway endpoints in every VPC and configure S3 route-table entries
as HTTP and gRPC service proxies.

### 58

A production RDS MySQL database runs on one DB instance in one Availability
Zone. Nightly snapshots meet the long-term backup requirement but do not meet
the availability objective: an instance or zonal failure causes an outage while
operators provision and restore another database. The application can tolerate
a short managed failover and minimal endpoint interruption, but the company
does not want to redesign the schema or implement application-managed database
replication.

Which improvement is most appropriate?

A. Add an asynchronous read replica in the same Availability Zone and require
operators to promote it manually after any instance or zonal failure.

B. Increase snapshot frequency to every five minutes while retaining the
single-AZ database and manual restore procedure.

C. Convert the database to an RDS Multi-AZ deployment with a synchronous standby
and managed automatic failover under the supported endpoint.

D. Place the single-AZ RDS instance behind a NAT gateway and use NAT route-table
changes as the database failover mechanism.

### 59

A company is retiring a tape library containing 400 TB of inactive compliance
archives. Its network connection cannot complete the initial transfer before
the facility closes. After migration, retrieval is rare and may take up to 48
hours, so the company wants the lowest-cost S3 archival tier rather than
millisecond access. It needs a managed offline transfer for initial ingestion
and a lifecycle-controlled destination, not an active block filesystem on EC2.

Which approach is most appropriate?

A. Base64-encode each tape and send it synchronously through API Gateway into S3
Standard over the bandwidth-constrained connection.

B. Use appropriately sized AWS Snow Family devices for offline ingestion into
S3, validate the import, and transition eligible objects to S3 Glacier Deep
Archive.

C. Copy the tapes to EC2 instance store and retain the instances permanently as
the durable archival system.

D. Configure Amazon AppFlow with the tape drives as a SaaS connector and use a
flow schedule as the offline shipping process.

### 60 - Choose THREE

Application teams are delegated permission to create and manage IAM roles in
development accounts. Central security needs three distinct layers: a reusable
maximum-permissions cap for the roles the teams create; delegation logic that
allows role creation only when that exact cap is attached and prevents teams
from escaping it; and an organization-level explicit denial for prohibited
services that remains effective even if an identity policy and boundary allow
them. No single allow policy should be described as all three layers.

Which THREE controls form the correct model?

A. Create and require an IAM permissions boundary that defines the maximum
permissions available to the delegated roles but grants no permissions by
itself.

B. Use one S3 bucket policy as the maximum-permissions boundary for all IAM roles
and all AWS services in each development account.

C. Use a VPC security group to deny IAM-authorized API calls to prohibited AWS
services, including calls made through public AWS endpoints.

D. Give team administrators an identity policy that conditions role creation
and management on attachment and preservation of the approved boundary.

E. Use an Organizations tag policy to read permissions from role tags and grant
those permissions directly to the role sessions.

F. Apply an SCP at the appropriate organization scope that explicitly denies
the prohibited services while preserving carefully tested required exceptions.

### 61

An Aurora Global Database has a primary cluster in one Region and read-only
secondary clusters close to global application users. A secondary-Region
application normally performs local reads but occasionally executes supported
DML statements. The team wants those statements forwarded to the primary
writer through the Aurora topology, with supported read-after-write consistency
options, rather than building and securing a custom cross-Region write proxy.
The application can connect to the secondary cluster's appropriate local
endpoint and accepts the additional write latency.

Which capability best meets the requirement?

A. Configure RDS Proxy in the secondary Region to synchronously replicate every
write to an independent RDS instance in the primary Region.

B. Place DAX in front of Aurora and enable DAX global write forwarding for SQL
statements received by secondary clusters.

C. Export SQL statements as S3 objects and use S3 Cross-Region Replication as
the transaction-forwarding mechanism.

D. Enable the supported Aurora Global Database write-forwarding capability on
the secondary cluster and use the documented secondary endpoint behavior.

### 62

An RDS MySQL production database requires a major-version upgrade and parameter
changes. The team needs a synchronized green environment that it can test with
application traffic before committing, while production continues on the blue
environment. At the approved cutover, AWS should perform a controlled
switchover with substantially less downtime and operational work than restoring
a snapshot and manually configuring replication. A rollback plan and the
service's documented switchover limitations will be reviewed beforehand.

Which capability is the best fit?

A. Export an RDS snapshot to S3 and run the application directly against the
Parquet export during the major-version upgrade.

B. Create an Amazon RDS Blue/Green Deployment, apply and validate the changes on
the synchronized green environment, then perform the managed switchover.

C. Enable DynamoDB point-in-time recovery on an unrelated table and use it to
upgrade the RDS MySQL engine.

D. Move the RDS recovery point to AWS Backup cold storage and use the cold-tier
transition as the low-downtime database switchover.

### 63 - Choose TWO

A company is exiting a data centre that hosts an ActiveMQ-based integration
platform and several Linux application servers. JMS clients and broker
semantics must change as little as possible, but the company wants AWS to
operate the broker infrastructure. The Linux servers contain vendor software
that cannot be containerized during the migration and must be rehosted through
continuous block replication, test launches, and a controlled cutover. The
architect must not confuse broker migration with AS2 partner-file exchange.

Which TWO services best meet the requirements?

A. Replace the ActiveMQ broker with Amazon SNS and assume SNS preserves all JMS
transactions, selectors, protocols, and broker-client behavior without changes.

B. Use AWS DMS full-load tasks to copy Linux operating systems, boot records,
and application binaries into EC2 AMIs.

C. Use Amazon MQ for ActiveMQ-compatible managed brokers, validating supported
versions, networking, storage, and client behavior before cutover.

D. Use AWS DataSync filters to translate OpenWire and JMS broker protocols into
an AWS-native messaging protocol.

E. Use AWS Application Migration Service to replicate, test, and cut over the
minimally changed Linux application servers to EC2.

### 64

A platform team publishes approved CloudFormation-based products such as
encrypted data buckets and standard application stacks. Business users should
launch only approved product versions through a curated portfolio. The platform
team must control launch roles, parameters, update behavior, and constraints
without giving users broad direct permissions to create the underlying AWS
resources. Product ownership and version lifecycle must remain centrally
managed and shareable across accounts.

Which service best meets the requirement?

A. Use AWS Service Catalog portfolios and products with launch constraints,
approved versions, sharing, and appropriately scoped launch roles.

B. Use AWS Artifact agreements as executable CloudFormation products and treat
compliance reports as launch roles.

C. Use Amazon Inspector findings as a self-service provisioning portal that
creates the remediating infrastructure automatically.

D. Store CloudFormation templates in CloudTrail Lake and let business users run
arbitrary templates with the CloudTrail service role.

### 65

A company is replacing a growing combination of VPC peering, private load
balancers, and custom DNS for service-to-service communication. Producers and
consumers run in many VPCs and accounts. The target design must provide private
application-layer service discovery, routing to registered targets, and IAM-
based authorization policies through a common service-network abstraction. The
company does not require public internet access or raw transitive IP routing
between every resource in every VPC.

Which service is the best fit?

A. Attach a shared internet gateway to every VPC and enforce service-level IAM
authorization through public IP route tables.

B. Use Amazon VPC Lattice services and a service network with associated VPCs,
targets, listeners, and auth policies.

C. Use S3 Transfer Acceleration edge endpoints as private HTTP service-
discovery and authorization proxies for the VPC applications.

D. Create one Direct Connect public virtual interface per microservice and use
BGP communities as application-layer authorization policies.

### 66 - Choose THREE

A ransomware threat model assumes that privileged identities in a workload
account may be compromised. Recovery points must survive that account's loss,
retain immutability for the full regulatory period without privileged shortening
or deletion, and be proven usable through scheduled restores of supported
resource types. Security wants administrative separation and evidence of
restore outcomes, not merely a second logically named vault controlled by the
same workload administrators.

Which THREE actions best meet these requirements?

A. Configure AWS Backup Vault Lock in compliance mode with validated retention
settings for recovery points whose retention must not be shortened or deleted.

B. Keep the only copy in the default vault administered by the compromised
workload account and rely on the vault name as isolation.

C. Maintain supported cross-account backup copies or logically air-gapped vault
copies governed by a separately administered recovery boundary.

D. Grant workload administrators deletion and retention-change permission on
all recovery points so incident response can remove evidence quickly.

E. Treat `COMPLETED` backup-job status as proof that application dependencies,
credentials, and recovery procedures meet RTO and RPO.

F. Configure AWS Backup restore testing plans for representative supported
resources and validate the restored resources and recovery evidence.

### 67

An existing application stores important session state in ElastiCache for
Memcached. Adding nodes improves capacity but does not provide native
replication or automatic failover, and the application loses sessions when a
node fails. The business now requires a Redis-compatible managed cache with a
primary and replicas across Availability Zones, automatic failover, and
backup-compatible recovery options. The team can update the cache client but
wants to keep a managed in-memory data-store architecture.

Which improvement best meets the requirement?

A. Add more independent Memcached nodes, keep the old client node list, and
assume node count creates synchronous replicas and automatic failover.

B. Register Memcached nodes behind an Application Load Balancer and treat HTTP
health checks as native key replication and promotion.

C. Move the sessions to EC2 instance store on one large instance and rely on
host replacement to preserve the ephemeral data.

D. Migrate to ElastiCache for Redis OSS or Valkey, configure a replication group
across Availability Zones, and enable automatic failover and suitable backups.

### 68

An organization trail already records management events such as bucket-policy
changes in all member accounts. Auditors now require object-level `GetObject`,
`PutObject`, and delete activity for a bounded set of sensitive S3 buckets,
centralized with the existing audit evidence. The architecture must distinguish
CloudTrail data events from management events and avoid enabling indiscriminate
high-volume logging for every object in every bucket when only selected
resources are in scope.

Which addition is required?

A. Enable VPC Flow Logs for imaginary S3 bucket subnets and infer object names,
API operations, and IAM principals from packet metadata.

B. Rely only on AWS Config configuration items, which record resource
configuration rather than every object-level data-plane API request.

C. Add advanced event selectors for S3 object-level data events on the selected
buckets to the organization trail or an appropriate CloudTrail event data
store.

D. Use Route 53 Resolver query logs as the authoritative record of S3 object
reads, writes, request parameters, and principal identity.

### 69 - Choose TWO

A global application has two separate paths. CloudFront serves cacheable static
reads from S3 origins in two Regions, while state-changing API requests are sent
to a regional API and database. The business wants automatic read failover for
methods supported by CloudFront origin groups and continued write capability
after an API Region fails. Architects must not assume that CloudFront origin
failover handles unsupported write methods or creates a second write-capable
application and data path.

Which TWO actions best meet the requirements?

A. Configure only a CloudFront origin group and assume it fails over all methods,
including state-changing POST requests, to a fully functional write stack that
does not otherwise exist.

B. Configure the two S3 origins as a CloudFront origin group for eligible
read-method failover, with appropriate status-code and origin-health behavior.

C. Redirect all state-changing API requests to the S3 website endpoint in the
failed Region and use object reads as database transactions.

D. Retain one regional API and database with no recovery deployment, but lower
the CloudFront TTL so write processing survives a regional outage.

E. Deploy and test a write-capable API plus replicated data path in the recovery
Region, and use health-aware routing or a controlled failover mechanism for
write traffic.

### 70

A new public serverless API accepts JSON Web Tokens from an external OpenID
Connect provider. Requests must be rejected when the issuer, audience, signature,
or time-based token claims are invalid, and valid claims must be passed to the
integration. The team wants API Gateway to perform managed JWT validation at
the edge of the API and does not want to run a custom EC2 proxy or embed user
passwords in infrastructure policies.

Which design is most appropriate?

A. Deploy an internet-facing EC2 proxy, copy every external-provider password to
the instance, and validate users by replaying their sign-in requests.

B. Add issuer and audience strings to a Network Load Balancer security group and
expect packet filtering to validate JWT signatures and claims.

C. Use an API Gateway HTTP API JWT authorizer configured with the external
issuer and intended audience, then authorize routes from validated claims.

D. Store tokens as S3 object keys and use a bucket policy to parse JWT
signatures and forward valid API requests.

### 71

A new industrial platform ingests millions of time-stamped measurements from
sensors. Analysts query values by equipment, time window, and aggregation
interval, and need interpolation and other time-series-oriented functions. The
business wants configurable memory and magnetic retention tiers and a serverless
managed database that scales ingestion and storage without the team operating
database servers. The workload is not a graph, message-broker, or directory
service problem.

Which service is the best fit?

A. Use Amazon Timestream with dimensions, measures, and retention policies
designed for the telemetry query and lifecycle requirements.

B. Model every sensor reading as a Neptune graph vertex and use graph traversals
as the primary time-window aggregation engine.

C. Retain every measurement permanently in Amazon MQ queues and execute SQL
time-series functions against broker acknowledgements.

D. Store the sensor measurements as Active Directory users and use LDAP group
membership as the time-series retention tier.

### 72 - Choose TWO

One S3 data lake contains thousands of application prefixes consumed by many
accounts. The bucket policy has become too large and operationally risky.
Architects need independently named access endpoints with policies scoped to
consumer roles and approved prefixes, while retaining centralized ownership of
the bucket. Separately, an organization-level resource-side guardrail must
prevent supported resource policies from granting access outside the
organization. Neither control should be described as granting access by
itself beyond its policy intersection.

Which TWO controls best meet these requirements?

A. Create one IAM user and long-term access key for every application to share,
and use object-name secrecy instead of resource policies.

B. Create S3 Access Points for the consumer boundaries, with access-point and
bucket-policy interactions scoped to approved prefixes, VPCs where required,
and consumer roles.

C. Remove IAM and resource policies and rely on unpredictable prefixes as the
only authorization boundary for the shared dataset.

D. Apply a tested Organizations resource control policy that denies unsupported
external access to the supported member-account resources at the appropriate
scope.

E. Make the bucket public, disable S3 Block Public Access, and use unguessable
prefixes as the central and consumer authorization controls.

### 73

A new order workflow coordinates human approval, inventory reservation, payment,
and fulfillment and can remain open for several days. Operations requires a
durable per-step execution history and exactly traceable retries. If a producer
retries `StartExecution` with the same execution name and identical input while
the workflow is still running, the orchestration service should not start a
second execution. Individual workflow transitions are not high-volume
streaming events that can tolerate only a short execution history.

Which service type is the best fit?

A. Use Step Functions Express Workflows and rely on their high-volume at-least-
once mode and log delivery as the multi-day idempotent execution record.

B. Use a Step Functions Standard Workflow with a stable execution name,
service-managed durable state, callback support where needed, and execution
history.

C. Use an SQS Standard queue with a zero-second visibility timeout and represent
every workflow step as a competing delivery with no durable orchestration
state.

D. Encode the workflow state in a CloudFront cache behavior and use cache-key
uniqueness as the multi-day execution history.

### 74

A public application runs behind Application Load Balancers in three AWS
Regions. The business wants DNS to select the Region that currently provides
the lowest network latency from the resolver's location. The public name is the
hosted zone apex, so an ordinary CNAME record is not allowed there. The design
should use native alias targets for the load balancers, preserve AWS-managed
target IP changes, and avoid publishing private addresses or using a private
hosted zone for internet users.

Which Route 53 record design is most appropriate?

A. Create a CNAME record at the apex of a private hosted zone and return the
regional load balancer DNS names only to internet resolvers.

B. Create geolocation records containing fixed load balancer IP addresses and
assume geography always represents the lowest observed network latency.

C. Create latency-based alias records at the public hosted-zone apex, each
targeting the Application Load Balancer in its Region with appropriate health
evaluation.

D. Create one multivalue answer record at the public apex containing the private
IP addresses of all EC2 targets behind all three load balancers.

### 75 - Choose THREE

An enterprise creates dozens of AWS accounts each month. Requesters must choose
approved account metadata, and each account must be enrolled into the governed
landing zone and correct OU through a repeatable vending workflow. Baseline
networking, IAM, and logging stacks must deploy automatically to current and
future accounts in selected OUs and Regions. A common package of AWS Config
rules and remediation guidance must evaluate the accounts without administrators
copying individual rules manually after every account creation.

Which THREE capabilities best meet these requirements?

A. Use AWS Control Tower Account Factory or Account Factory for Terraform for
governed account requests, provisioning, enrollment, and intended OU placement.

B. Email the root credentials of every new account to the platform team and
manually recreate the landing-zone controls through each console.

C. Use service-managed CloudFormation StackSets with Organizations integration
and OU targeting to deploy and update approved baseline stacks.

D. Ask account owners to submit quarterly console screenshots as the automated
configuration-compliance and remediation system.

E. Deploy organization conformance packs for the approved bundle of AWS Config
rules and remediation guidance across the intended accounts and Regions.

F. Create security groups in the management account and use their ingress rules
to move new accounts into organizational units.

---

## Frozen Submission

The following learner submission was frozen before marking. Wall-clock elapsed
time was 190 minutes. The learner reported approximately 10 minutes of short
interruptions near the end, so estimated active time was approximately 180
minutes; the exact interruption adjustment is learner-reported rather than
independently measured.

```text
Attempt date: Aug 12, 2026
Attempt number: 1
Start: 20:05
End: 23:15
Elapsed: 3:10
Interruption note: Approximately 10 minutes of short interruptions near the end
Estimated active time: Approximately 180 minutes
Uncertain: 4, 5, 6, 12, 15, 20, 26, 40, 56, 57, 63

1: A
2: BD
3: A
4: ACE
5: B
6: D
7: AE
8: C
9: CD
10: B
11: D
12: ACE
13: D
14: A
15: BD
16: A
17: A
18: BE
19: B
20: BDF
21: C
22: A
23: CE
24: B
25: D
26: AE
27: C
28: D
29: AB
30: C
31: AD
32: B
33: C
34: CE
35: A
36: A
37: BE
38: D
39: C
40: ACF
41: C
42: BD
43: D
44: A
45: E
46: B
47: B
48: AD
49: D
50: B
51: BE
52: D
53: A
54: BE
55: C
56: A
57: AD
58: C
59: B
60: ADF
61: D
62: B
63: CE
64: A
65: B
66: ACF
67: D
68: C
69: BE
70: C
71: A
72: BD
73: B
74: C
75: ACE

Submission status: FROZEN
```
