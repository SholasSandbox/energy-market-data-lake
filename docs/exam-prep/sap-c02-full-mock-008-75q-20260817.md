# AWS Certified Solutions Architect - Professional (SAP-C02)

## Full Mock 008 - 75 Questions

<!-- markdownlint-disable MD013 MD060 -->

**Created:** 2026-08-17<br>
**Attempted:** 2026-08-18<br>
**Mode:** Fresh closed-book, complexity-calibrated full simulation<br>
**Difficulty target:** SAP-C02 professional-level multi-constraint reasoning<br>
**Time limit:** 180 minutes<br>
**Question types:** 48 single-response and 27 multiple-response questions<br>
**Submission status:** FROZEN; independently marked

Multiple-response questions receive credit only when every required option and
no additional option is selected.

This original practice examination follows the current SAP-C02 domain
weightings and uses multi-constraint scenarios, plausible partial-fit
distractors, balanced answer positions, and exact-match composition traps. It
contains no proprietary AWS assessment wording. It is not an AWS examination
and does not reproduce live exam questions. The answer key, domain labels,
rationales, transfer labels, and scoring hints are deliberately excluded from
this document.

## Attempt rules

1. Keep lessons, reviews, answer keys, documentation, search, and AI assistance
   closed until the submission is frozen.
2. Select one answer unless the question says **Choose TWO** or **Choose
   THREE**.
3. Record start and end times, interruptions, and uncertain question numbers.
4. Stop when the 180-minute timer expires and guess rather than leave an item
   blank.
5. Complete the answer template and change `Submission status` to `FROZEN`
   before requesting marking. Do not revise a frozen response.
6. For multiple-response items, perform one final requirement-to-selection
   check before submission if time remains.

---

## Questions

### 1

A company uses AWS Organizations with separate production and development OUs.
An administrator in a production member account has an identity policy that
allows all AWS actions. An SCP attached to the production OU explicitly denies
disabling AWS CloudTrail. The administrator attempts to stop an organization
trail.

What is the expected result?

A. The request succeeds because an identity policy in the member account takes
precedence over an SCP.

B. The request is denied because the SCP limits the maximum permissions
available in the member account.

C. The request succeeds if the administrator assumes a role with a permissions
boundary.

D. The request is denied only if the organization trail is stored in the same
member account.

### 2 - Choose TWO

A regulated organization wants centralized evidence from every current and
future member account. API activity must be retained in a dedicated log archive
account, and security teams must query configuration state across the
organization without signing in to each workload account.

Which TWO actions best meet these requirements?

A. Create independent trails in each account and allow workload administrators
to choose whether to keep them running.

B. Create a multi-Region organization CloudTrail trail that delivers to a
protected S3 bucket in the log archive account.

C. Store configuration snapshots only on the instance volumes of configuration
recorders.

D. Use VPC Flow Logs as the authoritative record of all AWS control-plane API
activity.

E. Configure AWS Config recording and organization aggregation through an
approved delegated administrator.

### 3

Two companies are merging. Their VPC CIDR ranges overlap, but a payment service
in one company must be consumed privately by applications in the other. The
consumer must not gain general network reachability to the provider VPC.

Which solution best meets the requirements?

A. Establish VPC peering and add more-specific routes around the overlapping
CIDR ranges.

B. Attach both VPCs to one transit gateway and propagate every route into a
shared route table.

C. Connect the VPCs through a Site-to-Site VPN and use NAT gateways on both
sides.

D. Publish the payment service through a Network Load Balancer endpoint service
and consume it with AWS PrivateLink interface endpoints.

### 4

A global order platform uses an Aurora-compatible relational database. Reads
must be served locally in a second Region, cross-Region replication lag must
typically remain below one second, and the business requires a managed regional
disaster-recovery promotion path.

Which solution is the best fit?

A. Copy manual DB snapshots to the second Region every hour.

B. Run two unrelated Multi-AZ DB clusters and synchronize them with nightly
exports.

C. Use an RDS read replica in another Availability Zone of the primary Region.

D. Use an Aurora global database with a secondary cluster in the second Region.

### 5 - Choose THREE

A public order API receives unpredictable bursts. It must acknowledge requests
only after durable acceptance, process work asynchronously, suppress duplicate
client retries before applying side effects, and isolate messages that
repeatedly fail. The team does not want to operate brokers.

Which THREE actions best satisfy the requirements?

A. Integrate API Gateway with Amazon SQS by using a request mapping and an IAM
role.

B. Set the queue visibility timeout to zero so all consumers race for each
message.

C. Require a stable idempotency key and enforce it in a durable state store
around the side effect.

D. Store the only accepted copy of each request in API Gateway access logs.

E. Process the entire workflow synchronously inside an API Gateway integration
timeout.

F. Configure a dead-letter queue and an appropriate redrive policy.

### 6

A tightly coupled scientific simulation runs across many EC2 instances. It
requires low-latency, high-bandwidth node-to-node communication and direct
support for MPI-style traffic. The instances are in one Availability Zone.

Which feature should the solutions architect select?

A. Elastic Fabric Adapter

B. Elastic Network Adapter without a placement strategy

C. AWS PrivateLink

D. A Network Load Balancer

### 7

A migration team must collect detailed process inventory, running software, and
TCP communication dependencies from physical Linux and Windows servers before
grouping them into migration waves.

Which approach best provides the required host-level evidence?

A. Enable VPC Flow Logs in the target AWS account.

B. Install the AWS Application Discovery Agent on the source servers.

C. Use Migration Hub only, without connecting any discovery tool.

D. Create an AWS DataSync task for every source file system.

### 8 - Choose TWO

An on-premises analytics system must access Amazon S3 privately over Direct
Connect. The design must not traverse the public internet, and applications
must resolve the private endpoint name from on premises.

Which TWO components are required?

A. An S3 gateway endpoint, because gateway endpoints can be extended directly
through Direct Connect.

B. An S3 interface VPC endpoint reachable from the Direct Connect path.

C. An internet gateway with a route advertised to the on-premises network.

D. An inbound Route 53 Resolver path and DNS forwarding that resolve the S3
interface endpoint names from the on-premises network.

E. A NAT gateway in every Availability Zone used by the analytics system.

### 9

A developer assumes a role in a member account. The role's identity policy
allows `s3:PutObject`, and the bucket policy also allows that role. An SCP
attached to the account explicitly denies `s3:PutObject` for the destination
Region.

What will happen?

A. The request succeeds because both an identity policy and a resource policy
allow it.

B. The request succeeds if the bucket uses an AWS managed KMS key.

C. The request is denied because the SCP's explicit deny applies.

D. The request is denied only when the role also has a permissions boundary.

### 10

A data lake contains objects with changing and unpredictable access patterns.
The operations team wants automatic storage-cost optimization without retrieval
fees for frequently accessed data or manually defining when each object becomes
cold.

Which S3 storage class is the best fit?

A. S3 Intelligent-Tiering

B. S3 One Zone-Infrequent Access

C. S3 Glacier Deep Archive

D. S3 Express One Zone

### 11 - Choose TWO

A central event-bus account must receive selected Amazon EventBridge events
from application accounts and route them to a Step Functions state machine in
the central account.

Which TWO authorization components are required for the complete path?

A. A security group on the event bus that allows inbound TCP 443 from every
application VPC.

B. An event-bus resource policy that allows the approved source accounts to
call `events:PutEvents`.

C. A Lambda resource policy on every event producer.

D. An S3 bucket policy that grants EventBridge access to the default event bus.

E. A target role that EventBridge can assume to start the approved state
machine execution.

### 12

A company is migrating an Oracle database to Aurora PostgreSQL. It needs schema
conversion and ongoing replication with minimal downtime until cutover.

Which solution best meets the requirements?

A. Use AWS DataSync for schema conversion and block-level replication.

B. Use AWS Application Migration Service to convert the database engine during
test launch.

C. Use AWS Schema Conversion Tool for incompatible schema objects and AWS DMS
full load plus change data capture.

D. Export the Oracle database to CSV once and import it after a week-long
outage.

### 13

A company needs a managed shared file system for Windows workloads across
multiple Availability Zones. Clients require SMB, native Microsoft Active
Directory integration, Windows ACLs, and support for shadow copies.

Which service should the company use?

A. Amazon FSx for Windows File Server

B. Amazon EFS

C. Amazon FSx for Lustre

D. S3 File Gateway

### 14 - Choose THREE

An enterprise wants an organization-wide security-operations model. It needs a
central record of API activity, delegated threat-detection administration, and
central aggregation and management of security findings. The log archive
account must remain storage-oriented.

Which THREE actions best meet the requirements?

A. Deliver an organization CloudTrail trail to the protected log archive
account.

B. Give every workload administrator permission to stop the organization
trail during deployments.

C. Place all delegated security services in the Organizations management
account and use the root user for daily operations.

D. Designate a security tooling account as the GuardDuty delegated
administrator.

E. Store the only security findings as local text files on EC2 instances.

F. Configure centralized Security Hub administration and aggregation in the
security tooling account.

### 15

An application runs on EC2 Auto Scaling behind an Application Load Balancer.
Traffic per instance is the main driver of CPU and latency. The team wants the
group to add capacity as the average request load per target rises.

Which scaling approach is most appropriate?

A. Step scaling based only on the total number of registered targets.

B. Scheduled scaling once per month.

C. Target tracking using `ALBRequestCountPerTarget`.

D. Predictive scaling based on the number of IAM users.

### 16

A global multiplayer service exposes TCP and UDP endpoints from healthy
regional deployments. Clients require static anycast IP addresses, rapid
endpoint failover, and routing over the AWS global network.

Which service best meets these requirements?

A. Amazon CloudFront

B. Route 53 private hosted zones

C. AWS Transit Gateway

D. AWS Global Accelerator

### 17 - Choose TWO

A DynamoDB table receives writes for a small number of extremely popular
customers. Each customer's items currently use one partition key, creating hot
partitions. Reads must still return the customer's complete time-ordered
history.

Which TWO changes address the access-pattern problem?

A. Add a calculated or random shard suffix to distribute each customer's write
keys.

B. Change from provisioned capacity to on-demand capacity and keep the same hot
partition key as the only design change.

C. Add a strongly consistent global secondary index spanning all shards.

D. Query the relevant shard keys and merge and order the results in the
application.

E. Replace the partition key with one constant value for the entire table.

### 18

A company must transfer 80 TB from a data centre to AWS within two weeks. Its
available WAN connection cannot complete the transfer in time, and the data can
be exported to an encrypted physical appliance.

Which service is the most appropriate?

A. AWS Storage Gateway Tape Gateway

B. AWS Snowball Edge

C. Amazon SQS Extended Client Library

D. AWS Direct Connect hosted connection provisioned after the deadline

### 19

A central security team wants an S3 bucket to accept writes only from accounts
in its AWS Organization and only through an approved VPC endpoint. Existing
identity policies must not bypass either data-perimeter condition.

Which design best meets the requirement?

A. Use object tags as the only authorization control.

B. Allow every principal and rely on CloudTrail to detect unwanted writes
afterward.

C. Add bucket-policy explicit denies for requests outside the approved
organization and outside the approved VPC endpoint, with documented service
exceptions where required.

D. Make the bucket public and restrict access with a network ACL.

### 20 - Choose TWO

A company serves static media and a write-heavy regional API under one public
brand. Static `GET` requests can fail over between S3 origins. API `POST`
requests must fail over to a standby Region when the primary application is
unhealthy.

Which TWO actions satisfy the complete requirement?

A. Use a CloudFront origin group for every API method, including `POST`.

B. Use a CloudFront origin group for eligible read requests to the static
origins.

C. Add Lambda@Edge to make built-in CloudFront origin failover support all
write methods.

D. Put both regional API deployments behind one NAT gateway.

E. Use an appropriate regional application failover mechanism, such as Global
Accelerator endpoint health or Route 53 routing, for the write API.

### 21

A Kinesis Data Streams application has several independent consumers. One slow
consumer is contending for the shared per-shard read throughput and increasing
its own processing delay. Other consumers must continue receiving records with
dedicated throughput.

Which feature should be used?

A. SQS long polling

B. DynamoDB Accelerator

C. Kinesis Data Firehose buffering

D. Kinesis enhanced fan-out

### 22

Amazon ECS tasks in private subnets must retrieve secrets at startup without
using a NAT gateway. The secret must not be embedded in the image, and each task
should receive only its runtime permissions.

Which solution best meets the requirements?

A. Create the required interface VPC endpoints and grant the ECS task role
permission to retrieve only the approved secret.

B. Put the secret in the task definition as plaintext and use the execution
role for all application calls.

C. Assign a public IP address to every task and restrict egress with a network
ACL.

D. Store the secret in an unauthenticated S3 website endpoint.

### 23 - Choose TWO

Spoke VPC traffic is inspected by stateful appliances behind a Gateway Load
Balancer in a central inspection VPC. During failover, return traffic sometimes
uses a different appliance path and established flows are dropped.

Which TWO changes are required?

A. Enable source/destination checks on the firewall appliance interfaces.

B. Replace the Gateway Load Balancer with an Application Load Balancer.

C. Enable appliance mode on the transit gateway attachment for the inspection
VPC.

D. Enable appliance mode on every spoke attachment and remove central route
tables.

E. Configure transit gateway route tables so both directions traverse the
inspection attachment.

### 24

A company must move hundreds of VMware virtual machines to EC2 quickly with
minimal operating-system and application changes. It needs continuous
block-level replication, nondisruptive test launches, and a controlled cutover.

Which service is the best fit?

A. AWS Application Migration Service

B. AWS Database Migration Service

C. AWS DataSync

D. AWS App Runner

### 25

An enterprise uses an external identity provider for employees who need access
to many AWS accounts. Administrators want group-based assignments, reusable
permission sets, and short-lived role sessions without creating IAM users in
every account.

Which service should the enterprise use?

A. Amazon Cognito user pools

B. AWS IAM Identity Center

C. AWS Directory Service Simple AD with local IAM access keys

D. Amazon Verified Permissions as the workforce sign-in portal

### 26 - Choose THREE

A ransomware-resilience design requires backups that a compromised workload
administrator cannot delete, a copy outside the workload account, and recurring
proof that recovery points can be restored.

Which THREE actions best meet these requirements?

A. Keep the only recovery points in the workload account with the same
administrative roles as production.

B. Copy backups into an isolated backup vault in a separate backup account.

C. Disable all backup lifecycle controls so recovery points never transition.

D. Apply an appropriate AWS Backup Vault Lock mode after validating the
retention design.

E. Treat a successful backup job as proof that the workload can be recovered.

F. Run scheduled restore testing and validate the restored workload or data.

### 27

An IoT platform needs an ordered event stream per device, multiple independent
consumers, replay for several days, and scaling through partition-key design.

Which service best fits the requirement?

A. Amazon SNS standard topics only

B. Amazon SQS FIFO with one destructive consumer

C. Amazon Kinesis Data Streams

D. Amazon EventBridge Scheduler

### 28

A Lambda function experiences large concurrency bursts. Each execution opens a
new connection to the same RDS database, exhausting database connections even
though the database CPU remains moderate.

Which change most directly addresses the connection problem?

A. Increase the Lambda deployment package size.

B. Put the function in more Availability Zones.

C. Replace the database with an S3 bucket.

D. Place Amazon RDS Proxy between the function and the database.

### 29 - Choose TWO

An S3 replication rule copies objects encrypted with a customer managed KMS key
to a bucket in another account. Unencrypted objects replicate, but encrypted
objects fail. The destination must encrypt replicas with its own customer
managed key.

Which TWO changes are required?

A. Configure the replication rule to include KMS-encrypted objects and specify
the destination KMS key.

B. Make the source objects public before replication.

C. Replace both keys with AWS managed keys because customer managed keys cannot
be used across accounts.

D. Enable S3 Transfer Acceleration on both buckets.

E. Grant the replication role and relevant key policies the required decrypt
permission on the source key and encrypt permission on the destination key.

### 30

A global migration programme sends discovery and migration status from several
AWS Regions to AWS Migration Hub. Operators cannot see expected records after
signing in to a different Region.

What should they do first?

A. Create an S3 gateway endpoint in every source Region.

B. Open the configured Migration Hub home Region and verify that the tools are
connected there.

C. Run AWS DataSync to copy the Migration Hub control-plane database.

D. Enable cross-Region read replicas for Migration Hub.

### 31

A role has a permissions boundary that allows `dynamodb:GetItem`, but its
identity policy contains no DynamoDB permissions. There is no applicable
resource policy.

Can the role call `GetItem`?

A. Yes. A permissions boundary grants every action that it lists.

B. Yes, if the table uses on-demand capacity.

C. No. A permissions boundary limits permissions but does not grant them.

D. No, because permissions boundaries can be used only with IAM users.

### 32 - Choose TWO

A multi-tenant HTTP API uses Amazon Cognito user pools. The API must validate
signed access tokens and ensure that a caller can operate only on resources for
the tenant identified by an approved token claim.

Which TWO controls best satisfy the requirement?

A. Configure API Gateway JWT or Cognito authorization to validate the issuer,
audience, signature, and required scopes.

B. Trust a client-supplied tenant ID in the request body without comparing it
to the token.

C. Give the authorizer administrator access so it can bypass tenant checks.

D. Enforce the validated tenant claim in the application's data-access key or
authorization decision.

E. Publish all tenant objects and rely on unpredictable identifiers.

### 33

Thousands of Linux instances across multiple Availability Zones require
concurrent access to one elastic, managed, POSIX-compatible file system.

Which service is the best fit?

A. Amazon EBS Multi-Attach for every instance type

B. Amazon EFS Regional

C. Amazon FSx for Windows File Server

D. S3 Glacier Flexible Retrieval

### 34

A stateless web tier has a stable baseline load and large fault-tolerant bursts.
The company wants to reduce compute cost while retaining capacity for the
baseline and tolerating interruption of burst workers.

Which purchasing strategy is most appropriate?

A. Run the entire fleet as On-Demand Instances permanently.

B. Buy Dedicated Hosts for all possible peak capacity.

C. Use Spot Instances for the non-interruptible baseline and Capacity
Reservations for disposable workers.

D. Cover the predictable baseline with a Savings Plan and use diversified Spot
capacity for interruption-tolerant bursts.

### 35 - Choose TWO

A company deploys a new Amazon ECS service revision. It needs blue/green traffic
shifting, a test path before production traffic moves, CloudWatch-alarm-driven
rollback, and rapid restoration of the previous task set.

Which TWO components are central to the design?

A. AWS Systems Manager Patch Manager for application traffic shifting

B. AWS CodeDeploy configured for an ECS blue/green deployment

C. One Classic Load Balancer with no alternate target group

D. A monthly AWS Backup job for the container image

E. Application Load Balancer production and test listeners or listener rules
with the required target groups

### 36

An on-premises application must continue using an NFS file interface while
frequently accessed data is cached locally and durable objects are stored in
Amazon S3. The company wants a managed hybrid storage gateway.

Which service should it use?

A. Volume Gateway in stored-volume mode

B. Amazon EFS mounted directly over the public internet

C. Amazon S3 File Gateway

D. AWS Snowcone without DataSync

### 37

A bank requires exclusive control of single-tenant hardware security modules,
direct use of standard cryptographic interfaces, and the ability to manage its
own HSM users and key material.

Which service best meets the requirement?

A. AWS CloudHSM

B. AWS Secrets Manager

C. AWS Certificate Manager public certificates

D. An AWS managed KMS key

### 38 - Choose THREE

An organization is designing tamper-resistant control-plane audit logging.
Logs must cover all Regions and member accounts, be encrypted in a dedicated
archive, and be protected from workload administrators who try to stop logging
or delete the archive.

Which THREE actions best meet the requirements?

A. Create a multi-Region organization CloudTrail trail.

B. Store the only logs in each workload account's default CloudWatch log group.

C. Use a protected cross-account S3 destination and customer managed KMS key
with the required CloudTrail policies.

D. Give application administrators permission to call `cloudtrail:StopLogging`
during maintenance.

E. Disable S3 versioning and lifecycle controls on the archive.

F. Apply narrowly tested preventive controls that deny unauthorized trail
disablement and archive deletion.

### 39

A SaaS application needs a relational database with one writer Region and
low-latency read-only clusters in other Regions. The disaster-recovery design
requires managed cross-Region replication and promotion.

Which solution best meets the requirement?

A. One RDS Multi-AZ deployment stretched across Regions

B. Amazon ElastiCache Global Datastore as the system of record

C. Nightly database exports to S3 only

D. Aurora global database

### 40

A central operations account must view CloudWatch metrics, logs, and traces
from many source accounts without copying all telemetry into one S3 bucket.
Teams want a managed account-linking model for cross-account observability.

Which service should be used?

A. AWS CloudTrail Lake

B. CloudWatch cross-account observability using Observability Access Manager

C. AWS Config conformance packs

D. Amazon Inspector delegated administration

### 41 - Choose TWO

An SQS consumer processes jobs that sometimes exceed the initial visibility
timeout. Duplicate deliveries can occur, and the external side effect is not
inherently idempotent.

Which TWO changes make processing safer?

A. Extend message visibility while a valid long-running job is still in
progress.

B. Delete the message before starting the side effect.

C. Use a durable idempotency record or conditional write keyed by the logical
request before applying the side effect.

D. Change to a standard queue and assume exactly-once delivery.

E. Set the visibility timeout permanently to zero.

### 42

A company wants to assess and modernize a large mainframe estate. It needs an
AWS service that applies automated analysis and transformation capabilities
specific to mainframe modernization while retaining human review of outputs.

Which service is the best fit?

A. AWS Transform for mainframe

B. AWS Application Migration Service

C. AWS DataSync

D. Amazon AppFlow

### 43

A networking team owns a transit gateway in a central network account. VPCs in
other accounts in the same AWS Organization must attach to it without moving
ownership of the transit gateway.

Which service should the team use to share the transit gateway?

A. Amazon Cognito

B. AWS App Mesh

C. AWS Direct Connect SiteLink

D. AWS Resource Access Manager

### 44 - Choose TWO

Applications in several VPCs must resolve an on-premises private domain. The
on-premises DNS servers must also resolve Route 53 private hosted-zone records.
Connectivity already exists through Direct Connect.

Which TWO components are required?

A. Public hosted zones containing copies of every private record

B. Route 53 Resolver outbound endpoints and forwarding rules for the
on-premises domain

C. NAT gateways so DNS queries can traverse the public internet

D. An internet gateway in each application VPC

E. Route 53 Resolver inbound endpoints queried by the on-premises DNS servers

### 45

Users around the world upload large objects directly to one S3 bucket. The
company wants to accelerate long-distance transfers by routing traffic through
the AWS global edge network without deploying proxy servers.

Which feature should it enable?

A. S3 Object Lambda

B. S3 Inventory

C. S3 Transfer Acceleration

D. S3 Batch Operations

### 46

A compliance archive requires write-once-read-many retention. During the
retention period, no user, including the root user, must be able to shorten the
retention or delete protected object versions.

Which S3 feature best meets the requirement?

A. S3 Object Lock in compliance mode

B. S3 Object Lock in governance mode with bypass permission

C. An S3 lifecycle rule that immediately expires current versions

D. S3 Versioning alone

### 47 - Choose TWO

A producer account manages governed Glue Data Catalog tables with AWS Lake
Formation. An analytics account must query selected shared tables without
receiving broad access to the producer's S3 data lake.

Which TWO actions are part of the appropriate cross-account design?

A. Make the producer's data bucket public.

B. Grant the approved external account or principals Lake Formation
permissions on the selected resources.

C. Copy the producer account's administrator access keys into the analytics
account.

D. Accept or use the shared catalog resources in the consumer account and
create resource links where required for local discovery and queries.

E. Disable Lake Formation permissions and rely only on table names.

### 48

A company is moving a large MySQL database to Amazon RDS for MySQL. The source
must remain online, and the cutover window must be only a few minutes. No schema
engine conversion is required.

Which approach best meets the requirement?

A. Use AWS Snowball Edge as the ongoing change-data-capture engine.

B. Rehost the database server with AWS Application Migration Service and keep
the operating system permanently.

C. Use AWS DMS full load plus ongoing change data capture, then cut over after
replication lag is acceptable.

D. Export a snapshot after shutting down the source for several days.

### 49

An organization wants new accounts to receive standardized preventive and
detective guardrails, centralized logging, and governed account vending. Teams
must not manually assemble these controls for each account.

Which solution best meets the requirement?

A. One shared production account with tags as the only isolation boundary

B. AWS Control Tower with governed OUs and Account Factory

C. IAM users created manually in every new account

D. A CloudFormation stack run from each developer laptop without central
oversight

### 50 - Choose THREE

Images uploaded to S3 start an asynchronous processing workflow. The workflow
must branch by file type, retry transient steps, preserve execution state, and
isolate events that cannot be processed after repeated failures.

Which THREE actions best satisfy the requirements?

A. Route S3 object events through Amazon EventBridge using a narrowly scoped
rule.

B. Run every processing step synchronously inside the S3 upload request.

C. Store the only workflow state in Lambda `/tmp` directories.

D. Use AWS Step Functions for branching, retries, catches, and durable workflow
state.

E. Configure infinite retries with no terminal handling.

F. Route terminal failures to an explicit failure or reconciliation path, such
as an SQS dead-letter queue or review queue.

### 51

Several teams run services in VPCs with overlapping CIDR ranges. They want
service-to-service connectivity, centralized authorization policies, service
discovery, and no requirement to expose complete VPC routing between teams.

Which service is the best fit?

A. Amazon VPC Lattice

B. VPC peering with transitive routing enabled

C. An internet gateway shared through AWS RAM

D. Route 53 Resolver DNS Firewall alone

### 52

An RDS snapshot is encrypted with a customer managed KMS key. A separate AWS
account must restore its own copy of the database without receiving broad
administrator access to the source account.

Which approach best meets the requirement?

A. Share the encrypted snapshot but do not grant any use of the KMS key.

B. Share the snapshot and grant the destination account the required use of
the customer managed key; have the destination copy the snapshot with its own
key before restoring.

C. Make the snapshot public because encryption prevents unauthorized restores.

D. Replace the snapshot with an unencrypted copy shared to all accounts.

### 53 - Choose TWO

A batch-processing workload requires a custom operating-system image and can
tolerate job interruption. The team wants AWS to schedule jobs, retry them, and
obtain low-cost compute across multiple instance families.

Which TWO choices best meet the requirements?

A. Use AWS Batch only with Fargate and attempt to install kernel modules at
runtime.

B. Run one fixed On-Demand instance manually and disable job retries.

C. Use an AWS Batch EC2 compute environment that launches the required custom
AMI.

D. Use Lambda layers to replace the required operating-system image.

E. Configure diversified Spot capacity and suitable allocation and retry
settings for the interruptible jobs.

### 54

A company must copy millions of files from an on-premises NFS server to Amazon
S3 over its existing network. It needs incremental transfers, scheduling,
verification, encryption in transit, and managed throughput controls.

Which service should it use?

A. AWS DataSync

B. AWS Database Migration Service

C. AWS Application Migration Service

D. Amazon Kinesis Data Streams

### 55

An organization wants a member security account to administer Security Hub for
member accounts while the Organizations management account avoids routine
security operations.

Which action is most appropriate?

A. Share the management-account root credentials with the security team.

B. Create unrelated Security Hub configurations independently in every account
with no organization integration.

C. Move the management account into the Security OU and restrict it with an
SCP.

D. Designate the eligible security account as the Security Hub delegated
administrator and configure central management.

### 56 - Choose TWO

A company requires private hybrid connectivity to AWS with predictable
bandwidth. The design must tolerate a Direct Connect device failure and a full
Direct Connect location failure. A secondary encrypted path is also required
for recovery while private circuits are unavailable.

Which TWO actions best meet the requirements?

A. Create two private VIFs on one Direct Connect connection in one location.

B. Provision Direct Connect connections on separate devices in separate Direct
Connect locations.

C. Use one internet circuit with two VPN tunnels and describe it as location
diversity.

D. Disable dynamic routing so route changes cannot occur during a failure.

E. Configure a Site-to-Site VPN backup path with appropriate dynamic routing
and preference.

### 57

A globally distributed application requires strongly consistent reads and
writes across supported Regions. The table does not require TTL, and the team
can select only Region combinations supported by the service.

Which DynamoDB capability is the best fit?

A. A default multi-Region eventual consistency global table with eventually
consistent reads

B. DynamoDB Accelerator deployed in every Region without a global table

C. A multi-Region strong consistency global table

D. A global secondary index queried with `ConsistentRead=true`

### 58

A company deploys Lambda versions gradually. It wants 10% of traffic sent to a
new version first, automatic rollback when a CloudWatch alarm breaches, and no
custom traffic-shifting controller.

Which solution best meets the requirement?

A. Use AWS CodeDeploy with a Lambda canary deployment configuration and alarms.

B. Update the unqualified function ARN in place and delete the old version
immediately.

C. Use Systems Manager Patch Manager to route Lambda invocations.

D. Put both versions in one Auto Scaling group.

### 59 - Choose TWO

A workload role in Account A must encrypt objects with a customer managed KMS
key owned by Account B. The S3 and network paths are already correct.

Which TWO policy layers are required to authorize the cross-account KMS use?

A. A network ACL in Account B that names the IAM role ARN

B. A key policy in Account B that permits the approved external principal or
delegates permission to Account A

C. An SCP that grants `kms:Encrypt` to the role

D. An identity policy on the workload role in Account A that allows the
required KMS action on the key

E. A public KMS endpoint policy allowing anonymous encryption

### 60

A company plans to migrate Microsoft SQL Server databases to Aurora PostgreSQL.
Before data migration, the team must assess conversion complexity and convert
schema and code objects that are not directly compatible.

Which tool should it use for this task?

A. AWS DataSync

B. AWS Schema Conversion Tool

C. AWS Application Migration Service

D. Amazon S3 Batch Operations

### 61

A large organization wants centrally managed AWS Network Firewall policies
deployed consistently across accounts and VPCs, with automatic policy
application based on organization scope.

Which service should it use?

A. Amazon Detective

B. AWS Artifact

C. AWS Firewall Manager with an appropriate delegated administrator

D. Amazon Macie classification jobs

### 62 - Choose THREE

Finance wants organization-wide cloud cost governance. Teams need consistent
cost ownership metadata, proactive alerts before forecasted spend exceeds
approved limits, and a queryable detailed cost-and-usage dataset for analysis.

Which THREE actions best meet the requirements?

A. Define required cost-allocation tags and activate the selected tags in the
billing account.

B. Use security groups to block resources that do not have cost tags.

C. Treat a tag policy as if it granted and denied all IAM permissions.

D. Create AWS Budgets with actual and forecasted notifications or approved
actions.

E. Use only the monthly credit-card total as the detailed allocation dataset.

F. Deliver a Cost and Usage Report or equivalent detailed billing export to a
governed analytics location.

### 63

A read-heavy application uses DynamoDB and repeatedly reads the same eventually
consistent items and query results. The team can update the client library and
wants a managed, in-memory, DynamoDB-compatible cache with microsecond response
times.

Which service is the best fit?

A. Amazon MemoryDB for Redis as a transparent DynamoDB endpoint

B. DynamoDB Accelerator

C. Amazon OpenSearch Service

D. Amazon RDS Proxy

### 64

A serverless request crosses API Gateway, Lambda, and several downstream AWS
services. Operators need a trace showing where latency and errors occur along
the request path.

Which service best provides this view?

A. AWS Config

B. AWS CloudTrail event history

C. S3 Storage Lens

D. AWS X-Ray

### 65 - Choose TWO

Compliance data in S3 must remain immediately retrievable for its first 90
days. After that, it is rarely accessed and may use an archival tier with a
longer restore time. Access during the first 90 days is unpredictable.

Which TWO lifecycle choices best fit the requirement?

A. Use S3 Intelligent-Tiering during the period of unpredictable online
access.

B. Move objects immediately to S3 Glacier Deep Archive on creation.

C. Expire all current versions at day 90.

D. Use S3 One Zone-IA for the only compliance copy.

E. Transition eligible objects to an archival class after the required online
period.

### 66

During application portfolio assessment, a team confirms that an old reporting
application has no users, no regulatory retention need, and duplicates a
capability already provided by another system.

Which migration strategy is most appropriate?

A. Refactor

B. Repurchase

C. Retire

D. Relocate

### 67

A platform team must deploy the same CloudFormation baseline to selected OUs
and automatically include new accounts added to those OUs. The team wants AWS
Organizations integration rather than maintaining individual account roles.

Which solution best meets the requirement?

A. CloudFormation change sets created manually in each account

B. CloudFormation StackSets with service-managed permissions and OU targets

C. One nested stack in the Organizations management account

D. AWS Service Catalog portfolios shared by public S3 bucket

### 68 - Choose TWO

An on-premises application must invoke an API Gateway REST API privately over
Direct Connect. The API must not be callable from the public internet and
should accept requests only through approved interface endpoints.

Which TWO controls are required?

A. Configure the REST API as a private API and create the required execute-api
interface VPC endpoint.

B. Attach an internet gateway to the API Gateway service.

C. Publish an edge-optimized public endpoint and hide its URL.

D. Apply an API resource policy that restricts invocation to the approved VPC
endpoint or endpoints.

E. Put a NAT gateway in front of the private API.

### 69

A company operates Apache Kafka clusters on premises. It wants a managed AWS
Kafka service while preserving Kafka APIs, client compatibility, partitions,
and consumer-group behaviour with minimal application change.

Which service should it choose?

A. Amazon SQS

B. Amazon Kinesis Data Firehose

C. Amazon MQ for ActiveMQ

D. Amazon Managed Streaming for Apache Kafka

### 70 - Choose TWO

A company must receive business documents from a partner by using AS2 and
store the payloads in Amazon S3. It wants a managed AWS file-transfer service
with explicit local and partner identities and a relationship that controls
inbound exchanges.

Which TWO configuration elements are required?

A. An Amazon MQ broker and JMS queue

B. AWS Transfer Family AS2 profiles for the local organization and partner

C. A Kinesis shard for each trading partner

D. An S3 website endpoint that accepts unsigned HTTP posts

E. An AWS Transfer Family agreement associated with the server and the
required access role and profiles

### 71

A fleet uses gp2 EBS volumes mainly to obtain more baseline IOPS by allocating
unused capacity. The measured workload needs predictable IOPS and throughput
but much less storage. The company wants to reduce cost without changing the
EC2 instances.

Which change is most appropriate?

A. Convert every volume to magnetic `standard` storage.

B. Increase each gp2 volume until it reaches the required IOPS.

C. Replace the volumes with instance store and remove backups.

D. Migrate to gp3 and provision the required size, IOPS, and throughput
independently.

### 72 - Choose TWO

A monitoring account must observe CloudWatch metrics and logs from workload
accounts through CloudWatch cross-account observability. Source accounts should
control which telemetry they share.

Which TWO components establish the relationship?

A. An Observability Access Manager sink in the monitoring account

B. A CloudTrail trail in every source account as the only sharing mechanism

C. Observability Access Manager links from the source accounts to the approved
sink

D. VPC peering between every workload VPC and the monitoring VPC

E. IAM access keys copied from every source account into the monitoring account

### 73

A company is migrating a large on-premises Windows SMB file share to Amazon FSx
for Windows File Server. It needs scheduled incremental copies, verification,
and preservation of supported file metadata and permissions.

Which service is the best fit for the transfer?

A. AWS Application Migration Service

B. AWS DataSync

C. AWS Database Migration Service

D. Amazon Kinesis Data Streams

### 74 - Choose TWO

A business has selected a warm-standby multi-Region strategy. The secondary
Region must continuously hold replicated data and a scaled-down but functional
application stack. On declared disaster, traffic must move safely and the
standby must scale to production capacity.

Which TWO actions are essential to the complete design?

A. Keep only infrastructure templates in the secondary Region and create all
resources after the disaster.

B. Replicate data but leave every application dependency absent until failover.

C. Run the required application and dependency stack at reduced capacity in
the secondary Region and continuously replicate required state.

D. Change DNS manually without health checks, readiness checks, or a runbook.

E. Use a tested traffic-routing and health decision plus automation or a
runbook to scale the standby and perform failover and failback.

### 75 - Choose THREE

A tightly coupled HPC workload runs on Linux EC2 instances. It needs the lowest
possible node-to-node latency, high-throughput MPI communication, and a shared
parallel file system that can process a large S3-backed dataset.

Which THREE choices best meet the requirements?

A. Place compatible instances in a cluster placement group.

B. Spread every instance across many Regions.

C. Use a NAT gateway as the MPI fabric.

D. Use Elastic Fabric Adapter on supported instances.

E. Store the active shared dataset on individual instance-store volumes with no
shared namespace.

F. Use Amazon FSx for Lustre with the appropriate S3 data integration.

---

## Submission template

Complete this section only during the attempt. Freeze it before requesting
marking.

```text
Attempt date: Aug 18, 2026
Attempt number: 1
Start: 12:25
End: 14:44
Elapsed: 139 minutes wall clock; 117 active minutes after 22-minute pause
Interruption note: Paused:13:00 Resumed:13:22
Uncertain:38,47,52,56,65,70,72,73

1:B
2:BE
3:D
4:D
5:ACF
6:A
7:B
8:BD
9:C
10:A
11:BE
12:C
13:A
14:ADF
15:C
16:D
17:AD
18:B
19:C
20:BE
21:D
22:A
23:CE
24:A
25:B
26:BDF
27:C
28:D
29:AE
30:B
31:C
32:AD
33:B
34:D
35:BE
36:C
37:A
38:ACF
39:D
40:B
41:AC
42:A
43:D
44:BE
45:C
46:A
47:BD
48:C
49:B
50:ADF
51:A
52:B
53:CE
54:A
55:D
56:BE
57:C
58:A
59:BD
60:B
61:C
62:ADF
63:B
64:D
65:AE
66:C
67:B
68:AD
69:D
70:BE
71:D
72:AC
73:B
74:CE
75:ADF

Submission status: FROZEN
Submission date:  Aug 18, 2026
```
