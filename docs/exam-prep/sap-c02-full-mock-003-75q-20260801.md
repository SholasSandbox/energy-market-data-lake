# AWS Certified Solutions Architect - Professional (SAP-C02)

## Full Mock 003 - 75 Questions

Last revised: 2026-08-01

- **Mode:** Fresh closed-book full simulation
- **Time limit:** 180 minutes
- **Question types:** 48 single-response and 27 multiple-response questions
- **Scoring rule:** Multiple-response questions receive credit only when every
  required option, and no extra option, is selected.

This is an original practice examination aligned to the current SAP-C02 domain
weightings. It is not an AWS examination and does not reproduce live exam
questions.

### Attempt rules

1. Keep lessons, reviews, answer keys, documentation, search, and AI assistance
   closed until the submission is frozen.
2. Select one answer unless the question says **Choose TWO** or **Choose
   THREE**.
3. Record start and end times and any uncertain question numbers.
4. Complete the answer template at the end of this document.
5. Change `Submission status` to `FROZEN` before requesting marking. Do not
   revise a frozen response.

---

## Questions

### 1

A company acquires businesses frequently and must enrol their AWS accounts into
a standard multi-account environment. New accounts require approved guardrails,
centralized logging, and a repeatable account-vending process. The company wants
to minimize custom orchestration.

Which solution best meets these requirements?

A. Use AWS Resource Access Manager to share one production VPC with every
acquired account.

B. Establish an AWS Control Tower landing zone and use Account Factory to
provision or enrol accounts into governed organizational units.

C. Create an IAM user in every acquired account and attach the same managed
policy.

D. Use AWS Service Catalog only to deploy CloudTrail trails into independent
accounts that are not in AWS Organizations.

### 2 - Choose TWO

A checkout API starts a multi-step serverless workflow. Clients sometimes retry
after losing the HTTP response. Processing can continue for hours, and a repeat
submission of the same business operation must not create a second order.

Which TWO actions best meet these requirements?

A. Use an Express Step Functions workflow and rely on execution names to make
every completed execution idempotent.

B. Use a Standard Step Functions workflow for the long-running orchestration.

C. Store the idempotency key only in the API Lambda execution environment.

D. Return success before the request has been accepted by any durable AWS
service.

E. Claim the business idempotency key with a conditional write before starting
the workflow, and return the existing operation reference on a duplicate.

### 3

A company must migrate an on-premises Oracle database to Amazon Aurora
PostgreSQL-Compatible Edition with minimal downtime. The source and target use
different database engines, and stored procedures must be converted.

Which solution is most appropriate?

A. Use an RDS snapshot and restore it directly into Aurora PostgreSQL.

B. Use AWS DataSync to copy Oracle data files into the Aurora cluster volume.

C. Use AWS Schema Conversion Tool for schema and code conversion, then AWS DMS
with full load and change data capture.

D. Use AWS Application Migration Service to replicate the database server and
automatically change the database engine.

### 4 - Choose TWO

A company runs a large fleet of always-on EC2 instances. Utilization data shows
that many instances are oversized, while the remaining steady compute usage is
expected to continue for three years. The company wants to reduce cost without
losing flexibility across instance families.

Which TWO actions should a solutions architect recommend?

A. Use AWS Compute Optimizer recommendations to rightsize the fleet.

B. Purchase Standard Reserved Instances before measuring the rightsized
baseline.

C. Move all instances to Spot Instances, including interruption-intolerant
databases.

D. After rightsizing, purchase Compute Savings Plans for the stable usage
baseline.

E. Increase every Auto Scaling group's minimum capacity to its current peak.

### 5

An organization owns S3 buckets containing regulated data in many member
accounts. Resource policies must never permit access from principals outside
the organization, even if a member-account administrator changes a bucket
policy. The control must be centrally enforced.

Which solution best meets the requirement?

A. Attach a permissions boundary to the organization management account.

B. Use an SCP that applies only to external principals.

C. Attach a resource control policy that limits access to supported resources
based on the principal's organization.

D. Enable S3 server access logging in every member account.

### 6

A latency-sensitive multiplayer application uses UDP behind Network Load
Balancers in two Regions. Users require static entry-point IP addresses and
rapid endpoint failover that does not depend on DNS cache expiry.

Which solution should the company use?

A. Amazon CloudFront with an origin group

B. Route 53 weighted records with a 300-second TTL

C. An Application Load Balancer in each Region with Route 53 geolocation
routing

D. AWS Global Accelerator with regional endpoint groups

### 7

An Athena workload queries millions of small CSV objects. Queries scan far more
data than they return and are both slow and expensive. Most queries filter by
trade date and market.

Which change will provide the greatest improvement?

A. Move the CSV objects to S3 Glacier Flexible Retrieval.

B. Compact the data into larger Parquet files and partition by the commonly
filtered attributes.

C. Enable S3 Transfer Acceleration on the source bucket.

D. Increase the Athena query timeout and keep the existing layout.

### 8 - Choose THREE

A company is planning migration waves for hundreds of interconnected physical
servers. It needs operating-system-level configuration, utilization, and
inbound and outbound connection data. It will then rehost the servers to EC2
and wants one place to track application migration progress.

Which THREE services or components should the company use?

A. AWS Application Discovery Agent

B. AWS Application Discovery Service Agentless Collector VMware module

C. AWS Application Migration Service

D. AWS DataSync Discovery

E. AWS Migration Hub

F. AWS Database Migration Service

### 9 - Choose TWO

A company routes traffic from many spoke VPCs through a centralized inspection
VPC. Stateful virtual firewall appliances are deployed behind a Gateway Load
Balancer. Some return traffic bypasses the appliance that inspected the
outbound flow.

Which TWO changes are required to preserve symmetric inspection?

A. Replace the Gateway Load Balancer with an Application Load Balancer.

B. Correct the Transit Gateway route tables so both directions traverse the
inspection VPC and Gateway Load Balancer endpoints.

C. Enable equal-cost multipath routing on every spoke subnet route table.

D. Enable Transit Gateway appliance mode support for the inspection VPC
attachment.

E. Configure a NAT gateway in every inspected subnet.

### 10

A globally distributed inventory service needs multi-active writes and
strongly consistent reads of the latest item value in every participating
Region. It can accept the higher latency and exactly-three-Region topology
constraints required by the database service.

Which database configuration meets the requirement?

A. A default DynamoDB global table using multi-Region eventual consistency

B. An Aurora read replica in each Region

C. A DynamoDB global table created in multi-Region strong consistency mode

D. A DynamoDB table with a global secondary index in each Region

### 11

A CloudFront distribution has a poor cache-hit ratio because the application
adds many analytics query strings that do not change the response. The origin
still needs one query string named `language` to select content.

What should a solutions architect do?

A. Create a cache policy that includes only `language` in the cache key.

B. Forward and cache on every query string.

C. Disable caching and use origin request policies only.

D. Add all viewer headers and cookies to the cache key.

### 12

Employees use IAM Identity Center to access multiple AWS accounts. A developer
group needs a reusable set of permissions, but delegated administrators must
not be able to provision a generated role beyond a defined maximum.

Which configuration is most appropriate?

A. Create IAM users and attach an SCP directly to each user.

B. Put all developers into an Amazon Cognito user pool.

C. Attach a resource control policy to the IAM Identity Center directory.

D. Define a permission set with least-privilege policies and an appropriate
permissions boundary for its provisioned IAM roles.

### 13

A research site must transfer 300 TB to AWS. The available internet connection
would take months, and no AWS Direct Connect circuit can be installed before
the deadline. The data can be copied at the site and shipped securely.

Which service should the company use?

A. AWS DataSync over the existing internet link

B. AWS Snowball Edge Storage Optimized

C. S3 Transfer Acceleration

D. Amazon Kinesis Data Firehose

### 14 - Choose TWO

A business-critical application requires cross-Region recovery with an RTO
below five minutes. The secondary Region must already run a reduced-capacity
copy of every application tier, and the company accepts scaling it during
failover.

Which TWO actions implement this recovery pattern?

A. Maintain a continuously deployed, reduced-capacity application stack in the
secondary Region.

B. Store only infrastructure templates and backups in the secondary Region.

C. Run full production capacity in both Regions and serve traffic from both at
all times.

D. Recreate network and security resources manually after an outage.

E. Configure health-aware routing or routing controls and an automated scale-up
and failover runbook.

### 15

A transactional workload uses gp2 EBS volumes. It frequently exhausts burst
credits, but performance testing shows that its required IOPS and throughput
are within gp3 limits. The database does not require io2 durability or
sub-millisecond latency.

Which change is most cost-effective?

A. Increase the gp2 volume size until unused capacity reaches 90%.

B. Move the data to st1 volumes.

C. Migrate to gp3 and independently provision the required IOPS and throughput.

D. Use instance store as the only persistent database volume.

### 16 - Choose THREE

A security team must centrally manage AWS Config, Amazon GuardDuty, and AWS
Security Hub across an organization while minimizing activity in the
management account.

Which THREE actions are appropriate?

A. Designate delegated administrator accounts for the supported security
services.

B. Create IAM users in every member account for the central team.

C. Enable organization-wide service integration and centrally configure member
enrolment.

D. Move all application workloads into the management account.

E. Disable AWS Organizations trusted access after enrolment.

F. Use organization or delegated-administrator capabilities to apply the
required service configurations across accounts and Regions.

### 17

A bursty document-processing job takes up to 45 minutes and needs large native
libraries and more ephemeral storage than the Lambda execution model can
provide. Jobs are independent and do not require a continuously running server.

Which compute service is the best fit?

A. Lambda with a longer configured timeout

B. On-demand ECS tasks on AWS Fargate

C. Amazon EC2 Dedicated Hosts with one job per host

D. API Gateway integration response mapping

### 18 - Choose THREE

A company is migrating a large Oracle database to Aurora PostgreSQL. It needs
schema conversion, low-downtime data movement, and an assessment that identifies
unsupported objects before cutover.

Which THREE actions should the company take?

A. Run AWS DMS pre-migration assessments.

B. Copy the Oracle data files with AWS DataSync.

C. Use AWS Schema Conversion Tool to convert the schema and database code.

D. Restore an Oracle RMAN backup directly into Aurora PostgreSQL.

E. Use AWS DMS full load and change data capture.

F. Use Amazon EBS Elastic Volumes to convert the database engine.

### 19

A synchronous Lambda API has predictable traffic spikes at the start of each
business day. Users report latency caused by initialization, and the function
must be ready before the spike begins.

Which solution addresses this requirement most directly?

A. Increase the function's reserved concurrency only.

B. Send the function to an SQS standard queue.

C. Increase the API Gateway cache TTL.

D. Configure provisioned concurrency and schedule the required level before the
spike.

### 20

A hybrid network has Direct Connect and VPN paths to AWS. Traffic unexpectedly
uses the VPN after the on-premises router advertises a narrower CIDR over that
path, even though the Direct Connect AS path is shorter.

Which routing rule explains the behavior?

A. Longest-prefix match is evaluated before BGP path attributes.

B. VPN routes always take priority over Direct Connect routes.

C. Direct Connect local preference is ignored for private virtual interfaces.

D. Static routes always lose to propagated routes.

### 21 - Choose TWO

A ransomware-resilience requirement states that backups must be isolated from
workload-account administrators and must not be deletable or have retention
shortened during a defined compliance period.

Which TWO controls best meet the requirement?

A. Keep all recovery points in the workload account's default backup vault.

B. Copy recovery points to a dedicated backup account.

C. Grant workload administrators permission to delete copied recovery points.

D. Store the only backup as an unversioned object in the source bucket.

E. Use AWS Backup Vault Lock in compliance mode with an approved retention
configuration.

### 22

An Aurora database is overwhelmed by thousands of short-lived connections from
Lambda functions during bursts. Query capacity is sufficient, but connection
creation and database memory consumption cause failures.

Which service should be added?

A. DynamoDB Accelerator

B. ElastiCache Memcached as a database connection pool

C. Amazon RDS Proxy

D. AWS Global Accelerator

### 23 - Choose THREE

On-premises clients must resolve private Route 53 names in AWS, and VPC
workloads must resolve selected on-premises zones. Existing Direct Connect
connectivity provides IP reachability.

Which THREE components are required?

A. Route 53 Resolver inbound endpoints for on-premises-to-AWS DNS queries

B. An internet gateway attached to every workload VPC

C. A public hosted zone containing the private records

D. Route 53 Resolver outbound endpoints and forwarding rules for the selected
on-premises domains

E. Amazon CloudFront origin access control

F. Association or sharing of the Resolver rules with the VPCs that need them

### 24

A company must inventory a large VMware environment without installing agents
inside guest operating systems. It needs VM configuration and utilization data
from vCenter for migration assessment, but it does not require OS-level process
or detailed connection telemetry.

Which discovery mechanism should it use?

A. AWS Application Discovery Agent on every VM

B. AWS Application Discovery Service Agentless Collector with its VMware
vCenter module

C. AWS DataSync Discovery against each VM's operating-system disk

D. VPC Flow Logs exported from the on-premises hypervisor

### 25

A role in Account A must decrypt an object encrypted with a customer-managed
KMS key owned by Account B. The role's identity policy allows `kms:Decrypt`, but
the request is denied.

What additional configuration is required?

A. An SCP in Account A that grants `kms:Decrypt`

B. A permissions boundary that allows all KMS actions

C. A key policy in Account B that permits the cross-account principal or its
account, together with the appropriate IAM permission in Account A

D. An S3 lifecycle rule that changes the object's storage class

### 26 - Choose TWO

A distributed application uses API Gateway, Lambda, SQS, and DynamoDB. Operators
need to trace one transaction across components and quickly find the technical
cause of intermittent latency.

Which TWO actions provide the most useful diagnostic evidence?

A. Enable AWS X-Ray or supported distributed tracing across the request path.

B. Store all application logs only on Lambda temporary storage.

C. Use a different random correlation identifier in every component.

D. Disable API Gateway access logging to reduce noise.

E. Emit structured logs containing a propagated correlation identifier and
query them with CloudWatch Logs Insights.

### 27

A platform team must deploy a standard IAM role and Config rule to selected OUs
and automatically include accounts added to those OUs. The team wants AWS to
manage the cross-account deployment permissions.

Which solution has the least operational overhead?

A. Run a custom Lambda in each account from a cron schedule.

B. Use CloudFormation StackSets with service-managed permissions and automatic
deployment for the target OUs.

C. Share a CloudFormation template through AWS Resource Access Manager.

D. Put all member-account root credentials in Secrets Manager.

### 28 - Choose THREE

A global order API must remain writable in two Regions. An order retried in a
different Region must not be processed twice, and concurrent updates to the
same business entity must have an explicit conflict strategy.

Which THREE design elements are appropriate?

A. Deploy regional API and compute stacks behind health-aware global routing.

B. Store idempotency state only in each Lambda execution environment.

C. Use a suitable multi-Region data store, such as a DynamoDB global table,
with the consistency mode selected for the business requirement.

D. Assume asynchronous replication eliminates all application conflicts.

E. Use DNS TTL alone as the duplicate-prevention mechanism.

F. Define a business idempotency key and conflict-handling rules for concurrent
updates.

### 29 - Choose TWO

A company must migrate an initial 800 TB file archive to S3. The network cannot
complete the initial copy on time, but after seeding, daily changes can be
transferred across the available link before cutover.

Which TWO services should be combined?

A. Amazon Kinesis Data Streams for the initial bulk copy

B. AWS Snowball Edge for the initial offline transfer

C. AWS DMS for ongoing file-system deltas

D. AWS DataSync for the incremental online transfers

E. Amazon AppFlow for block-level replication

### 30

Private subnets transfer large volumes to S3 and DynamoDB through NAT gateways,
creating unnecessary data-processing charges. The workloads do not need these
services to be reached from on premises.

What should a solutions architect do?

A. Add S3 and DynamoDB gateway VPC endpoints and update the relevant route
tables and policies.

B. Add one internet gateway to every private subnet.

C. Replace the NAT gateways with Network Load Balancers.

D. Route the traffic through AWS Global Accelerator.

### 31 - Choose TWO

A company wants a dedicated Security Tooling account to administer organization-
wide security findings and a separate Log Archive account to receive immutable
audit logs from all accounts.

Which TWO actions best support this design?

A. Run all workloads in the Security Tooling account.

B. Store the organization trail only in each workload account.

C. Designate the Security Tooling account as delegated administrator for the
supported security services.

D. Give every application role write access to the central log bucket.

E. Create an organization CloudTrail trail that delivers to a tightly
controlled bucket in the Log Archive account.

### 32

A company serves private static content globally from S3. The bucket must not be
public, users need signed access, and content should be cached near users.

Which solution best meets the requirements?

A. Enable S3 static website hosting and public-read ACLs.

B. Use CloudFront with origin access control for the S3 origin and CloudFront
signed URLs or signed cookies.

C. Use an S3 gateway endpoint in every user's network.

D. Put an internet-facing Network Load Balancer in front of S3.

### 33 - Choose THREE

An Aurora read-heavy application experiences periodic connection storms and
read saturation. Writes remain within the writer's capacity. The company wants
to scale reads automatically and reduce connection overhead.

Which THREE actions are appropriate?

A. Add Aurora Replicas and enable replica Auto Scaling.

B. Send all read traffic to the cluster writer endpoint.

C. Direct read-only connections to the reader endpoint.

D. Replace Aurora with an S3 bucket.

E. Use RDS Proxy where connection pooling is needed and supported.

F. Disable replica health checks.

### 34

A company wants to modernize a COBOL mainframe application by converting it to
a cloud-optimized Java application. It wants automated codebase analysis,
decomposition, refactoring, and infrastructure-as-code output for deployment on
AWS.

Which service is designed for this migration?

A. AWS Mainframe Modernization managed runtime

B. AWS Transfer Family

C. AWS Application Discovery Service

D. AWS Transform for mainframe

### 35

A central networking account owns large VPCs. Application teams in multiple
accounts must launch resources into centrally managed subnets without creating
separate peered VPCs for every team.

Which solution should be used?

A. Share the subnets with the application accounts through AWS Resource Access
Manager.

B. Share the internet gateway through AWS Resource Access Manager.

C. Create identical overlapping VPCs in every account.

D. Move all application IAM roles into the networking account.

### 36 - Choose TWO

A loan workflow pauses for a human approval that can take several days. The
workflow must retain its state and resume only when the external approval system
returns the matching token. The complete execution history is required.

Which TWO features should be used?

A. An Express Step Functions workflow

B. A Standard Step Functions workflow

C. An API Gateway cache entry as the durable workflow state

D. A Lambda function that sleeps until the reviewer responds

E. A callback task using a Step Functions task token

### 37

An SQS-triggered Lambda receives batches of ten messages. One malformed message
causes the entire batch to be retried repeatedly, including nine messages that
were processed successfully.

Which change best reduces duplicate processing while preserving failed-message
retries?

A. Set the queue visibility timeout to zero.

B. Delete the queue after every batch failure.

C. Implement partial batch responses and configure an appropriate redrive
policy and dead-letter queue.

D. Return success for every record, including unprocessed records.

### 38

An organization requires audit logs from every existing and future account in a
central account. Workload administrators must not be able to alter retained log
objects.

Which solution best meets the requirement?

A. Create independent trails that deliver to buckets controlled by each
workload account.

B. Create an organization CloudTrail trail that delivers to a KMS-encrypted
central bucket with restrictive policies and S3 Object Lock where the retention
requirement calls for WORM protection.

C. Use VPC Flow Logs as the only record of management API activity.

D. Store CloudTrail event history in Lambda temporary storage.

### 39

An on-premises application requires an SMB or NFS file interface and low-latency
access to recently used data, while the durable backing store must be Amazon S3.
The company wants a continuously usable hybrid file gateway, not a one-time
migration.

Which service should it use?

A. Amazon S3 File Gateway

B. AWS DataSync only

C. Amazon EBS Multi-Attach

D. AWS DMS

### 40

An order service must publish an `OrderCreated` business fact. Several current
and future consumers need content-based routing without the publisher knowing
their queue or function endpoints.

Which service is the best event-routing backbone?

A. Amazon EBS

B. AWS Batch

C. SQS FIFO with one queue shared by every consumer

D. Amazon EventBridge

### 41 - Choose TWO

A DynamoDB base table has spare write capacity, but writes are throttled. A GSI
uses a low-cardinality partition key and receives a disproportionate share of
writes. The GSI also has insufficient configured write capacity for the current
load.

Which TWO actions address the actual causes?

A. Redesign or write-shard the GSI partition key to distribute writes.

B. Add an LSI after table creation and route all writes through it.

C. Increase only the base table's read capacity.

D. Add DAX to absorb GSI writes.

E. Increase the GSI write capacity or use an appropriate capacity mode after
fixing the hot-key distribution problem.

### 42

An SCP denies operations outside approved Regions. Administrators discover that
required global-service operations are also denied because those requests use
global endpoints with a regional context not in the approved list.

Which SCP design is most appropriate?

A. Add every AWS Region to the approved list.

B. Replace the SCP with a resource policy on each EC2 instance.

C. Use `aws:RequestedRegion` for the deny and maintain a narrowly reviewed
`NotAction` exception list for required global-service operations.

D. Attach an administrator identity policy, because it overrides an SCP deny.

### 43 - Choose THREE

A company requires a warm-standby database architecture across two Regions with
a low RPO and an RTO of minutes. The application uses Aurora, and operators want
a controlled way to route traffic during recovery.

Which THREE components fit the design?

A. Nightly snapshots as the only cross-Region database copy

B. Aurora Global Database with a secondary cluster

C. No application infrastructure in the secondary Region until failure

D. A reduced-capacity but deployed application stack in the secondary Region

E. DNS records with no health or operator control

F. Route 53 health-aware failover or Route 53 ARC routing controls with a tested
runbook

### 44

A company is moving a self-managed SQL Server database to Amazon RDS for SQL
Server. The schema is compatible, and the company requires a full load followed
by continuous replication until a short cutover.

Which service should it use for the data movement?

A. AWS Schema Conversion Tool only

B. AWS Application Migration Service

C. AWS Database Migration Service with full load and change data capture

D. AWS Snowball Edge Compute Optimized

### 45

An application uses DAX for DynamoDB reads but sends all writes directly to the
DynamoDB endpoint. Immediately after a write, the application sometimes reads
an older cached value through DAX.

Which change most directly restores write-through cache behavior?

A. Replace the table's partition key with a random UUID on every read.

B. Send the supported write operations through the DAX client so DAX updates
its cache after the table write succeeds.

C. Add an SQS dead-letter queue to the DAX cluster.

D. Request strongly consistent reads from DAX.

### 46 - Choose TWO

An enterprise wants AWS Firewall Manager to apply a common AWS WAF policy to in-
scope resources across an organization and report compliance.

Which TWO prerequisites are required?

A. Configure a Firewall Manager administrator account through AWS
Organizations.

B. Create a public hosted zone in every member account.

C. Disable continuous recording in AWS Config.

D. Enable AWS Config continuous recording for the relevant protected resource
types in the accounts and Regions in scope.

E. Give every application role permission to edit the central web ACL.

### 47

A private ECS service is behind a Network Load Balancer. An API Gateway REST API
must invoke the service without exposing the load balancer to the internet.

Which integration should be used?

A. A CloudFront origin access identity

B. An S3 gateway endpoint

C. An API Gateway VPC link to the Network Load Balancer

D. A Route 53 public hosted zone only

### 48 - Choose THREE

An investigator must determine who changed a security group, how the resource's
configuration changed over time, and whether related threat findings exist.

Which THREE data sources should be examined?

A. AWS CloudTrail for the API caller and request

B. S3 Inventory for EC2 security-group rules

C. AWS Config resource timeline and configuration history

D. Amazon Inspector network reachability as the record of the API caller

E. GuardDuty and Security Hub findings for related security context

F. Cost and Usage Reports as the authoritative configuration history

### 49

A company must migrate an SMB file share to Amazon FSx for Windows File Server.
It needs an initial copy plus incremental transfers before a scheduled cutover,
with integrity verification and task reporting.

Which service should it use?

A. AWS DMS

B. Amazon Kinesis Data Firehose

C. AWS Application Migration Service

D. AWS DataSync

### 50

An S3 bucket in Account B uses a customer-managed KMS key. A role in Account A
must read and decrypt selected objects. The bucket policy already allows the
role, but KMS returns `AccessDenied`.

Which change is required?

A. Add the role to an SCP allow list only.

B. Permit the cross-account use in the KMS key policy in Account B and grant
the role the required KMS action in Account A.

C. Enable S3 Transfer Acceleration.

D. Change the role's permissions boundary to grant permissions that no identity
policy grants.

### 51 - Choose TWO

A global TCP application needs static anycast IP addresses, health-based rapid
regional failover, and enhanced DDoS protection with access to the advanced
response capabilities offered by AWS.

Which TWO services should be used?

A. AWS Global Accelerator

B. Route 53 simple routing only

C. Amazon Macie

D. AWS Shield Advanced

E. AWS DataSync

### 52

An Auto Scaling workload has a recurring daily demand pattern. Reactive target
tracking consistently adds capacity too late, while keeping peak capacity all
day wastes money.

Which scaling approach best addresses the pattern?

A. Disable Auto Scaling and resize manually.

B. Use only a fixed desired capacity equal to the daily peak.

C. Add predictive scaling, retaining dynamic scaling for unforecast deviations.

D. Convert every instance to a Dedicated Host.

### 53 - Choose THREE

An enterprise wants centrally governed backups for member accounts. Backup
policies should apply by OU, recovery points should be isolated from workload
administrators, and protected copies must have immutable retention.

Which THREE actions best meet the requirements?

A. Use AWS Organizations backup policies to define and apply backup plans.

B. Keep all copies only in each source account's default vault.

C. Delegate AWS Backup administration to a designated backup account.

D. Grant workload administrators permission to shorten central-vault
retention.

E. Use EC2 instance store for the protected copy.

F. Copy recovery points to a central vault and configure Vault Lock according
to the compliance requirement.

### 54

During a migration portfolio assessment, a server is found to have no users,
no inbound or outbound dependencies, and no business owner. The business
confirms that the capability is no longer required.

Which migration strategy is appropriate?

A. Rehost

B. Relocate

C. Repurchase

D. Retire

### 55

S3 event notifications invoke processing asynchronously. Occasionally the same
logical event is delivered more than once. Creating the result twice would
cause a financial error.

Which design is most appropriate?

A. Assume S3 notifications are exactly once.

B. Use a stable business idempotency key and a conditional write or equivalent
atomic claim before applying the side effect.

C. Store a duplicate flag only in Lambda memory.

D. Disable retries for all failed invocations.

### 56 - Choose TWO

A Kinesis data stream has several independent low-latency consumers. Consumer
lag rises during peaks because the consumers share read throughput, and some
shards also cannot absorb the incoming record rate.

Which TWO changes address the two bottlenecks?

A. Replace every partition key with the same constant.

B. Increase shard capacity by splitting or resharding based on measured load.

C. Add a DynamoDB local secondary index.

D. Reduce the stream retention to one minute.

E. Use enhanced fan-out for consumers that need dedicated read throughput.

### 57

Two companies with overlapping VPC CIDR ranges need private access from a
consumer VPC to one specific provider service behind a Network Load Balancer.
They do not require full network connectivity.

Which solution is most appropriate?

A. VPC peering with route-table entries for both overlapping CIDRs

B. Transit Gateway with propagation of both identical CIDRs

C. AWS PrivateLink using an endpoint service and interface endpoint

D. An internet gateway and public IP addresses on every backend

### 58 - Choose THREE

A multi-tenant SaaS API needs customer sign-up and sign-in, JWT-based API
authorization, and protection against common web exploits and abusive request
patterns at the API edge.

Which THREE components should be used?

A. An Amazon Cognito user pool

B. IAM Identity Center permission sets for external application customers

C. An API Gateway JWT or Cognito authorizer as appropriate to the API type

D. AWS DataSync task reports

E. An S3 gateway endpoint as the identity provider

F. AWS WAF associated with the supported API endpoint

### 59 - Choose TWO

External partners must upload files using their existing SFTP clients. Files
must land in S3, and a managed post-upload step must decrypt and tag each file
before downstream processing.

Which TWO services or features meet the requirements?

A. S3 Transfer Acceleration as an SFTP server

B. AWS Transfer Family with an SFTP endpoint backed by S3

C. Amazon EFS replication to partner laptops

D. An AWS Transfer Family managed workflow for the post-upload processing

E. AWS DMS change data capture

### 60

A global write API uses `POST` requests to regional Application Load Balancers.
The architect initially proposes CloudFront origin failover, but the failover
must work for write requests as well as reads and must provide rapid regional
endpoint health failover.

Which service is the better fit for the global entry point?

A. S3 Multi-Region Access Points

B. Route 53 simple routing with no health checks

C. CloudFront origin failover, because it fails over every HTTP method

D. AWS Global Accelerator with the regional load balancers as endpoints

### 61 - Choose THREE

A company is building centralized stateful traffic inspection for many VPCs.
It wants horizontally scalable third-party appliances and predictable symmetric
routing through the inspection VPC.

Which THREE components or configurations are appropriate?

A. AWS Transit Gateway route tables that direct the required flows through the
inspection attachment

B. An Application Load Balancer in front of transparent network appliances

C. Transit Gateway appliance mode support on the inspection VPC attachment

D. Direct VPC peering between every pair of spoke VPCs

E. Gateway Load Balancer and Gateway Load Balancer endpoints

F. A NAT gateway as the stateful inspection engine

### 62

An SQS standard queue can deliver a message more than once. A worker charges a
customer and then writes a result record. A duplicate charge is unacceptable.

Which design best protects the business operation?

A. Increase the visibility timeout and assume duplicates are then impossible.

B. Use a stable operation key and atomically claim or record it in a durable
store before applying the charge.

C. Use a new random key on each retry.

D. Delete the message before attempting the charge.

### 63

A DynamoDB GSI is throttling although the table's aggregate capacity appears
adequate. Most indexed items share one GSI partition-key value.

Which change most directly addresses the root cause?

A. Add DAX and keep the same GSI key.

B. Increase only the base table's read capacity.

C. Redesign or write-shard the GSI partition key to distribute the write load.

D. Request strongly consistent reads from the GSI.

### 64

A company needs to rehost 500 physical and virtual servers to EC2. It requires
continuous block-level replication, non-disruptive test launches, and a short
cutover window.

Which service should it use?

A. AWS Application Migration Service

B. AWS DataSync

C. AWS Database Migration Service

D. AWS Transfer Family

### 65

A security team must centrally block DNS queries to known malicious domains
from VPCs across selected organization OUs and automatically associate the
required controls with in-scope VPCs.

Which solution best meets the requirement?

A. Network ACLs containing domain names

B. Route 53 Resolver DNS Firewall rule groups managed across the organization
with an AWS Firewall Manager DNS Firewall policy

C. A public Route 53 hosted zone for each malicious domain

D. AWS WAF rules attached to each VPC route table

### 66 - Choose THREE

A browser application must upload 50 GB objects directly to a private S3 bucket.
Uploads must be resumable, the application servers must not proxy the bytes, and
the bucket must remain protected from unintended public access.

Which THREE actions should be used?

A. Use S3 multipart upload.

B. Put long-lived AWS access keys in browser local storage.

C. Make the bucket publicly writable.

D. Issue time-limited presigned URLs for the required multipart operations.

E. Disable checksum validation to improve integrity.

F. Keep S3 Block Public Access enabled and restrict the bucket policy to the
required access paths.

### 67

A healthy Aurora Global Database requires planned maintenance in its primary
Region. The company wants to move the writer role to a secondary Region without
data loss before maintenance begins.

Which operation should be used?

A. Detach the secondary cluster and promote it independently.

B. Perform an Aurora Global Database switchover.

C. Delete the primary cluster and wait for DNS timeout.

D. Restore the latest manual snapshot in the secondary Region.

### 68

An RCP attached to an OU allows access to a class of supported resources. A role
in a member account has no identity-based permission for the requested action,
and the resource policy does not grant it either.

What is the result?

A. The request succeeds because an RCP is a granting policy.

B. The request succeeds only for the root user.

C. The RCP is copied into the role as an inline policy.

D. The request is denied because an RCP defines a maximum permissions
guardrail and does not grant access.

### 69

A field operation generates 50 TB of data in a location with intermittent
connectivity. It needs local storage and compute for preprocessing, followed by
secure physical transfer to AWS.

Which service best fits the requirement?

A. S3 Transfer Acceleration only

B. AWS Snowball Edge with the required storage and compute capabilities

C. AWS DataSync Discovery

D. Amazon CloudFront

### 70

A PostgreSQL-compatible transactional application has unpredictable, rapidly
changing demand. It requires relational ACID semantics and automatic fine-
grained capacity adjustment without managing database instances.

Which solution is most appropriate?

A. DynamoDB on-demand capacity

B. Amazon Redshift Serverless

C. Aurora Serverless v2

D. ElastiCache Memcached

### 71

EC2 workloads and on-premises systems connected through Direct Connect both
need private access to S3. The VPC workloads should avoid NAT costs, while the
on-premises systems need private IP endpoints reachable through the hybrid
network.

Which design is most appropriate?

A. Use only an S3 gateway endpoint for both VPC and on-premises clients.

B. Use only an internet gateway and public S3 endpoints.

C. Use an S3 gateway endpoint for appropriate VPC route-table traffic and S3
interface endpoints with the required private DNS and hybrid routing for on-
premises access.

D. Use a DynamoDB gateway endpoint for S3 traffic.

### 72

A global enterprise needs centrally defined network segments and routing
policies across many Regions and accounts. It wants a managed global core
network rather than building and operating a mesh of inter-Region Transit
Gateway peerings.

Which service is the best fit?

A. VPC peering

B. AWS PrivateLink

C. Route 53 Resolver DNS Firewall

D. AWS Cloud WAN

### 73 - Choose THREE

A company publishes business events to many independent consumers. Each
consumer needs durable buffering and isolated retries. Operators must be able
to replay past events after deploying a corrected consumer, and every consumer
must tolerate redelivery.

Which THREE design elements meet these requirements?

A. Use EventBridge archive and replay for the event bus.

B. Send every consumer to the same SQS queue and expect each to receive every
message.

C. Give each consumer its own SQS queue targeted by the appropriate EventBridge
rule.

D. Disable all retries so no event can be seen twice.

E. Make each consumer idempotent using a stable event or business-operation
identifier.

F. Store the only event copy in Lambda temporary storage.

### 74

A company must run software whose licence is tied to physical CPU sockets and
requires visibility and control of host placement. It wants to track licence
consumption centrally.

Which solution best meets the requirement?

A. EC2 Spot Instances with default tenancy

B. EC2 Dedicated Hosts with AWS License Manager

C. AWS Fargate Spot with Savings Plans

D. Lambda reserved concurrency

### 75

A company has steady compute usage but frequently changes EC2 instance families,
sizes, operating systems, and Regions. It wants a commitment discount with the
greatest compute flexibility.

Which pricing option is most appropriate?

A. Zonal Reserved Instances for one fixed instance type

B. EC2 Instance Savings Plans

C. Compute Savings Plans

D. Spot Instance blocks

---

## Frozen submission template

```text
Start:23:04
End:  00:50
Uncertain: 8,9,11,16,20,21,26,28,34,48,61,67

1:B
2:BE
3:C
4:AD
5:C
6:D
7:B
8:ACE
9: BD
10:C
11:A
12:D
13:B
14:AE
15:C
16:ACF
17:B
18:ACE
19:D
20:A
21:BE
22:C
23:ADF
24:B
25:C
26:AE
27:B
28:ACF
29:BD
30:A
31:CE
32:B
33:ACE
34:D
35:A
36:BE
37:C
38:B
39:A
40:D
41:AE
42:C
43:BDF
44:C
45:B
46:AD
47:C
48:ACE
49:D
50:B
51:AD
52:C
53:ACF
54:D
55:B
56:BE
57:C
58:ACF
59:BD
60:D
61:ACE
62:B
63:C
64:A
65:B
66:ADF
67:B
68:D
69:B
70:C
71:C
72:D
73:ACE
74:B
75:C

Submission status: FROZEN
```
