# AWS Certified Solutions Architect - Professional (SAP-C02)

## Full Mock 004 - 75 Questions

Last revised: 2026-08-05

- **Mode:** Fresh closed-book full simulation
- **Time limit:** 180 minutes
- **Question types:** 48 single-response and 27 multiple-response questions
- **Scoring rule:** Multiple-response questions receive credit only when every
  required option, and no extra option, is selected.

This is an original practice examination aligned to the current SAP-C02 domain
weightings. It is not an AWS examination and does not reproduce live exam
questions. The answer key is deliberately excluded from this document.

### Attempt rules

1. Keep lessons, reviews, answer keys, documentation, search, and AI assistance
   closed until the submission is frozen.
2. Select one answer unless the question says **Choose TWO** or **Choose
   THREE**.
3. Record start and end times and any uncertain question numbers.
4. Complete the answer template at the end of this document.
5. Change `Submission status` to `FROZEN` before requesting marking. Do not
   revise a frozen response.

---

## Questions

### 1

An organization stores regulated data in S3 buckets across many member
accounts. Member-account administrators must not be able to grant access to
principals outside the organization, even by changing a bucket policy. The
control must apply centrally to supported resources and must not grant access
by itself.

Which control best meets these requirements?

A. An identity-based policy in the management account

B. A resource control policy attached at the appropriate organization level

C. An IAM permissions boundary attached to every administrator role

D. An S3 lifecycle policy replicated to every bucket

### 2 - Choose TWO

A telemetry platform must ingest a very high volume of events from millions of
devices. Events for the same device must remain ordered. Three independent
applications must process the stream at different rates, and each application
must be able to replay retained events.

Which TWO actions best meet these requirements?

A. Use Amazon Kinesis Data Streams and use the device ID as the partition key.

B. Put all events into a single SQS standard queue.

C. Publish directly to an SNS topic without retaining the events.

D. Use one SQS FIFO message group for all devices.

E. Configure each application as an independent Kinesis consumer, using
enhanced fan-out when dedicated read throughput is required.

### 3

A company wants to replace a heavily customized on-premises customer
relationship management application with a commercially supported SaaS
offering. Historical customer data will be imported, but the existing
application code and servers will not be moved.

Which migration strategy applies?

A. Rehost

B. Replatform

C. Relocate

D. Repurchase

### 4 - Choose THREE

An organization requires an immutable audit trail of management events from
all current and future AWS accounts. Logs must be delivered to a dedicated log
archive account and encrypted with a customer-managed KMS key. Workload-account
administrators must not be able to stop the organization-wide collection.

Which THREE actions should be taken?

A. Create separate IAM users in each account to upload CloudTrail files.

B. Deliver the trail to a protected S3 bucket in the log archive account.

C. Configure the S3 bucket policy and KMS key policy to permit the CloudTrail
delivery path.

D. Create an organization trail from the management account or a delegated
CloudTrail administrator.

E. Depend only on the 90-day CloudTrail Event history in each Region.

F. Allow member-account administrators to delete the organization trail when
troubleshooting.

### 5

Dozens of application teams run HTTP services across separate VPCs and AWS
accounts. The platform team wants service discovery, service-to-service
routing, monitoring, and IAM-based authorization without managing a mesh of
VPC peering connections or load balancers for every consumer.

Which service is the best fit?

A. Amazon VPC Lattice

B. AWS Transit Gateway

C. AWS Direct Connect Gateway

D. Amazon Route 53 Resolver DNS Firewall

### 6

A company uses an Aurora Global Database. Both Regions are healthy, and the
company wants to move the writer to the secondary Region for a planned
resilience exercise with no data loss.

Which operation should the company use?

A. Promote a read replica by deleting the primary cluster.

B. Restore the latest automated snapshot in the secondary Region.

C. Perform an Aurora Global Database switchover.

D. Detach the secondary cluster and enable write forwarding.

### 7 - Choose TWO

An on-premises application connects to AWS through Direct Connect and must
access Amazon S3 privately. The application must use private IP connectivity
and the standard regional S3 DNS name. No internet gateway or NAT gateway may
be used.

Which TWO components are required?

A. An S3 gateway endpoint with a route propagated to the on-premises router

B. An S3 interface VPC endpoint reachable from the Direct Connect path

C. S3 Transfer Acceleration

D. An S3 website endpoint

E. A Route 53 Resolver inbound endpoint so on-premises DNS can resolve the
private endpoint mapping

### 8

A company must copy 60 TB from an on-premises NFS server to Amazon EFS. The
network connection is available, the transfer must be encrypted in transit,
and changed files must be copied efficiently during repeated pre-cutover runs.

Which service should the company use?

A. AWS Application Migration Service

B. AWS DataSync

C. AWS Transfer Family

D. Amazon S3 File Gateway

### 9 - Choose TWO

An enterprise is replacing IAM users used by employees with IAM Identity
Center. Employees need access to multiple AWS accounts using centrally managed
groups and temporary credentials.

Which TWO statements correctly describe the resulting access model?

A. An account assignment associates a user or group, an AWS account, and a
permission set.

B. Each permission set creates a permanent IAM user in every assigned account.

C. The permission set replaces all IAM policy evaluation in the target
account.

D. Employees must store long-lived access keys for every assigned account.

E. IAM Identity Center provisions and manages an IAM role in each assigned
account for the permission set.

### 10

A public API accepts requests that start a workflow lasting several hours.
Clients can retry after a lost HTTP response, but the same business operation
must not be created twice. The caller should receive an operation reference as
soon as durable processing has been accepted.

Which design best meets these requirements?

A. Invoke an Express Step Functions workflow and use only the HTTP connection
as the idempotency record.

B. Store the idempotency key only in the Lambda execution environment.

C. Run the entire workflow synchronously inside one Lambda invocation.

D. Use a Standard Step Functions workflow and claim a business idempotency key
in DynamoDB with a conditional write before starting the execution.

### 11

Athena queries scan millions of small JSON objects and usually filter on trade
date and market. Query cost and latency have increased sharply.

Which change is most likely to provide the largest improvement?

A. Enable S3 Transfer Acceleration.

B. Move the objects to S3 Glacier Flexible Retrieval.

C. Compact the data into larger Parquet files and partition it by commonly
filtered columns.

D. Increase the Athena query timeout.

### 12 - Choose THREE

A migration programme includes physical servers and VMware virtual machines.
Physical servers require detailed process and TCP connection evidence. VMware
inventory must be collected without installing software in the guest operating
systems. The programme also needs a central place to group servers into
applications and track migration progress.

Which THREE components meet these requirements?

A. Install the AWS Application Discovery Agent on the physical servers.

B. Use AWS DataSync to discover running processes.

C. Use AWS Snow Family as the migration progress database.

D. Use AWS Transfer Family to collect vCenter inventory.

E. Deploy the AWS Application Discovery Service Agentless Collector in the
VMware environment.

F. Use AWS Migration Hub for application grouping and migration tracking.

### 13

A security team wants backups that are stored in an AWS service-owned account,
are protected by Vault Lock in compliance mode, and can be shared to a separate
recovery account for restore testing.

Which AWS Backup feature is designed for this requirement?

A. A logically air-gapped vault

B. A default backup vault with governance-mode Vault Lock

C. An EBS Recycle Bin retention rule

D. An S3 Glacier vault

### 14

A global application uses CloudFront with an origin group. Reads fail over
successfully when the primary origin is unavailable, but `POST` requests do
not fail over to the secondary origin.

What is the correct explanation?

A. CloudFront origin failover requires Lambda@Edge for every HTTP method.

B. CloudFront origin failover applies only to eligible `GET`, `HEAD`, and
`OPTIONS` requests, not write methods such as `POST`.

C. The secondary origin must be in the same Availability Zone.

D. CloudFront can fail over only between two S3 website endpoints.

### 15 - Choose TWO

A company has a large, always-on EC2 fleet. Many instances are oversized, but
the stable baseline is expected to remain for three years. The company wants
cost savings while retaining flexibility across instance families and Regions.

Which TWO actions should the company take?

A. Purchase Standard Reserved Instances for the existing oversized fleet.

B. Apply AWS Compute Optimizer rightsizing recommendations before committing
to a new baseline.

C. Increase Auto Scaling minimum capacity to the observed peak.

D. Move all workloads, including interruption-intolerant databases, to Spot
Instances.

E. Purchase Compute Savings Plans for the stable rightsized baseline.

### 16

Migration Hub shows no discovery or migration data when an operator opens the
console in a newly selected Region. Agents and migration tools are still
reporting successfully.

What should the operator check first?

A. Whether a DataSync task is in the `AVAILABLE` state

B. Whether S3 Transfer Acceleration is enabled

C. Whether the application servers use Elastic IP addresses

D. Whether the console is open in the configured Migration Hub home Region

### 17

A developer role has an identity policy allowing `s3:*`. A permissions
boundary attached to the role allows only `s3:GetObject` and `s3:ListBucket`.
There is no explicit deny elsewhere.

What is the role's effective S3 access?

A. Only the actions allowed by both the identity policy and the boundary

B. All S3 actions because the identity policy grants `s3:*`

C. No S3 actions because a permissions boundary grants nothing

D. Only actions listed in the boundary, even if the identity policy does not
grant them

### 18 - Choose TWO

An organization wants a central event-routing account. Member accounts must
publish selected application events to a central EventBridge event bus, where
rules route matching events to different targets.

Which TWO configurations are required?

A. One SQS queue shared as the event bus for the entire organization

B. VPC peering between every sender account and the central account

C. A resource policy on the central event bus that permits the intended member
accounts or organization to call `events:PutEvents`

D. Long-lived IAM user access keys in each member account

E. Permissions in the sender accounts allowing the workloads to call
`events:PutEvents` on the central bus

### 19

A company deploys a three-node DAX cluster across Availability Zones, but the
application's DynamoDB reads still have single-digit-millisecond latency and
the DAX metrics show no requests.

What is the most likely cause?

A. DAX supports writes but not reads.

B. DAX requires a global secondary index for every cached item.

C. The application still uses the DynamoDB client instead of the DAX client
and endpoint.

D. DAX can be used only with DynamoDB global tables.

### 20 - Choose THREE

A company is migrating an on-premises Microsoft SQL Server database to Amazon
RDS for SQL Server with minimal downtime. The database engine remains the same.

Which THREE actions form an appropriate migration approach?

A. Use AWS Schema Conversion Tool to convert the schema to PostgreSQL.

B. Provision and configure the target RDS for SQL Server database.

C. Copy active database files with AWS DataSync while SQL Server is running.

D. Use AWS DMS full load plus change data capture.

E. Use AWS Application Migration Service to transform the database engine.

F. Validate the migrated data and cut over after replication lag is within the
approved threshold.

### 21

Traffic from AWS to an on-premises prefix can use either a VPN or Direct
Connect. The VPN advertises `10.40.0.0/16`, while Direct Connect advertises
`10.40.20.0/24`. Both routes are otherwise valid.

Which path will AWS prefer for traffic to `10.40.20.10`?

A. The VPN because encrypted routes always have higher priority

B. Direct Connect because the `/24` is the more specific prefix

C. The path with the shorter BGP AS path, regardless of prefix length

D. Both paths equally because connection type is evaluated before the route

### 22

A software provider must expose a TCP service privately to hundreds of customer
VPCs, including VPCs with overlapping CIDR ranges. Customers must initiate
connections through private IP addresses, and the provider does not want full
network connectivity between VPCs.

Which architecture best meets these requirements?

A. VPC peering from every customer VPC to the provider VPC

B. A Transit Gateway shared with every customer

C. A public Application Load Balancer restricted by source IP

D. An AWS PrivateLink endpoint service behind a Network Load Balancer

### 23 - Choose TWO

An operations team wants centralized visibility into CloudWatch metrics, logs,
and traces from many AWS accounts without copying all telemetry into the
monitoring account.

Which TWO components should be configured for CloudWatch cross-account
observability?

A. An observability access manager sink in the monitoring account

B. A CloudTrail event data store in every source account as the metrics sink

C. A Direct Connect gateway in the monitoring account

D. An observability access manager link from each source account to the sink

E. An IAM user in every source account with console access

### 24

A company must move 250 TB of archived data from a remote facility with a slow,
unreliable network connection. The transfer is one-time, and the facility can
receive and return secure physical appliances.

Which service is most appropriate?

A. Amazon S3 Transfer Acceleration over the existing connection

B. AWS Transfer Family

C. AWS Snowball Edge Storage Optimized devices

D. AWS Application Migration Service

### 25

A role in Account B must decrypt objects encrypted with a customer-managed KMS
key owned by Account A. The S3 bucket policy already permits the role to read
the objects.

Which additional configuration is required?

A. Permission in the KMS key policy in Account A and an identity policy for the
role in Account B

B. Only an SCP in Account B allowing `kms:Decrypt`

C. An S3 ACL granting the KMS key access to Account B

D. A permissions boundary in Account A granting the role access

### 26 - Choose TWO

A Lambda function processes an SQS standard queue in batches. One malformed
message causes the entire batch to be retried repeatedly, delaying good
messages. The company wants good records acknowledged and repeatedly failing
records isolated.

Which TWO changes should be made?

A. Set the source queue retention period to zero.

B. Disable the Lambda event source mapping retry behaviour.

C. Configure the function to return partial batch item failures.

D. Replace the queue with an SNS topic that has no subscriptions.

E. Configure a dead-letter queue and an appropriate redrive policy on the
source queue.

### 27

A transactional database uses a gp2 EBS volume. It repeatedly exhausts burst
credits. The workload needs a predictable 12,000 IOPS and 600 MiB/s but does
not require more than the gp3 maximums.

Which change is the most cost-effective first choice?

A. Increase gp2 capacity until the required IOPS is obtained indirectly.

B. Use a magnetic EBS volume with a larger burst bucket.

C. Move the database files to an S3 Standard bucket.

D. Move to gp3 and provision the required IOPS and throughput independently of
volume size.

### 28

A company uses AWS Application Migration Service to rehost servers. Before the
production cutover, the application team must validate boot behaviour,
connectivity, and post-launch automation in AWS without stopping source-server
replication.

What should the team do?

A. Finalize cutover immediately and delete the source servers.

B. Launch test instances from the replicated source servers and validate them.

C. Export the servers with VM Import/Export after stopping replication.

D. Use DataSync to mount the staging disks.

### 29 - Choose TWO

An enterprise uses AWS Control Tower. Security architects need one control that
prevents disallowed API operations and another that detects noncompliant
resource configurations.

Which TWO statements are correct?

A. Detective controls block the API call before the resource is created.

B. A permissions boundary applied to one role is an organization-wide
preventive control.

C. Preventive controls are commonly implemented through service control
policies.

D. Detective controls can use AWS Config rules to evaluate resources.

E. CloudTrail Event history automatically remediates every violation.

### 30

A financial application requires multi-active writes in two European Regions
and strongly consistent reads from either Region. A third Region can maintain
quorum without serving application traffic. The application uses a key-value
access pattern.

Which database design best meets these requirements?

A. A default DynamoDB global table using multi-Region eventual consistency

B. An S3 Multi-Region Access Point with versioning

C. A DynamoDB global table using multi-Region strong consistency with two
replicas and one witness in a supported Region set

D. An ElastiCache Memcached cluster in each Region

### 31 - Choose TWO

An ECS service uses an Application Load Balancer. The company wants controlled
blue/green releases, a test endpoint for validation, and automatic rollback if
the new task set causes an alarm.

Which TWO components should be used?

A. AWS CodeDeploy with ECS blue/green deployment

B. Systems Manager Patch Manager as the application deployment controller

C. An S3 lifecycle rule to shift traffic

D. Route 53 multivalue answers without a deployment controller

E. Production and test listeners or listener rules associated with the
deployment

### 32

A legacy application and its commercial database must move to EC2 quickly
because the data centre is closing. The vendor does not yet support a managed
database target, and no code or database-engine changes are permitted before
the move.

Which migration strategy should be used first?

A. Rehost

B. Repurchase

C. Refactor

D. Retire

### 33

An IAM permissions boundary allows `ec2:Describe*`, but the role has no
identity-based policy granting EC2 actions. No resource policy applies.

Can the role describe EC2 resources?

A. Yes, because the boundary grants the actions.

B. Yes, because describe actions never require authorization.

C. No, because permissions boundaries can be attached only to IAM users.

D. No, because a permissions boundary is a maximum-permissions filter and does
not grant permissions.

### 34 - Choose TWO

A company serves a static web application from a private S3 bucket through
CloudFront. Users must not access the bucket directly, and common web exploits
must be filtered at the edge.

Which TWO configurations meet these requirements?

A. Enable S3 static website hosting and allow public read access.

B. Attach a security group directly to the CloudFront distribution.

C. Configure CloudFront origin access control and a bucket policy that permits
only the intended distribution access.

D. Associate an AWS WAF web ACL with the CloudFront distribution.

E. Use a gateway endpoint policy as the only control on internet viewers.

### 35

An operations team receives hundreds of alerts during incidents because many
dependent resource alarms enter `ALARM` state together. The team wants one
actionable alarm that fires only when a defined combination of underlying
alarms is true.

Which CloudWatch feature should the team use?

A. Metric Streams

B. Contributor Insights

C. Composite alarms

D. Logs Insights scheduled queries only

### 36

A company is moving a continuously changing on-premises NFS dataset to Amazon
EFS. It needs an initial copy followed by incremental transfers over the
network before a short final cutover.

Which service is the best fit?

A. AWS Snowcone used only in offline mode

B. AWS DataSync

C. AWS Transfer Family AS2

D. AWS Database Migration Service

### 37 - Choose TWO

An organization wants centralized configuration compliance reporting and an
organization-wide API audit trail.

Which TWO statements are correct?

A. Creating an AWS Config aggregator automatically enables configuration
recording in every source account.

B. An AWS Config aggregator can aggregate configuration and compliance data
from source accounts and Regions.

C. CloudTrail replaces AWS Config resource configuration history.

D. AWS Config automatically records every API request body.

E. An organization CloudTrail trail can deliver management events from member
accounts to a central S3 destination.

### 38

A service calls `StartExecution` for a Standard Step Functions workflow. It
retries with the same execution name and identical input while the original
execution is still running.

What is the expected behaviour?

A. The call is idempotent for that running execution and returns the existing
execution information.

B. A second independent Standard execution always starts.

C. The original execution is cancelled and replaced.

D. The request succeeds only for Express workflows.

### 39

A DynamoDB table has ample write capacity, but base-table writes are throttled.
CloudWatch shows throttling on a global secondary index whose partition key has
low cardinality.

What is the best diagnosis?

A. Strongly consistent reads on the GSI are consuming table WCUs.

B. The base table has exceeded the 10 GB LSI item-collection limit.

C. DynamoDB Streams is synchronously blocking every write consumer.

D. GSI write throttling or a hot GSI key is applying backpressure to base-table
writes.

### 40 - Choose THREE

A company must migrate an Oracle database to Aurora PostgreSQL with minimal
downtime. The target engine is different, and stored procedures require
conversion.

Which THREE activities belong in the migration plan?

A. Assess and convert the schema and database code with AWS Schema Conversion
Tool or the applicable conversion tooling.

B. Restore an Oracle RMAN backup directly into Aurora PostgreSQL.

C. Use AWS DataSync to copy active Oracle data files into the Aurora storage
volume.

D. Run AWS DMS pre-migration assessments and remediate reported blockers.

E. Use Amazon ECR to translate stored procedures.

F. Use AWS DMS full load and change data capture for the data migration.

### 41

A company uses Route 53 Application Recovery Controller routing controls for a
multi-Region active-passive application. An operator changes the routing
control during a failover.

What does the routing control directly change?

A. The BGP advertisements from every Direct Connect location

B. The TTL cached in every client resolver worldwide

C. The health-check state used by associated Route 53 records

D. The primary Region of an Aurora Global Database

### 42

Fifty VPCs across several accounts require transitive connectivity to shared
services and on-premises networks. The company wants centralized route control
and does not want a full mesh.

Which service should be the routing hub?

A. VPC peering

B. AWS Transit Gateway

C. AWS PrivateLink only

D. An internet gateway in every VPC

### 43 - Choose TWO

A company operates an Aurora Global Database and is documenting Regional
recovery procedures.

Which TWO statements are correct?

A. Every secondary cluster accepts writes at all times without promotion.

B. Restoring an automated snapshot is the only supported way to change the
primary Region.

C. A planned switchover synchronizes the secondary before changing the primary
and is designed for zero data loss.

D. Aurora Global Database uses synchronous storage replication between all
Regions for every write.

E. An unplanned cross-Region failover can have a non-zero RPO based on
replication lag.

### 44

A bank wants to analyse and modernize a large COBOL mainframe application. It
needs automated code analysis, decomposition assistance, and transformation
capabilities designed specifically for mainframe modernization.

Which AWS service is the best fit?

A. AWS Application Migration Service

B. AWS DataSync

C. Amazon Inspector

D. AWS Transform for mainframe

### 45

An organization centrally owns VPC subnets and wants application accounts in
the same AWS Organization to launch their own resources into those shared
subnets without transferring subnet ownership.

Which service enables this model?

A. AWS Resource Access Manager

B. AWS Directory Service

C. AWS Artifact

D. AWS CloudFormation StackSets without resource sharing

### 46 - Choose TWO

An on-premises application connects to a VPC through Direct Connect and must
invoke an API Gateway private REST API without traversing the public internet.
The application should use private DNS resolution.

Which TWO components are required?

A. An internet-facing Application Load Balancer in front of API Gateway

B. An interface VPC endpoint for `execute-api` that is reachable through the
private network path

C. A NAT gateway in a public subnet

D. A Route 53 Resolver inbound endpoint and appropriate on-premises forwarding
for the private DNS name

E. An API Gateway edge-optimized endpoint

### 47

A team proposes an MRSC DynamoDB global table for sessions that require
strongly consistent reads in multiple Regions. The same table design depends
on DynamoDB TTL to expire sessions automatically.

What should the architect conclude?

A. The design is valid because MRSC supports TTL in every replica.

B. TTL is supported only when the MRSC table has a witness Region.

C. The design must change because TTL is not supported for MRSC global tables.

D. TTL becomes synchronous when strongly consistent reads are enabled.

### 48

External partners must upload files by using SFTP. The company wants a managed
endpoint that authenticates partners and stores uploaded objects directly in
Amazon S3.

Which service should be used?

A. AWS DataSync

B. AWS Transfer Family

C. Amazon S3 File Gateway

D. AWS Application Migration Service

### 49 - Choose TWO

A central network-security team must apply Route 53 Resolver DNS Firewall rule
groups consistently to VPCs across an AWS Organization.

Which TWO services or capabilities support this design?

A. AWS Firewall Manager with a DNS Firewall policy

B. A single network ACL attached to every Route 53 hosted zone

C. AWS Shield Advanced automatic DNS query filtering

D. S3 Block Public Access

E. AWS Organizations integration and a delegated Firewall Manager
administrator

### 50

An EventBridge consumer had a software defect and processed events incorrectly
for six hours. The event bus had archiving enabled before the incident. The
team has fixed the consumer and wants to send the affected historical events
back through the matching rules.

What should the team do?

A. Redrive the events from an S3 lifecycle rule.

B. Restore the event bus from AWS Backup.

C. Reboot the EventBridge service.

D. Replay the relevant time range from the EventBridge archive.

### 51

Hundreds of Lambda execution environments connect directly to an RDS database
during traffic spikes. The database exhausts available connections even though
CPU utilization remains moderate.

Which solution best addresses the problem?

A. Increase the Lambda memory setting only.

B. Enable S3 Transfer Acceleration.

C. Place Amazon RDS Proxy between the functions and the database.

D. Replace security groups with network ACLs.

### 52 - Choose THREE

A migration team is classifying applications according to the AWS migration
strategies.

Which THREE workload-to-strategy mappings are correct?

A. Move an unchanged virtual machine to EC2: refactor.

B. Replace an on-premises email platform with a SaaS product: repurchase.

C. Keep an application on premises temporarily: repurchase.

D. Move an unchanged virtual machine to EC2 with AWS Application Migration
Service: rehost.

E. Convert a monolith into cloud-native services without architectural change:
relocate.

F. Decommission an unused reporting application: retire.

### 53

Administrators normally federate through an external identity provider. The
company requires emergency AWS access if that provider is unavailable.

Which break-glass design is most appropriate?

A. Maintain tightly controlled emergency credentials independent of the
primary IdP, protect them with MFA, monitor their use, and test the procedure.

B. Share the root user password among all administrators.

C. Create permanent administrator access keys on every employee laptop.

D. Disable CloudTrail during emergency access.

### 54

An application needs a simple, distributed in-memory object cache. It does not
need persistence, replication, pub/sub, sorted sets, or automatic failover. The
application can recreate all cached values and benefits from a multithreaded
cache engine.

Which service and engine best fit?

A. DynamoDB Accelerator

B. ElastiCache for Valkey with Multi-AZ enabled

C. Amazon MemoryDB

D. ElastiCache for Memcached

### 55 - Choose TWO

An Application Load Balancer fronts an EC2 Auto Scaling group. The company
wants capacity to track request pressure while keeping an approximate number of
requests per instance.

Which TWO configurations are appropriate?

A. Scale only on total S3 bucket size.

B. Use a scheduled action as the only response to unpredictable traffic.

C. Use an Auto Scaling target tracking policy.

D. Configure CloudTrail Insights as the scaling metric.

E. Use the `ALBRequestCountPerTarget` predefined metric specification.

### 56

A database-migration workload has unpredictable capacity requirements, uses
source and target engines supported by AWS DMS Serverless, and does not need a
custom CDC start point. The company wants to avoid sizing and maintaining a
replication instance.

Which option best meets these requirements?

A. AWS DataSync Enhanced mode

B. AWS DMS Serverless replication

C. AWS Application Migration Service

D. An EC2 instance running S3 sync

### 57

An organization wants to configure Security Hub controls centrally for member
accounts and organizational units. It also wants a member account to manage
the configuration rather than using the management account for daily
operations.

Which design is appropriate?

A. Create one IAM user in every member account and configure controls manually.

B. Use an S3 bucket policy as the Security Hub administrator.

C. Designate a Security Hub delegated administrator and use central
configuration policies from the home Region.

D. Delegate Security Hub administration through a VPC peering attachment.

### 58 - Choose TWO

A global application reads objects from replicated S3 buckets in two Regions.
It needs one global S3 endpoint that routes requests to the closest active
bucket and allows operators to shift S3 data-access traffic during a Regional
event.

Which TWO features meet these requirements?

A. An S3 Multi-Region Access Point

B. An S3 gateway endpoint propagated over Direct Connect

C. S3 Transfer Acceleration without replication

D. S3 Multi-Region Access Point failover controls

E. An EFS mount target in each Region

### 59

A storage team needs organization-wide visibility into S3 usage, activity
trends, and potential cost-efficiency or data-protection findings across many
accounts and Regions.

Which feature should the team use?

A. S3 Storage Lens

B. S3 Select

C. S3 Object Lambda

D. S3 Transfer Acceleration

### 60

An on-premises application must continue using an NFS interface while files
are stored as objects in Amazon S3. Frequently accessed data should be cached
locally, and the solution is operational rather than a one-time migration.

Which service should be used?

A. AWS DataSync

B. AWS Transfer Family

C. AWS Database Migration Service

D. Amazon S3 File Gateway

### 61 - Choose THREE

An organization is designing an SCP that denies most API operations outside
approved Regions while preserving required global services and a controlled
security exception.

Which THREE design elements are appropriate?

A. Assume every global service is automatically exempt from the condition and
omit all exception handling.

B. Use the `aws:RequestedRegion` condition key for the Regional restriction.

C. Use `NotAction` or an equivalent exception list for required global-service
API operations, including services such as IAM, CloudFront, and Route 53 where
needed.

D. Attach a permissions boundary only to one developer role and treat it as
the organization-wide Region control.

E. Use a narrowly scoped principal exception condition for the approved
security or break-glass role.

F. Add an `Allow` statement to the SCP and rely on it to grant workload
permissions.

### 62

An SQS FIFO queue receives order events for many customers. Events for one
customer must be processed in order, but different customers should be
processed concurrently.

How should message groups be assigned?

A. Use one message group for the entire queue.

B. Generate a new random message group ID for every event from the same
customer.

C. Use the customer ID as the message group ID.

D. Omit message group IDs and rely on visibility timeout.

### 63

A company creates a multi-Region KMS key and its replica. An application role
is authorized in the primary key policy but receives `AccessDenied` when using
the replica key.

What is the likely cause?

A. KMS replica keys cannot decrypt ciphertext.

B. Multi-Region keys share key material but each key has an independent key
policy that must authorize access.

C. Multi-Region keys require S3 Transfer Acceleration.

D. The replica automatically inherits every IAM and key-policy grant from the
primary after 24 hours.

### 64 - Choose TWO

A team has completed successful test launches with AWS Application Migration
Service and is preparing for production cutover.

Which TWO actions are appropriate parts of the controlled cutover process?

A. Schedule the cutover window and verify source-server replication is healthy
before launching cutover instances.

B. Delete all source-server data before cutover instances start.

C. Disable networking and monitoring in the target account.

D. Launch cutover instances, validate the application, and finalize cutover
only after acceptance.

E. Convert the workload to Aurora automatically during MGN cutover.

### 65

AWS Firewall Manager reports that it cannot consistently evaluate or apply a
centrally managed policy to member-account resources. The organization and
delegated administrator are configured correctly.

Which prerequisite should be checked?

A. AWS Config recording is enabled as required in the member accounts and
Regions.

B. Every member account has an IAM user named `FirewallManager`.

C. All VPCs have an internet gateway.

D. S3 Transfer Acceleration is enabled for the Firewall Manager policy.

### 66

A data lake stores raw data in S3. The company needs centralized fine-grained
permissions on catalogued tables and columns across producer and consumer
accounts while queries continue to run through Athena.

Which service should govern this access?

A. AWS Artifact

B. Amazon Macie classification jobs only

C. EC2 security groups

D. AWS Lake Formation

### 67 - Choose TWO

A DynamoDB application needs microsecond read latency. The company will use DAX
and requires high availability. The application currently sends all requests
through the DynamoDB SDK client.

Which TWO changes are required?

A. Enable strongly consistent reads through DAX for every request.

B. Deploy a multi-node DAX cluster across Availability Zones.

C. Replace every table partition key with one constant value.

D. Route DAX traffic through an internet gateway.

E. Change the application to use the DAX client and cluster endpoint for the
eligible request path.

### 68

A company decomposes a tightly coupled monolith into independently deployable
managed and serverless services as part of its migration. The application
architecture and code change substantially.

Which migration strategy is this?

A. Rehost

B. Relocate

C. Refactor or re-architect

D. Retain

### 69

An organization delegates administration of a supported security service to a
member account. The team assumes that the delegated administrator can now
apply SCPs to the management account.

Which statement is correct?

A. Delegated administration transfers ownership of AWS Organizations to the
member account.

B. Delegated administration is service-specific and does not make SCPs apply
to the management account.

C. The delegated administrator becomes the root user for every member account.

D. SCPs grant the delegated administrator all service permissions.

### 70 - Choose TWO

A company uses Route 53 Application Recovery Controller routing controls for a
critical active-passive application. Operators must make highly available,
guarded failover changes even when the primary Region is impaired.

Which TWO practices are appropriate?

A. Depend only on the ARC console control plane in the failed Region during an
emergency.

B. Delete Route 53 health checks before every failover.

C. Change routing-control states through the highly available ARC cluster
endpoints.

D. Configure safety rules to prevent unsafe combinations of routing-control
states.

E. Use routing controls to modify client DNS caches directly.

### 71

A workload uses gp3 EBS volumes. The application needs more throughput but does
not need more storage capacity or IOPS.

What is a key gp3 advantage for this requirement?

A. Throughput can increase only by increasing volume size.

B. gp3 performance always depends on burst credits.

C. gp3 volumes cannot be modified after creation.

D. Throughput and IOPS can be provisioned independently of volume size within
gp3 limits.

### 72

Business partners must exchange files with the company by using managed SFTP
and AS2 endpoints. Files must land in Amazon S3, and the company does not want
to operate transfer servers.

Which service should be used?

A. AWS Transfer Family

B. AWS DataSync

C. AWS Application Migration Service

D. Amazon EBS direct APIs

### 73 - Choose TWO

A company has two Direct Connect paths and wants predictable active-passive
routing. The same prefixes are advertised over both paths, but the primary
should be preferred for traffic from AWS to on premises.

Which TWO statements are correct?

A. Apply a higher AWS Direct Connect local-preference BGP community to the
primary advertisement and a lower one to the backup.

B. Make the backup advertise a more specific prefix than the primary to ensure
the primary wins.

C. Connection encryption always overrides prefix specificity and BGP policy.

D. Use Route 53 weighted records to select the BGP path.

E. AWS evaluates the longest prefix match before local-preference communities,
so advertisements should use equivalent prefix lengths when communities are
intended to control preference.

### 74

An Aurora Global Database suffers an unplanned primary-Region outage. The
secondary Region is available, but asynchronous replication lag existed at the
time of failure.

Which statement is accurate?

A. A switchover guarantees zero data loss even though the primary Region is
unavailable.

B. Every committed transaction is guaranteed to exist in the secondary
because cross-Region replication is synchronous.

C. Cross-Region failover can promote the secondary, but the RPO can be non-zero
and depends on replication lag.

D. The secondary can never become a writer.

### 75 - Choose THREE

An enterprise wants stronger ransomware recovery evidence for workloads
protected by AWS Backup. Backups must resist administrator deletion, survive a
source-account compromise, and be regularly demonstrated as restorable.

Which THREE actions best meet these requirements?

A. Store the only recovery point in the workload account's default vault.

B. Use AWS Backup Vault Lock in compliance mode for the protected vault.

C. Treat successful backup-job status as proof that the workload can be
restored within its RTO.

D. Maintain supported cross-account or logically air-gapped backup copies with
separately controlled recovery access.

E. Give workload administrators permission to delete all recovery points
during an incident.

F. Configure AWS Backup restore testing plans and validate restored resources.

---

## Frozen Submission Template

```text
Start:21:25
End: 23:18
Uncertain:3,4,5,7,14,18,23,35,38,42,43,47,52,58,65

1:B
2:AE
3:D
4:BCD
5:A
6:C
7:BE
8:B
9:AE
10:D
11:C
12:AEF
13:B
14:A
15:BE
16:D
17:A
18:CE
19:C
20:BDF
21:B
22:D
23:AD
24:C
25:A
26:CE
27:D
28:B
29:CD
30:C
31:A
32:A
33:D
34:CD
35:C
36:B
37:BE
38:A
39:D
40:ADF
41:C
42:B
43:CE
44:D
45:A
46:BD
47:B
48:B
49:AE
50:D
51:C
52:BDF
53:A
54:D
55:CE
56:B
57:C
58:AD
59:A
60:D
61:BCE
62:C
63:B
64:AD
65:A
66:D
67:BE
68:C
69:B
70:CD
71:D
72:A
73:AE
74:C
75:ABF

Submission status: FROZEN
```
